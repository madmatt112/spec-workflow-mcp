---
id: "d-473aa261"
status: "deferred"
title: "Run the document-phase Lint step on the spec-lint fixture after the plugin re-install"
createdAt: "2026-09-14T21:48:33.036Z"
updatedAt: "2026-09-16T21:13:59.729Z"
resolvedAt: null
originSpec: "spec-lint"
originPhase: "implementation"
revisitTrigger: "After the harness-tool allowlist fix lands — mcp__spec-workflow__harness added to the four SDD orchestrator agent definitions (sdd-document/implementation/closeout/retro-orchestrator) — and the plugin is re-installed and the session restarted. A document phase then persists reviews/lint-brief-<PHASE>-v<D>.md and the round-1 prompt carries `## Changes since`. Blocked this run (run-20260916-194812): the Lint step ran live (role=lint reviser spawns; surfaced a spec-lint citation-identifier false-positive) but the orchestrator could not call `harness brief`, so no lint-brief file persisted. See question-gates retrospective-log harness-defect entry."
tags: ["verification", "harness", "spec-lint", "manual-precondition"]
resolution: null
resolvedInSpec: null
supersededBy: null
supersedes: null
---

## Context
The run needs the plugin re-installed from this checkout and the session restarted (CLAUDE.md: sessions must restart to pick up server changes). No agent can do that mid-session; the orchestrator confirmed the installed plugin skill has no Lint step and the running MCP server has no spec-lint tool. Task 12 ran `npm run build` and `npm test` (green) and staged a scratch spec store at /tmp/scratchpad/sdd/spec-lint/scratch-store/ (README.md names the manual steps; spec `lint-fixture` holds the fixture documents and an agent-rules.md with `## Word caps`). Alternative considered: fabricating the two facts; rejected. The tool half of the scenario is proven by src/tools/__tests__/spec-lint.e2e.test.ts.

## Decision Deferred
The second half of the spec-lint decomposition scenario (requirement 10.3) was not executed: a document-phase requirements run on the task 8 fixture that shows `reviews/lint-brief-requirements-v1.md` before round 1 and a round-1 prompt holding `## Changes since` with the v1 lint diff.

## Revisit Criteria
Run one sdd-document-phase requirements phase on the scratch store's `lint-fixture` spec; confirm `reviews/lint-brief-requirements-v1.md` exists before round 1 and the round-1 prompt contains `## Changes since` with the v1 lint diff; then resolve this record.
