---
id: "d-1880d115"
status: "deferred"
title: "question-gates gate A/B live scenarios: changed-answer (1b), annotation (3b) and both headless runs (2, 4) still unverified"
createdAt: "2026-09-17T00:54:51.021Z"
updatedAt: "2026-09-19T04:17:06.933Z"
resolvedAt: null
originSpec: "question-gates"
originPhase: "implementation"
revisitTrigger: "After question-gates ships in a release and the spec-workflow-harness plugin plus MCP server are re-installed in this project (uninstall then re-install at the same scope; restart the session so the server change loads)."
tags: ["verification", "question-gates", "harness-gates"]
resolution: null
resolvedInSpec: null
supersededBy: null
supersedes: null
---

## Context
The four live gate scenarios drive a live sdd-continue supervisor that calls this spec's new `harness gate` server action and uses the updated sdd-drafter, sdd-document-phase and sdd-continue skills. In this repository the installed plugin and running MCP server are the pre-spec versions; harness/server changes take effect only after the npm package republishes and the plugin re-installs. The tool half was verified in-process: npm run build pass, npm test 1260 passed/2 skipped, claude plugin validate . --strict pass, the veto-rules and harness gate vitest suites assert gate-B ranking (sensitive-path score 2 above keyword score 1), header-row keyword firing, the null/missing sensitive list, and put/get/delete round-trips with the missing-payload guard. A fixture is staged at /tmp/scratchpad/sdd/question-gates/scratch-store/ (agent-rules.md with gates: block and a ## Sensitive paths entry; specs/gate-fixture/requirements.md with five decisions, two direction-setting).

## Decision Deferred
Partially verified live on worktree-review-signals under plugin 5.8.0 (2026-09-17/18, block mode): Scenario 1 (Gate A interactive) — the run paused at requirements v1 and asked four of five ranked decisions with AskUserQuestion; the answers were recorded in specs/worktree-review-signals/questions.md and the run proceeded to round 1 (all answers were delegated, so the "changed answer yields a v2 before any round" half was NOT exercised). Scenario 3 (Gate B interactive) — after tasks approval a single ranked veto list of 7 class-a items (sensitive-path above keyword) was presented and approved; slot b was deleted and implementation began (no annotation was given, so the "one annotation runs exactly one tasks-revision round" half was NOT exercised). Scenarios 2 and 4 (headless via claude -p, record mode) were not run. Still open: 1b, 2, 3b, 4.

## Revisit Criteria
Plugin 5.9.0 is installed here (2026-09-19) and the MCP server is published; after a session restart, run a fresh sdd-continue supervisor against the staged fixture /tmp/scratchpad/sdd/question-gates/scratch-store/ (specs/gate-fixture) — note /tmp may have been wiped by the 2026-09-18 reboot; re-stage if absent. Evidence to show: (1b) changing one direction-setting Gate A answer yields a v2 whose ## Decisions reflect the change before any adversarial round; (2) Gate A headless via claude -p with AskUserQuestion denied writes decisions and "no answer" to questions.md plus a HANDOFF row and proceeds to round 1 on v1; (3b) one Gate B annotation runs exactly one tasks-revision round, then implementation begins; (4) Gate B headless via claude -p writes the veto list to questions.md with a HANDOFF row and proceeds without stalling.
