---
id: "d-1880d115"
status: "deferred"
title: "question-gates gate A/B live end-to-end scenarios 1-4 await the republished plugin+server"
createdAt: "2026-09-17T00:54:51.021Z"
updatedAt: "2026-09-17T00:54:51.021Z"
resolvedAt: null
originSpec: "question-gates"
originPhase: "implementation"
revisitTrigger: "After question-gates ships in a release and the spec-workflow-harness plugin plus MCP server are re-installed in this project (uninstall then re-install at the same scope; restart the session so the server change loads)."
tags: ["verification", "question-gates", "harness-gates"]
resolution: null
resolvedInSpec: null
supersededBy: null
supersedes: null
---

## Context
The four live gate scenarios drive a live sdd-continue supervisor that calls this spec's new `harness gate` server action and uses the updated sdd-drafter, sdd-document-phase and sdd-continue skills. In this repository the installed plugin and running MCP server are the pre-spec versions; harness/server changes take effect only after the npm package republishes and the plugin re-installs. The tool half was verified in-process: npm run build pass, npm test 1260 passed/2 skipped, claude plugin validate . --strict pass, the veto-rules and harness gate vitest suites assert gate-B ranking (sensitive-path score 2 above keyword score 1), header-row keyword firing, the null/missing sensitive list, and put/get/delete round-trips with the missing-payload guard. A fixture is staged at /tmp/scratchpad/sdd/question-gates/scratch-store/ (agent-rules.md with gates: block and a ## Sensitive paths entry; specs/gate-fixture/requirements.md with five decisions, two direction-setting).

## Decision Deferred
Scenarios 1-4 of the decomposition entry (Gate A interactive, Gate A headless, Gate B interactive, Gate B headless) were not exercised end-to-end; only scenario 5 (npm test green + claude plugin validate) plus the in-process tool half were run.

## Revisit Criteria
Run a fresh sdd-continue supervisor against the staged fixture at /tmp/scratchpad/sdd/question-gates/scratch-store/ (specs/gate-fixture). Evidence to show: (1) Gate A interactive — the run pauses at requirements v1 and asks at most five questions; changing one direction-setting answer yields a v2 whose ## Decisions reflect the change before any adversarial round, and the unchanged answers appear in questions.md. (2) Gate A headless via claude -p with AskUserQuestion denied — decisions and "no answer" are written to questions.md, a HANDOFF row is written, and the run proceeds to round 1 on v1 unchanged. (3) Gate B interactive — after tasks approval a single ranked veto list shows the sensitive-path task above a new-dependency item above an out-of-scope item; one annotation runs exactly one tasks-revision round, then implementation begins. (4) Gate B headless via claude -p — the veto list is written to questions.md with a HANDOFF row and the run proceeds to implementation without stalling.
