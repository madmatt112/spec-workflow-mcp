---
id: 02b0033d-55c6-476d-b554-96091f323546
taskId: "2"
specName: tighter-reviews
version: 1
verdict: findings
timestamp: 2026-04-30T14:47:40.947Z
criticalCount: 0
warningCount: 0
infoCount: 4
---

# Task Review: Task 2 (v1)

**Spec:** tighter-reviews
**Reviewed:** 2026-04-30T14:47:40.947Z
**Verdict:** findings

---

## Summary

Test suite covers all design-mandated cases. Sub-agent review found 1 critical (drive-letter strip not mutation-sensitive) and 2 warnings (empty-entry rejection was smoke-only; one mixed-separator case was tautological). All actioned: exposed splitSegments + validateEntries from path-denylist.ts and added direct unit tests; replaced tautological forward-slash test with a drive-letter+forward-slash combined case. 31 tests pass; typecheck clean.

## Findings

### INFO: Drive-letter strip mutation-sensitivity (resolved)
- **File:** src/core/__tests__/path-denylist.test.ts:169
- **Issue:** Original tests at lines 173-179 used C:\proj\secrets\foo.json — without the strip, the 'secrets' segment still triggers denial, so deleting the strip code wouldn't have failed any test. Resolved by exporting splitSegments and adding direct assertions: splitSegments('C:\\proj\\secrets\\foo.json') === ['proj','secrets','foo.json'], splitSegments('C:foo') === ['foo'], plus negative cases for non-drive prefixes. These now fail if the /^[A-Za-z]:/ strip is removed.
- **Task Requirement:** design.md path-denylist §Windows drive-letter handling

### INFO: Empty-entry rejection smoke-only (resolved)
- **File:** src/core/__tests__/path-denylist.test.ts:245
- **Issue:** Original test only re-imported the module — the throw branch was unreachable. Resolved by exporting validateEntries(entries) and unit-testing it directly with [''], ['foo',''], ['','foo'] (all throw) plus ['foo','bar'] and [] (no-throw). Module-load smoke check retained as belt-and-braces.
- **Task Requirement:** Task 2 — empty-entry rejection at construction

### INFO: Forward-slash on Windows tautology (resolved)
- **File:** src/core/__tests__/path-denylist.test.ts:161
- **Issue:** Original test asserted partitionPaths('proj/secrets/foo.json') skips — true on every platform regardless of Windows-specific code. Replaced with paths that combine drive-letter prefix + forward-slash separators (C:/proj/secrets/foo.json, D:/code/.aws/config) which require both the strip and the cross-separator splitter to work together.
- **Task Requirement:** Task 2 — mixed-separator inputs

### INFO [hygiene]: Platform-conditional branches only exercise one side per CI run
- **File:** src/core/__tests__/path-denylist.test.ts:65
- **Issue:** Lines 65-77 and 88-103 use if (CASE_INSENSITIVE_VOLUME) { it(...) } else { it(...) }. Linux CI exercises only the case-sensitive branch; darwin/win32 CI exercises only the case-insensitive branch. This is intentional per the restriction 'do NOT collapse case-folded only on case-insensitive volumes into a platform-blind assertion', but the project's CI matrix should include at least one case-insensitive runner for full coverage. Worth verifying.
- **Task Requirement:** Task 2 — deterministic on POSIX and Windows

