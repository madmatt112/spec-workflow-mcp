---
id: 6226f841-4cd1-4b24-af7e-de69514f71fb
taskId: "17"
specName: tighter-reviews
version: 1
verdict: findings
timestamp: 2026-05-04T19:03:58.886Z
criticalCount: 0
warningCount: 0
infoCount: 0
---

# Task Review: Task 17 (v1)

**Spec:** tighter-reviews
**Reviewed:** 2026-05-04T19:03:58.886Z
**Verdict:** findings

---

## Summary

Implementation faithfully executes the Track-B test plan: 17 composite-pin fixtures, two-way drift test with pinned heading regex and explicit duplicate/empty-block error paths, sentinel test, R4.2a/R4.2b distinguisher via data.diffRejection, and end-to-end secret-leak coverage across all three consumers (diff/hygiene/typecheck coverage+diagnostics). All 534 tests pass. Three info-level observations on documentation/cleanup nits — no blockers.</summary>
<parameter name="findings">[
  {
    "severity": "info",
    "title": "extractDirectiveSentences deviates from literal restriction text by excluding item-9",
    "description": "The task prompt restriction lists 'keep **Read first:** / item-9 / item-10 paragraphs' as the 5-step algorithm. The implementation deliberately EXCLUDES item-9 hygiene paragraphs (numbered list items other than `10.` close zones and are not kept). The deviation is correct — item-9 is fast-reviews boilerplate (R4.8 explicitly says it stays verbatim from fast-reviews and is NOT pinned to R4), and including it would cause Direction B to fail because hygiene prose has no R4 block. The deviation is documented in the test-file comment block and the self-test asserts hygiene prose is excluded. Worth flagging because the literal prompt text is misleading; the implementation correctly chose Direction B integrity over the prompt's literal wording.",
    "file": "/home/mcf/reference/spec-workflow-mcp/src/tools/__tests__/review-task.test.ts",
    "line": 1182,
    "category": "spec-compliance",
    "taskRequirement": "R4.10 Direction B"
  },
  {
    "severity": "info",
    "title": "**Note:** truncation prose is not pinned by either drift direction",
    "description": "Three fixtures (diff-present-truncated, cross-timeout-diff-present-truncated, cross-unavailable-other-diff-present-truncated) emit a `**Note:** data.diffTruncated is true ...` render-side paragraph. Direction B explicitly skips `**Note:**` paragraphs (`if (p.startsWith('**Note:**')) continue;`). Direction A doesn't pin it because it's not in any R4.x block. The composite-pin tests do pin the Note text via fixture equality, so a regression there is caught — but the two-way drift contract has a deliberate hole at this prose. The skip is documented in the test header comment as 'render-side note (not authoritative R4 prose)'. Acceptable given composite-pin is the authoritative regression net for non-R4 prose, but worth surfacing as a known coverage boundary.",
    "file": "/home/mcf/reference/spec-workflow-mcp/src/tools/__tests__/review-task.test.ts",
    "line": 1248,
    "category": "spec-compliance",
    "taskRequirement": "R4.10 Direction B"
  },
  {
    "severity": "info",
    "title": "normalizeFull and normalizeBlock are byte-identical functions",
    "description": "Lines 1006-1014 and 1018-1026 define normalizeFull and normalizeBlock with identical bodies. The spec restriction calls out two pipelines: composite-pin (full whitespace collapse) and boundary-preserving (paragraph split on \\n\\n BEFORE whitespace-collapse). The boundary-preservation has been internalized into `extractDirectiveSentences` (which line-splits and detects paragraph boundaries before any normalization) and `extractR4BlocksFromRequirements` (which walks lines), so by the time text reaches normalizeBlock, boundaries are already extracted. The two functions are therefore correctly equivalent in their current call sites — but the duplication invites future drift if one is changed without the other. Either consolidate into a single `normalize` helper, or add a comment explaining why they're intentionally identical. Minor cleanup, no functional issue.",
    "file": "/home/mcf/reference/spec-workflow-mcp/src/tools/__tests__/review-task.test.ts",
    "line": 1018,
    "category": "hygiene"
  }
]

## Findings

_No findings — clean review._
