# Implementation Log: Task 20

**Summary:** Wired loadSettings + resolveRunnerModel('taskReview') into the task-review initial and retry handlers in multi-server.ts. Replaced ad-hoc readFile/JSON.parse blocks; retry now sorts failed jobs by startedAt DESC to pick the most recent model; pins prior job's model with whitespace-rejecting filter and warn-once fallback; translates the runner's `"A task review is already running for "` duplicate-guard error into a 409 with `{error:'in-flight-spec-task',specName,taskId}`.

**Timestamp:** 2026-05-04T20:49:52.026Z
**Log ID:** d0370f67-28d9-4b3e-99e6-8383f0c98429

---

## Statistics

- **Lines Added:** +73
- **Lines Removed:** -50
- **Files Changed:** 1
- **Net Change:** 23

## Files Modified
- src/dashboard/multi-server.ts

## Files Created
_No files created_

---

## Artifacts

### API Endpoints

#### POST /api/projects/:projectId/specs/:specName/tasks/:taskId/review
- **Purpose:** Initial task review; resolves model via grouped/legacy adversarial-settings, translates runner duplicate-guard into 409 in-flight-spec-task
- **Location:** src/dashboard/multi-server.ts:1750
- **Request Format:** {} (URL params only)
- **Response Format:** { success: true, jobId } | 409 { error:'in-flight-spec-task', specName, taskId } | 400/404 { error }

#### POST /api/projects/:projectId/specs/:specName/tasks/:taskId/review-retry
- **Purpose:** Retry of failed task review; selects most recent failed job by startedAt DESC; pins model from prior job with warn-once fallback; cli/cliArgs always reflect current settings; 409 on concurrent retry
- **Location:** src/dashboard/multi-server.ts:1797
- **Request Format:** {} (URL params only)
- **Response Format:** { success: true, jobId } | 409 { error:'in-flight-spec-task', specName, taskId } | 404 { error:'No failed review job found for this task' } | 400 { error }

### Integrations

#### Integration
- **Description:** Replaces direct readFile+JSON.parse of adversarial-settings.json with shared loadSettings + resolveRunnerModel('taskReview') helpers; matches the adversarial path's wiring (multi-server.ts:789, :960). Retry handler reads failedJob.model (TaskReviewJob.model field added in task 18) and falls back via warnOnce key 'multi-server:retry-task-review-prior-job-not-found'.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

