# Implementation Log: Task 5.3

**Summary:** Extended runProjectTypecheck with the post-parse pipeline: chunked async fs.realpath (100/chunk) for allFiles, listFiles, and diagnostic file paths; ENOENT silently buckets allFiles entries as `coverage.excluded`; non-ENOENT realpath errors warn-once with format `[spec-workflow] typecheck realpath: <code> on <path>`. Set ops are computed on case-folded normalized form (lowercase on darwin/win32); reported `coverage.compiled` and `coverage.excluded` arrays preserve original allFiles paths, deduped by normalized form (first-seen original wins). Diagnostics are tagged `inScope: true` when their normalized file is in the normalized allFiles set. Denylist filtering via `partitionPaths` is applied to compiled, excluded, and diagnostic file paths; total dropped count is surfaced as `suppressedDenylistedFiles` (only emitted when > 0). Diagnostics are then capped at 100 with in-scope-first ordering, setting `truncated: true` when capped (cap operates on the post-denylist set per task restriction). Tagging and capping run after denylist filter, and only the success branch invokes the pipeline — early-return unavailable/timeout branches stay unchanged. The `_allFiles` parameter was renamed to `allFiles` so 5.3's logic actually consumes it.

**Timestamp:** 2026-05-01T15:42:15.046Z
**Log ID:** 3eca2d18-e74f-4fee-b514-c814710e72d5

---

## Statistics

- **Lines Added:** +116
- **Lines Removed:** -4
- **Files Changed:** 1
- **Net Change:** 112

## Files Modified
- src/core/typecheck.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### postProcess
- **Purpose:** Async post-parse pipeline: realpath-normalize allFiles/listFiles/diagnostic paths, compute compiled/excluded set ops on normalized form preserving original-path dedup, tag inScope on diagnostics, denylist-filter all three output arrays, cap diagnostics at 100 in-scope-first.
- **Location:** src/core/typecheck.ts:189
- **Signature:** async function postProcess(allFiles: string[], listFilesOutput: string[], diagnostics: TypecheckDiagnostic[]): Promise<{ compiled: string[]; excluded: string[]; diagnostics: TypecheckDiagnostic[]; suppressedDenylistedFiles: number; truncated: boolean }>
- **Exported:** No

#### realpathChunked
- **Purpose:** Resolve a list of paths via fs.realpath in chunks of 100 using Promise.allSettled. ENOENT failures return undefined silently; non-ENOENT failures warn-once and return undefined. On case-insensitive volumes (darwin/win32) the resolved path is case-folded to lowercase.
- **Location:** src/core/typecheck.ts:81
- **Signature:** async function realpathChunked(paths: string[]): Promise<Resolved[]>
- **Exported:** No

#### warnRealpathOnce
- **Purpose:** Module-private warn-once for non-ENOENT realpath failures, keyed by (code, path) so distinct paths/codes each emit once.
- **Location:** src/core/typecheck.ts:65
- **Signature:** function warnRealpathOnce(code: string, p: string): void
- **Exported:** No

#### caseFold
- **Purpose:** Lowercase a path on case-insensitive volumes (darwin/win32); identity elsewhere.
- **Location:** src/core/typecheck.ts:73
- **Signature:** function caseFold(p: string): string
- **Exported:** No

