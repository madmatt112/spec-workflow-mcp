# Implementation Log: Task 5

**Summary:** Extended the plugin hook sdd-activity.sh (Component 5, Requirement 3) to append spawn boundary events to harness-events.jsonl in the LedgerEvent shape, alongside the unchanged activity writes. On a PreToolUse whose tool_input.subagent_type is an sdd-* agent and whose tool_input.prompt contains a brief path, it appends {ts,type:spawn.start,run,spec,agent,role} where spec is the spec-dir basename and role is the brief-filename stem before -brief. On SubagentStop of an sdd-* agent whose type does not end in -orchestrator, it appends {ts,type:spawn.end,run,spec,agent}. Nothing is written without a matching pointer line or for a non-sdd-* child (3.4). Added a node integration test that drives the real script with a fake pointer file and payloads. Regenerated the three plugins/ copies.

**Timestamp:** 2026-09-15T19:23:32.499Z
**Log ID:** 3ec371be-ecfa-477f-8be8-3fa81d9bd842

---

## Statistics

- **Lines Added:** +217
- **Lines Removed:** -0
- **Files Changed:** 5
- **Net Change:** 217

## Files Modified
- harness/hooks/sdd-activity.sh
- plugins/spec-workflow-harness/hooks/sdd-activity.sh
- plugins/spec-workflow-mcp/hooks/sdd-activity.sh
- plugins/spec-workflow-mcp-with-dashboard/hooks/sdd-activity.sh

## Files Created
- src/__tests__/hook-spawn-events.test.ts

---

## Artifacts

### Integrations

#### Integration
- **Description:** PreToolUse hook emits spawn.start and SubagentStop hook emits spawn.end to harness-events.jsonl, joined by the --watch buildModel spawn-pairing loop
- **Frontend Component:** harness/hooks/sdd-activity.sh (spawn.start/spawn.end LedgerEvent writer, gated on sdd-* subagent + brief path in prompt; role = brief filename stem before -brief; spawn.end skips -orchestrator agents; env vars SDD_EVENTS_FILE and SDD_SPEC added)
- **Backend Endpoint:** src/watch/ledger.ts buildModel spawn.start/spawn.end pairing (unchanged)
- **Data Flow:** orchestrator Agent-tool spawn -> PreToolUse hook writes spawn.start{ts,type,run,spec,agent,role}; worker stop -> SubagentStop hook writes spawn.end{ts,type,run,spec,agent}; both appended as JSON lines to <specDir>/harness-events.jsonl and read by spec-workflow-mcp --watch

