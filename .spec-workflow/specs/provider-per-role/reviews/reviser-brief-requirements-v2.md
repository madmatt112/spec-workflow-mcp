# provider-per-role requirements v2

Read and obey /home/mcf/repo/spec-workflow-mcp/.spec-workflow/agent-rules.md first.

## Job
Produce v2 of /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/requirements.md in place from the revision input below (a Gate A decision change), then report in 150 words or fewer: files touched; the finding as accepted | partially accepted | rejected; citations verified (count); the document's word count; flags. No file contents. Read /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/codebase-context.md first. Cap: 3,500 words — do not grow the document past it. Add the Revision History line for v2: '- **v2** (2026-09-22) — Gate A decision change (refused-at-start run writes no ledger row).' with one nested bullet per finding.

## Findings
RI-1: Gate A changed the decision "A refused run leaves a ledger trace". The human replaced the option "Write run start, a note and run end; spawn nothing" with "Stop before any ledger row". Update the requirements so a run refused AT START — a missing provider key, or a bad/unresolvable provider-map row detected before any task runs — stops before ANY ledger row is written: no run-start row, no note row, no run-end row, and no free text. Correct the relevant decision-log entry (the "A refused run leaves a ledger trace" decision under the Decisions-taken section) AND every acceptance criterion or requirement that currently states a refused-at-start run writes a ledger trace, so the document is internally consistent. This is a MUST_FIX. Do not change the other Gate A decisions, which were approved as recorded: "Refuse, never fall back" (refuse the run with a note), "Failed preflight blocks the spec" (escalate and block remaining tasks until a human rules), "Anthropic total is the headline" (Anthropic tokens headline, DeepSeek beside it, all-provider totals kept in the data), and the provider-map location. This is the only substantive change; keep everything else intact and within the word cap.
