# provider-per-role requirements v4

Read and obey /home/mcf/repo/spec-workflow-mcp/.spec-workflow/agent-rules.md first.

## Job
Produce v4 of /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/requirements.md in place by dispositioning the round-2 adversarial findings, then report in 150 words or fewer: files touched; each finding as accepted | partially accepted | rejected; citations verified (count); the document's word count; flags. No file contents. Read /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/codebase-context.md first. Cap: 3,500 words — the doc is AT the cap (3500), so every fix that adds words must remove the same number elsewhere; do not exceed 3,500. Assess every finding on its merits (accept, partially accept, or reject, each with one line of reasoning). Verify every citation you add or change against the real tree under /home/mcf/repo/spec-workflow-mcp, both ends of any range. Do not widen scope or re-decide the approved Gate A answers. R2-1 is marked fix-induced and Compounds R1-2: it is a cross-artifact seam — the response model field is unsettled between the requested claude-* alias and the message.model DeepSeek-name requirement, and preflight (a) does not gate it. Do NOT reword the R1-2 clause again; write one plain sentence of what the response contract must claim, delete the old text, and settle it across Req 4 crit 4, Req 7 crit 1 and preflight (a) so both ends of the seam agree. Add the Revision History line for v4: '- **v4** (2026-09-22) — Round-2 adversarial response (adversarial-analysis-requirements-r2.md, verdict iterate 1/1/2).' followed by one nested bullet per finding: '- **<id> — <Accepted | Partially accepted | Rejected> (<severity>).** <what changed, or why not>'.

## Findings
Read the full round-2 analysis at /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/reviews/adversarial-analysis-requirements-r2.md and disposition every finding it lists. Verdict: iterate, MUST_FIX 1, SHOULD_FIX 1, MINOR 2. R2-1 (MUST_FIX, fix-induced, Compounds R1-2): the accepted R1-2 alias fix makes the launcher request the claude-* alias the endpoint maps, but Req 4 crit 4 and Req 7 crit 1 still require message.model to be a DeepSeek name; the doc never settles the response model field and preflight (a) does not gate on it, voiding the credential proof. Treat R2-1 as a seam per the job: fix both ends and cite them. Also read the memory file's `## Guidance for Next Review` at /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/reviews/adversarial-memory-requirements.md (read only). Do not re-open the approved Gate A decisions on their merits; if a finding attacks an approved Gate A answer itself (not its internal consistency), reject it with that reason.
