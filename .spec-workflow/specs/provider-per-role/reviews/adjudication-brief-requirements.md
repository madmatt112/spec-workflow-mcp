# provider-per-role requirements, post-cap corrective pass

Read and obey /home/mcf/repo/spec-workflow-mcp/.spec-workflow/agent-rules.md first.

## Open items
R3-1 (MUST_FIX): Preflight (a) is specified to run `--model deepseek-v4-pro` (Req 6 crit 2, line 92), but the launcher is forbidden from ever passing that value (Req 2 crit 5, line 37: `--model` = the `claude-*` alias, never the DeepSeek name). Per the endpoint's settled routing (decomposition.md:255-258) `deepseek-v4-pro` is not a `claude-*` string, so it routes to `deepseek-flash` — the probe measures the wrong model. Worse, the v4 fix made preflight (a)'s recorded `message.model` the oracle for Req 7 crit 1, but the two runs send different `--model` values so the oracle can never be trusted; and Req 7 crit 1 still applies one recorded value to both `spawn.start` (map's requested DeepSeek name, Req 3 crit 1) and `spawn.end` (observed `message.model`, Req 3 crit 2), which cannot both match — the DeepSeek-name assumption R2-1 was accepted to remove still survives. Also, preflight (a)'s pass condition (Req 6 crit 2) only checks the analysis file exists and ends with the verdict block; the `message.model` equality is recorded but never gated. Fix: run preflight (a) with the launcher's own `--model` (the `claude-*` alias), and define a single, checkable expectation for `message.model` that Req 7 crit 1 asserts against. R3-2 (SHOULD_FIX): Preflight (a)'s pass gate is too weak to protect the launcher facts it is meant to prove. Req 6 User Story and D7 promise the launcher is 'built on tested behaviour' and D8 makes a failed preflight (a) block the whole spec, but Req 6 crit 2's pass condition is only 'analysis file exists + verdict block'. The probes Req 6 crit 5 records (auth path, `--agents` key acceptance, transcript location + whether `--session-id` fixes the name, child hooks, effort) and the `message.model` value have no consequence branch except (a)-fail (escalate), (b) (reviser eligibility) and child-hooks. So a launcher-design fact can be falsified while preflight (a) still passes. Fix: give the launcher-critical probes a defined pass/fail consequence, or rule out with a stated reason.
