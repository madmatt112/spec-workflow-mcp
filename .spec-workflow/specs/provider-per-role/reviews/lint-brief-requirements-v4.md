# provider-per-role requirements v4 lint

Read and obey /home/mcf/repo/spec-workflow-mcp/.spec-workflow/agent-rules.md first.

## Job
Fix the lint findings below in v4 of /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/requirements.md in place, then report in 150 words or fewer: files touched; each finding as accepted | partially accepted | rejected; citations verified (count); the document's word count; flags. No file contents. Read /home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/provider-per-role/codebase-context.md first. Cap: 3,500 words — the doc is at 3497; do not exceed 3,500. Assess each finding on its merits, but these are mechanical lint fixes: L-2 (error) must be fixed — give the bare `decomposition.md` citation at line 149 its directory prefix (the spec-store path is `.spec-workflow/spec-decomposition/decomposition.md`; use whatever prefix the other decomposition.md citations in this document already use so all are consistent). L-1 (warning, citation-identifier at line 92): the identifier `model` is absent from the cited ranges — either cite a range that actually contains the `model` token, or remove/rephrase the identifier reference so the citation matches what the ranges contain; verify both ends of any range you change against the real tree under /home/mcf/repo/spec-workflow-mcp. Add NO new version line. Append under the existing v4 Revision History line one nested bullet exactly: '- **Lint pass.** <n> fixed; rejected: <none | L-n reason>'. Every citation you insert or change carries its filename, never a bare `:<line>`.

## Findings
L-1 (warning, citation-identifier, line 92): Identifier 'model' is absent from the cited ranges (harness/skills/sdd-document-phase/references/briefs.md:125-176, harness/skills/sdd-continue/references/formats.md:5-17). L-2 (error, citation-path, line 149): Cited path decomposition.md has no directory prefix; cite it by its path from the code root or spec store (for example dir/decomposition.md). After you accept a fix, search the document for every sibling with the same construct (any other bare decomposition.md citation, any other identifier cited against a range that lacks it) and fix each; list them under the finding's bullet.
