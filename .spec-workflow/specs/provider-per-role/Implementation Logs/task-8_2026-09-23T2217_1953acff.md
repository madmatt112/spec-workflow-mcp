# Implementation Log: Task 8

**Summary:** Made the watch model and render provider-aware (design Component 6, D8, D12; Req 5.5-5.7). In src/watch/ledger.ts: added provider? to SpawnNode, set from e.provider on spawn.start and overwritten by e.provider on the paired spawn.end; added providers? (from runStart.providers) and tokensByProvider {anthropic, deepseek} to RunModel, the split summed beside tokensTotal where a spawn counts as deepseek only when its provider equals 'deepseek', else anthropic; tokensTotal keeps its all-provider sum. In src/watch/render.ts: the header right cell now prints the Anthropic figure as `tokens` and appends ` deepseek <n>` only when the DeepSeek bucket is above zero, keeping the tokens 0 / tokens - branches keyed on tokensTotal and hasActivity; a dim, untruncated `providers <value>` line is pushed under the rule when model.providers is set and not none; the tier line prints `actual <provider> <model>` when the spawn's provider is set and not anthropic, keeping the != test on s.model vs profile.model. Extended both test files.

**Timestamp:** 2026-09-23T22:17:56.286Z
**Log ID:** 1953acff-4abd-4ebc-b8f5-cae909a86890

---

## Statistics

- **Lines Added:** +68
- **Lines Removed:** -4
- **Files Changed:** 4
- **Net Change:** 64

## Files Modified
- src/watch/ledger.ts
- src/watch/render.ts
- src/watch/__tests__/ledger.test.ts
- src/watch/__tests__/render.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### buildModel
- **Purpose:** Watch run model builder; now also computes tokensByProvider {anthropic, deepseek} (a per-provider split of tokensTotal where a spawn is deepseek only when its provider === 'deepseek'), sets SpawnNode.provider from spawn.start (overwritten by spawn.end), and returns providers from run.start's providers key. tokensTotal keeps its all-provider sum (D8).
- **Location:** src/watch/ledger.ts:224-434
- **Signature:** buildModel(input): RunModel
- **Exported:** No

#### render
- **Purpose:** Watch frame renderer; header right cell prints tokens=formatTokens(anthropic) with ' deepseek <n>' appended only when the deepseek bucket > 0 (branch still keyed on tokensTotal>0, else tokens 0/tokens -); pushes a dim untruncated 'providers <value>' line under the header rule when model.providers is set and not 'none' (D12).
- **Location:** src/watch/render.ts:60-176
- **Signature:** render(model: RunModel, opts: RenderOptions): string
- **Exported:** No

#### agentLines
- **Purpose:** Renders one spawn's head + tier line; the tier line's actual cell is now `<provider> <model>` when s.provider is set and not 'anthropic', else `<model>`, keeping the != substitution flag on s.model vs profile.model.
- **Location:** src/watch/render.ts:184-224
- **Signature:** agentLines(s, level, opts, p, warnMin, width): string
- **Exported:** No

### Classes

#### SpawnNode
- **Purpose:** Added provider?: string (the provider that ran the spawn; absent means Anthropic).
- **Location:** src/watch/ledger.ts:94-116
- **Exported:** No

#### RunModel
- **Purpose:** Added providers?: string (run.start's provider map string) and tokensByProvider: {anthropic: number; deepseek: number} (tokensTotal split by provider).
- **Location:** src/watch/ledger.ts:142-168
- **Exported:** No

