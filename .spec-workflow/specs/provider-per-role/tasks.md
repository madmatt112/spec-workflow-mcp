# Tasks Document — provider-per-role

Document version: v4

This spec sends `sdd-reviewer` and `sdd-checker` (and `sdd-reviser` on a passed preflight) to DeepSeek through a `claude -p` child while the session and every other role stay on Anthropic, and splits the ledger, `harness usage` and the watch view by provider. The eight design components map to tasks as follows: Component 3 is tasks 1 and 2; Component 7 is tasks 1 and 3; Component 1 is task 4; Component 2 is task 5; Component 4 is task 6; Component 5 is task 7; Component 6 is task 8; Component 8 is task 9; task 10 is the verification of Requirement 7. No steering document exists on this store, so the standards applied are those of `.spec-workflow/agent-rules.md`: tests beside the module, `harness/` as the single source with the generated `plugins/` copies committed in the same commit, node 20 assertions only.

Dependency order: task 1 writes the launcher body and proves it against DeepSeek; a failed proof or an unset key makes task 1 report `ESCALATE:` and `RETRO: gotcha` (Requirement 6 criteria 6-7). In the run that builds this spec the orchestrator does not yet route `ESCALATE` — task 3's branch is live only from the next session restart — so this is a human-mediated stop, not an automatic halt: no later task checks task 1's outcome before running, and a human reading the retro entry decides whether to continue. Task 2 pins that body with a stub-driven test; task 3 adds the `ESCALATE` flag text task 1's report uses. Task 4 ships the map script, whose eligible set reads task 1's record; task 5 wires the supervisor to tasks 1 and 4 and writes the per-run `launch.sh` text; task 6 routes the document orchestrator to it. Tasks 7 and 8 are additive `src/watch` changes with their tests; task 9 is docs; task 10 is verification only. Every task leaves `npx tsc --noEmit` clean and every existing suite green: tasks 1-6 touch no `src/` module beyond their own new test files; task 7 rewrites one existing assertion (the empty-report literal) and no numeric value; task 8 alters no existing assertion.

- [ ] 1. Preflight: write the launcher body and prove it against DeepSeek
  - File: harness/skills/sdd-continue/references/sdd-launch.sh
  - File: docs/deepseek-preflight.md
  - Write the launcher body design Component 3 specifies, then run probes (a), (b) and the five one-line probes with it from a scratch spec dir, and write the record.
  - Purpose: the launcher is built on measured DeepSeek behaviour and the reviser's eligibility is recorded (design Components 3 and 7, D3-D6, D9-D10, D13, D15-D16; Requirement 6).
  - _Leverage: harness/hooks/sdd-activity.sh:32-49, harness/skills/sdd-continue/references/formats.md:5-17, harness/skills/sdd-continue/references/formats.md:170-182, harness/skills/sdd-continue/references/harness-source.sh:13-21, harness/agents/sdd-reviewer.md:7-12, harness/agents/sdd-reviser.md:7-16, harness/agent-profiles.json:47-56, harness/skills/sdd-document-phase/references/briefs.md:125-197, docs/step-0-answers.md:1-17_
  - _Requirements: 2.5, 2.6, 2.7, 2.8, 3.1, 3.2, 3.3, 3.4, 4.3, 4.4, 6.1, 6.2, 6.3, 6.4, 6.5, 6.6, 6.7_
  - _Prompt: Task: First test `DEEPSEEK_API_KEY` in your environment: when unset or empty, write nothing under `docs/`, call `log-implementation` with the summary `preflight not run: DEEPSEEK_API_KEY unset`, and report `ESCALATE: DEEPSEEK_API_KEY unset; preflight not run` (Requirement 6 criterion 7). Otherwise write harness/skills/sdd-continue/references/sdd-launch.sh as design Component 3 specifies, steps 1 to 8, with the child command, environment and `--agents` JSON from the design's Data Models: usage `bash sdd-launch.sh AGENT "MESSAGE"`; every run value read from the `SDD_*` environment the per-run wrapper exports; step 1 refusals as exit 2 with one stderr line `launcher: REASON` and no row; the `--agents` `effort` from the first existing profiles file, else the frontmatter (D13); a fresh session id per call from `node -e 'console.log(require("crypto").randomUUID())'`; an empty per-run `XDG_STATE_HOME` directory so the child's hooks exit before reading their payload (D3, harness/hooks/sdd-activity.sh:11-12); `--add-dir` only when the store repo is outside the code root (D9); the `--mcp-config SDD_CODE_ROOT/.mcp.json` and `--allowedTools` pair only when the frontmatter lists `mcp__` tools (D10); the transcript at `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/projects/SLUG/SID.jsonl` with the D15 slug rule, summed with a copy of `readUsage` (harness/hooks/sdd-activity.sh:37-49) into `spawn.end` with `provider=deepseek`, `model` and the five digit keys, or `tokens=unknown` alone when the file is missing or sums nothing. Put node code in single-quoted strings without apostrophes, as the hook does (:32-35). Then stage `/tmp/scratchpad/sdd/provider-per-role/preflight/` as a scratch spec dir: an `event.sh` from the formats.md text (:170-182) pointed at a ledger there, a fixture document (a copy of this spec's `requirements.md`), and a review prompt file that names the document, an analysis path under the scratch dir, the round section shape (harness/skills/sdd-document-phase/references/briefs.md:125-197) and the verdict block (harness/skills/sdd-continue/references/formats.md:5-17). Export all nine `SDD_*` values the design's per-run `launch.sh` block lists (.spec-workflow/specs/provider-per-role/design.md:113-121): `SDD_LAUNCH_BODY` the body path just written; `SDD_EVENT_SCRIPT` the scratch dir's `event.sh`; `SDD_SPEC_DIR` the scratch spec dir; `SDD_RUN` a fresh id; `SDD_SPEC` `provider-per-role`; `SDD_CODE_ROOT` and `SDD_HARNESS_REPO` this checkout; `SDD_SPEC_STORE_REPO` the scratch dir, so `--add-dir` covers it; `SDD_PROVIDERS=sdd-reviewer:deepseek:deepseek-v4-pro,sdd-reviser:deepseek:deepseek-v4-pro`; and run (a): `bash sdd-launch.sh sdd-reviewer "Read and execute the instructions in PROMPT_PATH"`. Run (b) the same way for `sdd-reviser` with a prompt that makes one `adversarial-response` call (`specName: provider-per-role`, `phase: requirements`) and writes the first line of the answer to a file; `dist/index.js` must exist first (`npm run build` when it does not). From the two runs' stderr, transcripts and scratch files, record the five one-line probes of Requirement 6 criterion 5: auth by `ANTHROPIC_AUTH_TOKEN` alone; whether `--agents` rejected `tools` or `model`; where the transcript landed and whether `--session-id` named it; whether the child appended anything to the scratch ledger or activity file beyond the launcher's two rows; whether effort was applied. Where a probe shows a flag or path wrong (a rejected `tools` key moves the list to `--tools`; a different directory name changes the slug rule), fix the body and say so in the record. Write docs/deepseek-preflight.md in the docs/step-0-answers.md:1-17 shape: `Date`, `Source` (the commands run, key redacted), a seven-row summary table — (a) reviewer run, (b) reviser MCP call, auth token alone, `--agents` keys, transcript path and `--session-id`, child hooks, effort — and one section per row with the evidence and the consequence Requirement 6 criterion 5 names; the (a) section states the `message.model` string, the transcript path and the usage sums. Run `node scripts/sync-plugin-assets.cjs` and commit the `plugins/` copies with the body. (a) passes when all three hold: the analysis file exists and ends with the verdict block; the transcript was found at the computed path and sums to digits; `message.model` is `deepseek-v4-pro` or `claude-opus-4-8`. On any other (a) outcome write the record with the failure, commit, log, and report `ESCALATE: preflight (a) failed — REASON` and `RETRO: gotcha — the same line` | Restrictions: Never print, commit or write the key anywhere; the child gets it only as `ANTHROPIC_AUTH_TOKEN` with `ANTHROPIC_API_KEY` unset (Requirement 4 criteria 3-5). Do not pass `--bare`, `--no-session-persistence` or `--effort` (design Data Models). Read no file in the projects directory other than the computed transcript (Requirement 3 criterion 3). Do not edit `harness/hooks/`, any skill markdown, or `harness/agent-profiles.json`; do not add `sdd-reviser` to any eligible list, task 4 reads this record. Scratch files stay under `/tmp/scratchpad/sdd/provider-per-role/` | Success: `bash -n harness/skills/sdd-continue/references/sdd-launch.sh` is clean; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass; docs/deepseek-preflight.md holds the seven-row table and a consequence line per section; the scratch ledger holds exactly one `spawn.start` and one `spawn.end` per launcher call, both `provider=deepseek`, the start with `effort=not-applied`; the report says (a) passed with the recorded `message.model` string and whether (b) answered, or carries `ESCALATE:`_

- [ ] 2. Launcher integration test with a stub `claude`
  - File: src/__tests__/launcher.test.ts
  - File: harness/skills/sdd-continue/references/sdd-launch.sh
  - Drive the body task 1 wrote with a stub `claude` first on `PATH` and a temp config root; assert the refusals, the two ledger rows, the child's argv and environment, and both exit paths. Fix the body only where a case fails.
  - Purpose: the launcher is tested like the hook, on node 20 guaranteed fields (design Testing Strategy, requirements D3; Requirement 2 criterion 2, Requirement 7 criterion 5).
  - _Leverage: src/__tests__/hook-spawn-events.test.ts:13-41, src/__tests__/hook-spawn-events.test.ts:58-94, harness/skills/sdd-continue/references/formats.md:170-182, .spec-workflow/agent-rules.md:30-32_
  - _Requirements: 2.2, 2.5, 2.6, 2.8, 3.1, 3.2, 3.3, 3.4, 4.3, 4.4, 7.5_
  - _Prompt: Task: Add src/__tests__/launcher.test.ts in the shape of src/__tests__/hook-spawn-events.test.ts:13-41: a temp root per test carrying `HOME`, `CLAUDE_CONFIG_DIR` and `XDG_STATE_HOME`; a stub `claude` (a bash file, first on `PATH` via `env`) that writes its argv and environment to files in the root, reads its own `--session-id` value, writes the fixture transcript of src/__tests__/hook-spawn-events.test.ts:58-94 at `CLAUDE_CONFIG_DIR/projects/SLUG/SID.jsonl` for the slug of the temp code root, prints a fixed report, and exits per a knob file; a temp spec dir with `event.sh` written from the formats.md text (:170-182); the `SDD_*` exports of the design's per-run `launch.sh` block pointed at those paths, `SDD_LAUNCH_BODY` the real body under `harness/`. Drive the body with `execFileSync('bash', [BODY, agent, message], { env, cwd })` and assert: exit 2 and no row without `DEEPSEEK_API_KEY`, for an agent absent from `SDD_PROVIDERS`, for a mapped agent whose file is missing under `harness/agents/`, and with a `PATH` holding no `claude`; the `spawn.start` keys (`agent`, `role`, `provider`, `model`, `effort=not-applied`, `run`, `spec`); `spawn.end` sums equal to the fixture literals with `model` `claude-opus-4-8+claude-sonnet-5`; the child environment carries no `ANTHROPIC_API_KEY`, the base URL, `ANTHROPIC_AUTH_TOKEN` equal to the key, `ANTHROPIC_MODEL` and `--model` both `claude-opus-4-8` for `deepseek-v4-pro` and `claude-sonnet-5` for `deepseek-flash`, and `XDG_STATE_HOME` under the scratch dir; `--session-id` is a UUID that differs across two calls; `--strict-mcp-config`, `--permission-prompts none`, `--permission-mode auto`, `--output-format text` present and `--tools` the frontmatter list; `--mcp-config` and `--allowedTools` present for `sdd-reviser` only; `--add-dir` present when `SDD_SPEC_STORE_REPO` is outside the code root and absent when equal; stdout equal to the stub report with exit 0; stub exit 1 gives exit 1 and still one `spawn.end`; no transcript gives `tokens=unknown` and no `model` or usage keys. Read only `execFileSync` options `input`, `env`, `cwd`, the thrown error's `status`, `stdout` and `stderr`, and `fs` read, write, mkdir and `existsSync`. Where a case fails because the body task 1 wrote departs from the design, fix the body, re-run `node scripts/sync-plugin-assets.cjs`, and commit the `plugins/` copies with it | Restrictions: Never call the real `claude`; never read `DEEPSEEK_API_KEY` from the outside environment (set a fake value in `env`). Do not change what docs/deepseek-preflight.md recorded; a body fix that contradicts a recorded probe is a `RETRO: doc-gap`. src/__tests__/hook-spawn-events.test.ts is unchanged and this task alters no value it asserts | Success: `npx vitest run src/__tests__/launcher.test.ts src/__tests__/hook-spawn-events.test.ts` green; `npx tsc --noEmit` clean; `npm run check:plugin-assets` passes_

- [ ] 3. `ESCALATE` flag for implementers and the orchestrator's escalate branch
  - File: harness/skills/sdd-implementation-phase/references/briefs.md
  - File: harness/skills/sdd-implementation-phase/SKILL.md
  - File: harness/skills/sdd-continue/references/formats.md
  - Add `ESCALATE: <one line>` to the implementer's flag list and the worker report contract; add the orchestrator branch that reverts the task, records `escalation` and reports `PHASE: escalate`.
  - Purpose: a failed vendor probe stops the spec without reopening design (design Component 7, D11; Requirement 6 criterion 6).
  - _Leverage: harness/skills/sdd-implementation-phase/references/briefs.md:38-45, harness/skills/sdd-implementation-phase/SKILL.md:103-105, harness/skills/sdd-implementation-phase/SKILL.md:177-184, harness/skills/sdd-continue/references/formats.md:119-121, harness/skills/sdd-continue/references/formats.md:130-132, harness/skills/sdd-continue/SKILL.md:247-248_
  - _Requirements: 6.6_
  - _Prompt: Task: In harness/skills/sdd-implementation-phase/references/briefs.md add to the standing brief's flag bullets (:38-45) one bullet: when a task's own instructions say a measured outcome needs a human ruling before any later task, report `ESCALATE: <one line>`. In harness/skills/sdd-continue/references/formats.md add `ESCALATE: <one line>` to the worker-report flag list (:130-132). In harness/skills/sdd-implementation-phase/SKILL.md add a fourth flag bullet after `RETRO:` (:105): `ESCALATE:` ⇒ **Escalate**; then a `## Escalate` section after `## Design defect` (:177-184) in the same shape: revert the task to `[ ]`, append a retro-log entry with `retro.sh` (`escalation`, the flag's line, evidence = task N), write the HANDOFF section, commit the spec store, report `PHASE: escalate`, `STATE: tasks <done>/<total>`, `REASON: <the flag's line>`; the supervisor already stops on it (harness/skills/sdd-continue/SKILL.md:247-248). Run `node scripts/sync-plugin-assets.cjs` and commit the `plugins/` copies in the same commit | Restrictions: Change no other flag's wording or routing; do not touch the document-phase skill's own `ESCALATE` handling of the reviewer's verdict block; MDX rule: `<one line>` only inside code spans | Success: `grep -n 'ESCALATE' harness/skills/sdd-implementation-phase/SKILL.md harness/skills/sdd-implementation-phase/references/briefs.md harness/skills/sdd-continue/references/formats.md` shows the bullet, the branch and the contract line; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [ ] 4. Map script `sdd-providers.sh` and its integration test
  - File: harness/skills/sdd-continue/references/sdd-providers.sh
  - File: src/__tests__/providers-map.test.ts
  - Write the validator design Component 1 specifies, with `ELIGIBLE` set from task 1's record, and a test that drives it on temp `agent-rules.md` files with and without the key.
  - Purpose: a refusal that leaves no ledger row is deterministic and testable (design Component 1, D1, D17; Requirement 1 criteria 1-4, Requirement 4 criterion 2, Requirement 6 criterion 4).
  - _Leverage: harness/hooks/sdd-activity.sh:32-35, src/core/gate-rules.ts:15-22, src/__tests__/hook-spawn-events.test.ts:36-41, docs/deepseek-preflight.md_
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 4.2, 6.4_
  - _Prompt: Task: Write harness/skills/sdd-continue/references/sdd-providers.sh per design Component 1 and the `## Providers` Data Model: `bash sdd-providers.sh AGENT_RULES_PATH`; `ELIGIBLE` as one shell variable at the top, `sdd-reviewer sdd-checker`, plus `sdd-reviser` only when the (b) row of docs/deepseek-preflight.md's summary table says the MCP call was made and answered; exit 0 with stdout `providers=none` for the path `none`, a missing file, no `## Providers` heading, or no bullet before the next `## ` heading; exit 0 with `providers=AGENT:PROVIDER[:MODEL],...` in section order otherwise; exit 2 with one stderr line `providers: REASON` and no stdout for a bullet outside the row grammar `- AGENT: PROVIDER [MODEL]`, an agent outside `ELIGIBLE`, an unknown provider, a `deepseek` row without a model or with a model outside `deepseek-v4-pro | deepseek-flash`, an `anthropic` row with a model, or a duplicate agent; exit 3 with `providers: DEEPSEEK_API_KEY unset; AGENT runs on deepseek` when a `deepseek` row exists and the key is empty in the calling environment. Parse in a `node -e` body in a single-quoted string without apostrophes (harness/hooks/sdd-activity.sh:32-35). Add src/__tests__/providers-map.test.ts driving the script with `execFileSync('bash', [SCRIPT, path], { env })` on temp files (src/__tests__/hook-spawn-events.test.ts:36-41), `env` carrying a fake `DEEPSEEK_API_KEY` except in the exit-3 case: `none` for each of the four empty cases; the two-row value `sdd-reviewer:deepseek:deepseek-v4-pro,sdd-checker:anthropic` in section order and unaffected by other sections before and after; exit 2 with a stderr line for each bad-row case; exit 3 naming `sdd-reviewer` and the key; `ELIGIBLE` names `sdd-reviser` only when the record's (b) row passed (read the record in the test and assert accordingly). Run `node scripts/sync-plugin-assets.cjs` and commit the `plugins/` copies with the script | Restrictions: Read nothing but the rules file and the environment; print nothing on success but the one stdout line; never print the key. Assert only on the thrown error's `status` and `stderr`, `stdout` and `fs` read and write. Do not edit `.spec-workflow/agent-rules.md` here (task 10 adds the section for its scenarios) | Success: `npx vitest run src/__tests__/providers-map.test.ts` green; `bash harness/skills/sdd-continue/references/sdd-providers.sh .spec-workflow/agent-rules.md` prints `providers=none` and exits 0; `npm run check:plugin-assets` passes_

- [ ] 5. Supervisor: map check at the roots step, `providers` on `run.start`, per-run `launch.sh`, launch-prompt lines, event table
  - File: harness/skills/sdd-continue/SKILL.md
  - File: harness/skills/sdd-continue/references/formats.md
  - Run the script task 4 ships at the roots step and refuse before any ledger row; record the map on `run.start`; write the per-run wrapper when a `deepseek` row exists; pass `PROVIDERS` and `LAUNCHER`; document the launcher's rows and the wrapper text.
  - Purpose: the supervisor is the single reader of the map and the only writer of run files (design Component 2, D5, D9, D14; Requirement 1 criteria 3-5, Requirement 2 criterion 1, Requirement 3 criterion 6, Requirement 4 criterion 2).
  - _Leverage: harness/skills/sdd-continue/SKILL.md:49-56, harness/skills/sdd-continue/SKILL.md:71-72, harness/skills/sdd-continue/SKILL.md:76-86, harness/skills/sdd-continue/SKILL.md:190-208, harness/skills/sdd-continue/SKILL.md:419-433, harness/skills/sdd-continue/references/formats.md:101-117, harness/skills/sdd-continue/references/formats.md:163-182, harness/skills/sdd-continue/references/formats.md:186-198_
  - _Requirements: 1.3, 1.4, 1.5, 2.1, 3.6, 4.2_
  - _Prompt: Task: In harness/skills/sdd-continue/SKILL.md, after the agent-rules bullet of the roots step (:71-72), add a **Providers** bullet: run `bash <base dir>/references/sdd-providers.sh <agent-rules path | none>` (the base dir of :49-50); keep its stdout as `PROVIDERS`; on a non-zero exit print its stderr line, the roots line and the status line `<project>:- - refused — <the stderr line>` and stop, so the run-ledger paragraph (:76-86) never runs and no run id, `event.sh`, pointer line or `run.start` exists (D5, D14). In the run-ledger paragraph add `providers=<PROVIDERS value>` to the `run.start` call, and: when the value contains `:deepseek:`, write `/tmp/scratchpad/sdd/<spec>/launch.sh` with the Write tool from the `## Launcher (launch.sh)` text in formats.md with the values filled in, and keep its path as `LAUNCHER`; else `LAUNCHER` is `none`. In the launch prompt block (:190-208) add `PROVIDERS: <the value>` and `LAUNCHER: <path | none>` after `EVENT_SCRIPT`. In harness/skills/sdd-continue/references/formats.md: the `run.start` row (:188) adds `providers`; the `spawn.start` row (:192) adds "the launcher for a DeepSeek-provider worker" under Written by and the keys `provider`, `model`, `effort`; the `spawn.end` row (:193) adds the launcher under Written by and the key `provider`; after the event-script block (:163-182) add a `## Launcher (launch.sh)` section carrying the per-run text of the design's Data Models verbatim (shebang, usage comment, the nine `export` lines, `exec bash "$SDD_LAUNCH_BODY" "$@"`), the fill rules (`BASE_DIR` the supervisor's skill base dir, `MAIN_CHECKOUT` the roots step's main checkout, `HARNESS_REPO_OR_none` the preflight's `source`), and two sentences: the orchestrator calls it as `bash <LAUNCHER> <agent> "<launch message>"` and reads the report from stdout; a row without `provider` reads as `anthropic`. Run `node scripts/sync-plugin-assets.cjs` and commit the `plugins/` copies in the same commit | Restrictions: Do not restate the body's behaviour in the skill; name the script and its exit codes only. Keep the status-line format (:419-433) and the `event.sh` text (:170-182) unchanged. The key is never written into `launch.sh` or the prompt (Requirement 4 criterion 1). MDX rule: angle-bracket placeholders only inside code spans | Success: `grep -n 'sdd-providers.sh\|LAUNCHER\|PROVIDERS' harness/skills/sdd-continue/SKILL.md` shows the roots bullet, the ledger paragraph and the two prompt lines; `grep -n 'Launcher (launch.sh)\|providers' harness/skills/sdd-continue/references/formats.md` shows the section and the three table rows; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [ ] 6. Document orchestrator: route a DeepSeek-provider worker to the launcher
  - File: harness/skills/sdd-document-phase/SKILL.md
  - File: harness/agents/sdd-document-orchestrator.md
  - Add `PROVIDERS` and `LAUNCHER` to the variable list; make the spawn rule run a DeepSeek-provider worker as `bash LAUNCHER AGENT "MESSAGE"`; point the reviewer and checker spawn steps at the rule; state the `PHASE: error` case; update the agent's standing rule.
  - Purpose: only the spawn call changes for a DeepSeek role; briefs, reports and `spawn.usage` are untouched (design Component 4; Requirement 2 criteria 3, 4, 9, 10, 11).
  - _Leverage: harness/skills/sdd-document-phase/SKILL.md:8-11, harness/skills/sdd-document-phase/SKILL.md:23-27, harness/skills/sdd-document-phase/SKILL.md:44-52, harness/skills/sdd-document-phase/SKILL.md:160-164, harness/skills/sdd-document-phase/SKILL.md:278-279, harness/agents/sdd-document-orchestrator.md:9-16, harness/agents/sdd-document-orchestrator.md:50_
  - _Requirements: 2.3, 2.4, 2.9, 2.10, 2.11_
  - _Prompt: Task: In harness/skills/sdd-document-phase/SKILL.md add `PROVIDERS` and `LAUNCHER` to the launch-prompt variable list (:8-11). Rewrite the spawn rule (:23-27): the Agent tool as today, except that a worker `PROVIDERS` lists with `deepseek` (an entry `<agent>:deepseek:<model>`) runs with the Bash tool as `bash <LAUNCHER> <agent> "<launch message>"`, foreground, the exact message the Agent tool would have got, its report being the command's stdout; when such a worker is due and `LAUNCHER` is `none` or the file is missing, report `PHASE: error` with `REASON: launcher missing for <agent>` and never spawn it through the Agent tool; a non-zero launcher exit is the stall of Step 2 item 5. Change Step 2 item 4 (:160-161) and Step 4b item 2 (:278-279) to "spawn per the standing rule" with the same exact message. Leave the `spawn.usage` bullet (:44-52) as it is. In harness/agents/sdd-document-orchestrator.md line 50 say: with the Agent tool, or `bash <LAUNCHER>` for a DeepSeek-provider worker; never a `model` parameter, never `fork`. Run `node scripts/sync-plugin-assets.cjs` and commit the `plugins/` copies in the same commit | Restrictions: Do not touch the implementation-phase skill's spawn rule (no eligible role runs there); do not change the drafter, reviser or adjudicator spawn steps beyond the rule they already follow; the orchestrator's tool list (:9-16) already holds Bash, add nothing. MDX rule: placeholders in code spans | Success: `grep -n 'LAUNCHER' harness/skills/sdd-document-phase/SKILL.md harness/agents/sdd-document-orchestrator.md` shows the variable line, the rule, the error case and the agent rule; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [ ] 7. Usage fold by provider in `src/watch/usage.ts`
  - File: src/watch/usage.ts
  - File: src/watch/__tests__/usage.test.ts
  - File: src/tools/__tests__/harness.test.ts
  - Read `provider` off a spawn's rows, key a DeepSeek spawn's agent cell `agent@deepseek`, add per-provider cells to each phase and the report, and print `anthropic N  deepseek N` on total lines in both formats.
  - Purpose: DeepSeek tokens sit apart from the Anthropic figure the Max plan limit applies to, with every existing figure unchanged (design Component 5, D7; Requirement 5 criteria 1-4).
  - _Leverage: src/watch/usage.ts:13-23, src/watch/usage.ts:38-43, src/watch/usage.ts:76-161, src/watch/usage.ts:163-216, src/watch/usage.ts:258-298, src/watch/__tests__/usage.test.ts:42-73, src/watch/__tests__/usage.test.ts:222-241, src/watch/__tests__/usage.test.ts:263-267, src/tools/__tests__/harness.test.ts:519-530_
  - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - _Prompt: Task: In src/watch/usage.ts add `provider?: string` to `Spawn` (:38-43), set from the opening `spawn.start` row (:89-92); in `reduceSpawn` (:163-216) return `provider` as the last `spawn.end` row carrying `provider`, else the start's, else `anthropic`; export `UsageByProvider { anthropic: UsageCell; deepseek: UsageCell }` and add `providers: UsageByProvider` to `UsagePhase` and `UsageReport` (:15-22), both cells always present; in the aggregation (:109-136) key the agent cell `AGENT` for `anthropic` and `AGENT@deepseek` otherwise, and add each spawn's `spawns`, `tokens` and `unknown` to the phase's provider cell and the report's (:148-158); leave `total`, `kinds`, `orchestratorShare`, `usageDelta` and `headLine` on all-provider values. In `formatOne` (:258-269) append `  anthropic N  deepseek N` (grouped digits) to the end of each phase total line and the spec total line; in `formatCompare` (:271-298) append `  anthropic A | B  deepseek A | B` to the end of each total line, after the delta text, so the substrings src/watch/__tests__/usage.test.ts:258-259 assert still hold (R2-1 left the placement to implementation). Extend src/watch/__tests__/usage.test.ts: provider from `spawn.end`, else `spawn.start`, else `anthropic`; the `@deepseek` key with the anthropic key untouched for the same agent; `providers` cells per phase and spec; the pair on one-spec and compare total lines; rewrite the empty-report literal (:263-267) to add `providers` with zero cells, every number unchanged; the fixture case (:270-283) keeps `4_554_189` and gains `providers.deepseek.spawns` 0 and `providers.anthropic.tokens` equal to `total.tokens`. In src/tools/__tests__/harness.test.ts add one case beside :519-530 asserting `data.report.providers.deepseek` is `{ spawns: 0, tokens: 0, unknown: 0 }` on the review-gate-shape ledger and `data.report.total` unchanged | Restrictions: `src/tools/harness.ts:1063-1084` unchanged; no field renamed; `src/__tests__/fixtures/usage-ledger.jsonl` unchanged; the empty-report literal is the only existing assertion this task rewrites, and it alters no numeric value; src/watch/__tests__/usage.test.ts:222-241 and :243-261 must pass without edits | Success: `npx tsc --noEmit` clean; `npx vitest run src/watch/__tests__/usage.test.ts src/tools/__tests__/harness.test.ts` green_

- [ ] 8. Watch model and render: provider per spawn, tokens by provider, the run's map
  - File: src/watch/ledger.ts
  - File: src/watch/render.ts
  - File: src/watch/__tests__/ledger.test.ts
  - File: src/watch/__tests__/render.test.ts
  - Carry `provider` on `SpawnNode`, `providers` and `tokensByProvider` on `RunModel`; print the Anthropic figure as `tokens` with `deepseek` beside it, a dim `providers` line, and the provider before the model on the tier line.
  - Purpose: the header's `tokens` is the Max plan number and a DeepSeek spawn is visible as such (design Component 6, D8, D12; Requirement 5 criteria 5-7).
  - _Leverage: src/watch/ledger.ts:94-114, src/watch/ledger.ts:142-163, src/watch/ledger.ts:262-288, src/watch/ledger.ts:357, src/watch/ledger.ts:403-422, src/watch/render.ts:67-79, src/watch/render.ts:204-211, src/watch/__tests__/ledger.test.ts:36-46, src/watch/__tests__/render.test.ts:122-132_
  - _Requirements: 5.5, 5.6, 5.7_
  - _Prompt: Task: In src/watch/ledger.ts add `provider?: string` to `SpawnNode` (:94-114), set from `e.provider` at `spawn.start` (:265-275) and overwritten by `e.provider` on the paired `spawn.end` (:276-287); add `providers?: string` and `tokensByProvider: { anthropic: number; deepseek: number }` to `RunModel` (:142-163), the first from `runStart?.providers` in the return (:403-422), the second summed beside `tokensTotal` (:357) as `s.tokens` by `s.provider ?? 'anthropic'` (any other value counts as deepseek only when it equals `deepseek`; else anthropic). In src/watch/render.ts change the header's right cell (:77): when `tokensTotal` is above zero print `tokens <formatTokens(anthropic)>` and, only when `tokensByProvider.deepseek` is above zero, ` deepseek <formatTokens(deepseek)>` after it; keep the `tokens 0` and `tokens -` branches keyed on `tokensTotal` and `hasActivity` as today; after the rule line (:79) push a dim line `providers <value>` only when `model.providers` is set and not `none`. On the tier line (:204-211) print `actual <provider> <model>` when `s.provider` is set and not `anthropic`, else `actual <model>`, keeping the `!=` test on `s.model` against `profile.model`. Extend src/watch/__tests__/ledger.test.ts on the `ledger()` fixture (:36-46): `provider` from a `spawn.start` row, overwritten by `spawn.end`; `tokensByProvider` splits two spawns; `providers` from `run.start`; `tokensTotal` still sums both. Extend src/watch/__tests__/render.test.ts beside :122-132: a `spawn.start`/`spawn.end` pair with `provider=deepseek`, `model=deepseek-v4-pro`, `tokens=182000` renders `tokens 84k deepseek 182k` when the implementer's Anthropic `spawn.end` carries `tokens=84000`, the tier line `actual deepseek deepseek-v4-pro !=`, no `deepseek` word in the header when the DeepSeek spawn is absent, and the `providers sdd-reviewer:deepseek:deepseek-v4-pro` line from a `run.start` carrying it | Restrictions: `tokensTotal` keeps its all-provider sum (D8); src/watch/__tests__/render.test.ts:42-62 and :122-132, src/watch/__tests__/ledger.test.ts:251-273 and src/watch/__tests__/index.test.ts:82-94 must pass without edits, and this task alters no value they assert (the fixture and review-gate ledgers carry no `provider`, so their `tokens` figures are the Anthropic totals); a header line stays within `width`; the `providers` line is dim and not truncated (D12) | Success: `npx tsc --noEmit` clean; `npx vitest run src/watch/__tests__/ledger.test.ts src/watch/__tests__/render.test.ts src/watch/__tests__/index.test.ts` green; `npm run build` then `node dist/index.js --watch . --spec review-gate --once` still prints `tokens 6.3M`_

- [ ] 9. Docs: the fifth machine-read line, the provider model, the split figures
  - File: docs/SDD-HARNESS.md
  - File: docs/TOOLS-REFERENCE.md
  - Name `## Providers` as the fifth machine-read line with its row grammar, eligible set and model names; say a DeepSeek role is a `claude -p` child and the session never changes provider; say the header's `tokens` and the `usage` split are by provider.
  - Purpose: Requirement 1 criterion 6, Requirement 5 criterion 8 (design Component 8).
  - _Leverage: docs/SDD-HARNESS.md:263-282, docs/SDD-HARNESS.md:284-303, docs/SDD-HARNESS.md:330-333, docs/TOOLS-REFERENCE.md:574-577, docs/deepseek-preflight.md_
  - _Requirements: 1.6, 5.8_
  - _Prompt: Task: In docs/SDD-HARNESS.md, under `## Writing agent-rules.md` (:263-282), change the count word in `Four lines are machine-read:` (:271) to `Five` and add a fifth bullet: a `## Providers` section whose bullets `- <agent>: <provider> [<model>]` name a provider per role, `anthropic` (default) or `deepseek` with `deepseek-v4-pro` or `deepseek-flash`, for `sdd-reviewer`, `sdd-checker` and, when docs/deepseek-preflight.md records (b) as answered, `sdd-reviser`; a row outside that set refuses the run before any ledger row. List the bullets with `sed -n '/lines are machine-read:/,/^Keep it short/p' docs/SDD-HARNESS.md | grep -n '^- '` before and after, and keep the count word equal to their number. After the model-policy paragraph (:284-303) add one paragraph: a DeepSeek role runs as a `claude -p` child with its own environment through the supervisor's per-run `launch.sh`; the session never changes provider; the child gets the agent's frontmatter tools only and no MCP server; its declared effort is not applied. Rewrite :330-333 so the header's `tokens` figure is the Anthropic total, the Max plan number, with `deepseek` printed beside it when present, and the launcher writes a DeepSeek spawn's rows itself. In docs/TOOLS-REFERENCE.md extend the `usage` bullet (:574-577): each agent row names its provider when not `anthropic` (`sdd-reviewer@deepseek`), total lines print `anthropic` and `deepseek` figures, and `data.report.providers` carries the cells | Restrictions: Change no other docs section; keep the artifacts table (:155-169) as it is; no bare angle brackets outside code spans; do not edit `README.md` or `CHANGELOG.md` (the release step owns them) | Success: `grep -n 'Five lines are machine-read\|## Providers\|deepseek' docs/SDD-HARNESS.md docs/TOOLS-REFERENCE.md` shows the count word, the bullet, the paragraph, the rewritten watch lines and the usage bullet; `npm run validate:mdx` passes if the script runs on `docs/`, else `npx tsc --noEmit` as the no-op check_

- [ ] 10. End-to-end verification of Requirement 7
  - File: none (verification only)
  - Run the checks and the six corrected scenarios; this task's own implementer (spawned like any task, `harness/skills/sdd-implementation-phase/SKILL.md:84-99`) does the work and reports the supervisor/orchestrator halves as `AFFECTS-FUTURE-SPECS:` lines, since the implementer is barred from `deferrals` (`harness/agents/sdd-implementer.md:35`); the orchestrator, which holds `deferrals`, routes that flag through the Deferral bar (`harness/skills/sdd-implementation-phase/SKILL.md:100-106,167-175`) and files the record, tagged `verification` explicitly since the bar itself names none. Step 8's completion-gate verifier is a different agent that still greps the decomposition entry's superseded wording (requirements Scope notes: Requirement 7's "as written" means the outcome, not that wording) — this task, not that grep, is the corrected check.
  - Purpose: the spec is judged on its ledger and checks (Requirement 7; design Testing Strategy, end-to-end).
  - _Leverage: harness/skills/sdd-implementation-phase/SKILL.md:84-99, harness/skills/sdd-implementation-phase/SKILL.md:100-106, harness/skills/sdd-implementation-phase/SKILL.md:167-175, harness/skills/sdd-implementation-phase/SKILL.md:201-209, harness/agents/sdd-implementer.md:35, harness/agents/sdd-implementation-orchestrator.md:31-33, docs/deepseek-preflight.md, src/watch/__tests__/index.test.ts:82-94_
  - _Requirements: 7.1, 7.2, 7.3, 7.4, 7.5_
  - _Prompt: Task: Run, each as its own command: `npm run check:plugin-assets`, `npx tsc --noEmit`, `npm run build`, `npm test -- --run`, `claude plugin validate . --strict`. Then the scenarios. (1) With `DEEPSEEK_API_KEY` set, stage `/tmp/scratchpad/sdd/provider-per-role/scratch-store/.spec-workflow/specs/provider-per-role/` with its own `event.sh` (formats.md text), build `launch.sh` from the formats.md `## Launcher (launch.sh)` text pointed at it with `SDD_PROVIDERS=sdd-reviewer:deepseek:deepseek-v4-pro`, and run it for `sdd-reviewer` with a review prompt as task 1 built: the analysis file ends with the verdict block; the ledger holds one `spawn.start` (`provider=deepseek`, `model=deepseek-v4-pro`, `effort=not-applied`) and one `spawn.end` (`provider=deepseek`, `model` equal to the string docs/deepseek-preflight.md recorded, digit usage). (2) A ledger written by today's hook path carries no `provider` key: `npx vitest run src/__tests__/hook-spawn-events.test.ts` green stands for it. (3) `env -u DEEPSEEK_API_KEY bash harness/skills/sdd-continue/references/sdd-providers.sh <a temp rules file with a deepseek row>` exits 3 naming the role and the key; `node dist/index.js --watch <scratch store repo> --spec provider-per-role --once` on a spec dir without a ledger prints the no-ledger line (src/watch/__tests__/index.test.ts:79). (4) Fold the (1) ledger with `node -e` importing `dist/watch/usage.js` (`buildUsageReport`, `formatUsageTable`): the reviewer row is `sdd-reviewer@deepseek`, the total line's `deepseek` figure equals its tokens and the `anthropic` figure excludes them. (5) `node dist/index.js --watch . --spec review-gate --once` prints `tokens 6.3M`. (6) is the check list above. The supervisor halves of (1)-(3) (refusal at the roots step, the orchestrator's launcher call, the Anthropic reviser round through the Agent tool) need the merged skills in a restarted session: this task runs as a normal implementer task (`harness/skills/sdd-implementation-phase/SKILL.md:84-99`), not the step-8 verifier of `harness/skills/sdd-implementation-phase/SKILL.md:201-209`, so report `AFFECTS-FUTURE-SPECS: <one line>` naming, as the revisit check, the exact command `run a fixture requirements round of a scratch spec with agent-rules.md naming sdd-reviewer: deepseek deepseek-v4-pro, then with every role anthropic, then with the key unset`; the orchestrator holds `deferrals` and you do not (`harness/agents/sdd-implementer.md:35`); it passes the flag to the Deferral bar (`harness/skills/sdd-implementation-phase/SKILL.md:167-175`), which — only when its three-part test holds — files a `deferrals` record with tag `verification` and that command as `revisitCriteria`, both set explicitly since the bar names no tag on its own | Restrictions: Never print the key; scratch files under `/tmp/scratchpad/sdd/provider-per-role/` only; never run the Playwright suites; do not edit `.spec-workflow/agent-rules.md` of this store (the scratch store carries the test map). With `DEEPSEEK_API_KEY` unset, (1) and (4) are not deferred but failed: report the failure naming the key with `RETRO: bug` (Requirement 6 criterion 7 applies to the run, and the design's end-to-end paragraph says so); no `VERIFY:` line — this task's report uses the implementer's flags, not the step-8 verifier's | Success: every command in the check list exits 0; (1) and (4) show the recorded `message.model` and the split figures; (3) exits 3 and the watch frame shows no run; (5) prints `tokens 6.3M`; the report names each scenario's outcome and carries one `AFFECTS-FUTURE-SPECS:` line naming that command_

## Decisions taken in this document

- D1 — Task 1 bundles the launcher body with the preflight: options were the body as its own first task with the preflight second, a preflight that probes with a throwaway command line, the bundle; chosen because Requirement 6 criterion 1 fixes the preflight as the first task and design Component 7 has it probe with the body itself, so the body must exist inside that task.
- D2 — The eligible-set line is written by task 4 from the preflight record, not by the preflight task: options were the preflight task editing a script that does not exist yet, a placeholder script written before the preflight, the map-script task reading the record; chosen because the record is the single source of the (b) outcome and the script is created after it (design D17 names the editing task; the record decides the value).
- D3 — Compare-mode provider pair goes at the end of each total line, after the delta text: options were after each spec's total cell, at the end of the line; chosen because the existing compare assertions are substring matches on the cells through the delta, and R2-1 left the placement to implementation.
- D4 — Every harness task runs the asset sync and commits the `plugins/` copies in its own commit, so no sync task exists: options were one sync task at the end, per-task sync; chosen because the agent rules require the copies in the same commit as the source.
- D5 — Task 10 is a verification-only task (`File: none`) that names the checks and the six corrected scenarios, run by the `sdd-implementer` the per-task loop spawns for every task (`SKILL.md:84-99`), gate and the step-4 verifier skipped per `SKILL.md:107-109`; the supervisor/orchestrator halves it cannot exercise this run are reported as `AFFECTS-FUTURE-SPECS:` lines, since the implementer is barred from `deferrals` (`sdd-implementer.md:35`), and the orchestrator, which holds `deferrals` (`sdd-implementation-orchestrator.md:31-33`), routes that flag through the Deferral bar (`SKILL.md:100-106,167-175`) and, when its three-part test holds, files the record with tag `verification` set explicitly (the bar names no tag on its own and may instead record a HANDOFF gotcha), not a `VERIFY:`/deferred-report line the per-task loop never routes: options were leaving the scenarios to the completion gate's decomposition grep, a named task; chosen because step 8's own verifier still greps the decomposition entry's superseded wording regardless (requirements Scope notes: "as written" means the outcome, not that wording) — task 10, not that grep, is the actual, corrected verification of Requirement 7.
- D6 — Task 1's failure report carries a retro flag beside the escalate flag: options were the escalate flag alone, both; chosen because the orchestrator running this spec loaded its skill before task 3 lands, and the retro flag it already routes leaves a visible record.
- D7 — Tests for the two scripts live in `src/__tests__/` beside the hook test, not under `harness/`: options were a bash test runner under `harness/`, vitest beside the hook test; chosen because vitest already drives the hook script there and CI runs it on node 20.

## Scope notes

- Carried ruling on Requirement 2 criterion 5 (the `--agents` `model` key carries the request alias, effort from the profile) is written into tasks 1 and 2 as the body's rule and the test's assertion; not re-flagged.
- Carried ruling on Requirement 2 criterion 7 (`--add-dir` when the spec store repo is outside the code root) is written into tasks 1 and 2 as the body's conditional flag and the test's two cases; not re-flagged.
- R2-1 (compare-mode pair placement) gets no task of its own; task 7's prompt fixes the placement (D3).
- The `ESCALATE` branch task 3 adds is harness text; the orchestrator running this spec's implementation loaded the pre-spec skill, so in this run a failed preflight (a) is carried by the implementer's `ESCALATE:` and `RETRO: gotcha` lines (D6; `gotcha` is a category the implementer's brief list already authorises, `harness/skills/sdd-implementation-phase/references/briefs.md:43-45`, unlike `escalation`) and the retro-log entry the orchestrator writes for the latter. The branch is live from the next session restart.
- The supervisor and orchestrator halves of scenarios (1)-(3) need the merged skills in a restarted session; task 10 reports them as `AFFECTS-FUTURE-SPECS:` lines and the orchestrator, per the Deferral bar's three-part test, files the resulting record with tag `verification` set explicitly (D5), while task 10 itself runs the launcher, map-script and fold halves in-process. Without the key, task 1 does not halt the spec automatically (see Dependency order above); regardless, scenarios (1) and (4) are failures, not deferrals, when task 10 itself runs without the key (design Testing Strategy, end-to-end).
- Step 8's completion-gate verifier greps the decomposition entry for `SPEC` and takes only its **End-to-end verification** section (`.spec-workflow/spec-decomposition/decomposition.md:377-387`, `harness/skills/sdd-implementation-phase/SKILL.md:190-191`), which reads the superseded scenario (4) wording at `decomposition.md:383-384`; the entry's other superseded wording at `decomposition.md:344,346` sits in a different bullet the verifier never reads into its brief. Requirements Scope notes already rule that Requirement 7's "as written" means the outcome, not that wording, so this is accepted, not re-decided or fixed here (R1-2).
- Req 3 crit 5, Req 4 crit 1 and Req 4 crit 5 map to no task's `_Requirements:` line; they are negative constraints enforced by restrictions and regression coverage — 3.5 by tasks 7/8's Anthropic-row assertions and task 10 scenario (2); 4.1/4.5 by tasks 4/5/7/8's key-reading restrictions and the folds reading no environment — not an omission (R1-3).
- Spec 9's `harness-run.json` override and control pane are not built; scenario (3) uses `--watch --once` (requirements Scope notes).
- The implementation-phase skill's spawn rule is unchanged: no eligible role runs in that phase.
- A prompt-launched Anthropic reviewer or checker still gets no hook `spawn.start`; not fixed here (requirements and design Scope notes).
- `README.md` and `CHANGELOG.md` are the release step's; no task edits them.

## Revision History

- **v1** (2026-09-22) — Initial draft.
  - **Lint pass.** 2 fixed; rejected: L-1, L-33 (bridge-missing) — narrative ordering only, the naming task does not compile or depend on the later task's artifact contents; L-2–L-11, L-14–L-30, L-34–L-41, L-42–L-54, L-55–L-64, L-65–L-71 (citation-identifier) — each token is either a new artifact/env var/field this spec introduces (correct for a tasks document to cite ahead of its own creation) or an existing artifact already cited correctly elsewhere in the same task's prompt, not tied to the particular range the finding compared it against; L-31, L-32 (citation-identifier) — `ESCALATE` and `escalation` are new text task 3 adds; the cited line is the existing dispatch branch the new text hooks into, not a claim the words already appear there.
- **v2** (2026-09-22) — Round-1 adversarial response (adversarial-analysis-tasks.md, verdict iterate 0/2/2).
  - **R1-1 — Accepted (SHOULD_FIX).** The Dependency-order paragraph's halt claim was false for this run; it now says a failed proof or an unset key makes task 1 report an escalate flag and a retro-log entry, that the orchestrator does not yet route that flag this run, and that this is a human-mediated stop, not an automatic halt. The Scope notes' matching claim that task 1 halts the spec is corrected the same way.
  - **R1-2 — Partially accepted (SHOULD_FIX).** D5's false premise, that naming task 10 makes the end-to-end verifier brief carry the corrected outcomes, is removed; task 10 and D5 now say the completion gate's own verifier still greps the decomposition entry's superseded wording regardless, and a new Scope note records that as accepted per the requirements ruling, not re-decided here. The mismatch between task 10's verifier-style report and the implementer that actually executes it is reconciled: task 10 now files its own deferral record directly and reports a plain failure, instead of a verifier-only pass or fail line the per-task loop never routes. Not accepted: wiring task 10's scenarios into the completion gate's grepped scenario slot, since that would mean editing the decomposition entry, which requirements Scope notes already ruled to record, not re-decide.
  - **R1-3 — Accepted (MINOR).** A Scope note now names the three acceptance criteria that map to no task's requirements list as negative constraints carried by restrictions and regression, with the enforcing tasks listed.
  - **R1-4 — Accepted (MINOR).** Task 1's prompt now names every environment export the design's launcher block lists, instead of naming only four of them.
  - **Lint pass.** 5 fixed; rejected: L-6 — same citation-identifier/bridge-missing class v1's lint pass rejected: each of the 79 flagged tokens, including the v2-introduced `SDD_EVENT_SCRIPT`, `SDD_SPEC_DIR`, `SDD_RUN`, `SDD_SPEC` (design.md's per-run `launch.sh` export block, a new artifact this spec's task 1 implements) and `deferrals`, `add`, `verification`, `revisitCriteria` (the existing `deferrals` tool, its `add` action and `tags`/`revisitCriteria` fields, already used correctly in task 10's own restrictions and success lines), is a new artifact/env var/field this spec creates or an existing artifact cited correctly elsewhere in the same task's prompt, with no range genuinely wrong.
- **v3** (2026-09-22) — Round-2 adversarial response (adversarial-analysis-tasks-r2.md, verdict iterate 1/1/1).
  - **R2-1 — Accepted (MUST_FIX, fix-induced, Compounds R1-2).** The v2 mechanism (task 10's implementer calls `deferrals add` itself) has no permitted executor: `sdd-implementer.md:35` and `briefs.md:49` bar the implementer from `deferrals`. Task 10 now reports the supervisor/orchestrator halves as `AFFECTS-FUTURE-SPECS:` lines instead; the implementation orchestrator, which holds `deferrals` (`sdd-implementation-orchestrator.md:31-33`) and already routes that flag through the existing Deferral bar (`SKILL.md:100-106,167-175`), files the record tagged `verification`. D5, the description bullet, the Prompt's deferral sentence and Success line, the Leverage citation, and the matching Scope note are all corrected; task 10 no longer names a record id it cannot see.
  - **R2-2 — Accepted (SHOULD_FIX, Compounds R1-1).** `RETRO: escalation` is not in the implementer brief's authorised category list (`briefs.md:43-45`: gotcha, bug, tool-error, mcp-deficiency, harness-defect, misunderstanding, inefficiency, doc-gap, model-behaviour), so an implementer following its own brief might not write it. Task 1's report, its Prompt, and the matching Scope note now use `RETRO: gotcha`, a category the brief already authorises; the orchestrator's own `## Escalate` branch (task 3, a different reporter with the full `retro.sh` category set) still uses `escalation` and is untouched.
  - **R2-3 — Accepted (MINOR, Compounds R1-2).** The Scope note said the step-8 verifier reads superseded wording at `decomposition.md:344,346,383-384`; the verifier takes only the **End-to-end verification** section (`:377-387`, `SKILL.md:190-191`), which holds 383-384 but not 344/346 (a different bullet, `:341-350`). The note now cites only what the verifier actually reads.
  - **Lint pass.** 6 fixed; rejected: L-7 — the finding claimed a second bare `SKILL.md` on line 119, but only one occurrence exists there; the file's only other `SKILL.md:190-191` instance is in the v2 Revision History (line 139), historical text this pass does not edit; L-8 — same citation-identifier/bridge-missing class v1 and v2 lint rejected, rule 11 suppresses a token unchanged since its prior rejection, and no range was found genuinely wrong.
- **v4** (2026-09-22) — Round-3 adversarial response (adversarial-analysis-tasks-r3.md, verdict iterate 1/1/1).
  - **R3-1 — Accepted (MUST_FIX, fix-induced, Compounds R2-3).** The Scope note's bare `:383-384` and `:344,346` refs at line 119 bound to the nearest preceding path, `SKILL.md:190-191` (320 lines), and resolved out of bounds. Both are now explicit: `decomposition.md:383-384` and `decomposition.md:344,346`, verified against the real file (377-387 is the End-to-end verification section holding 383-384; 341-350 is the subprocess-spawn bullet holding 344 and 346).
  - **R3-2 — Accepted (SHOULD_FIX, fix-induced, Compounds R2-1).** Task 10's description bullet, its Prompt, D5 and the matching Scope note all asserted the cited Deferral bar (`SKILL.md:167-175`) "already routes" the flag "into one record tagged `verification`." Read whole, the bar is a discretionary three-part gate whose record fields include a generic `tags`, not `verification` by name, and anything failing the bar goes to HANDOFF as a gotcha instead. All four now say the orchestrator routes the flag through the bar and, only when the bar's test holds, files the record with tag `verification` set explicitly — not a guarantee the bar produces on its own.
  - **R3-3 — Accepted (MINOR, fix-induced, Novel).** Task 10's Success line read "one `AFFECTS-FUTURE-SPECS:` line per deferred half" against the Prompt body's "one line, one record, one command." Success now reads "one ... line naming that command," matching the Prompt's single-command, single-record design.
