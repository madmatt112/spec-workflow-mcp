# Implementation Log: Task 5

**Summary:** Created src/core/lint-words.ts (design Component 6, requirements 6.1-6.3, 10.1). Exports DEFAULT_CAPS ({requirements:3500, design:4000, task:150}); wordCount (0 for blank text, else text.trim().split(/\s+/).length, equal to wc -w on ASCII per D5); checkDocWords (doc-words warning on line 1, "<count> words, cap <cap>", when over cap); checkTaskWords (task-words warning on the checkbox line, "task <id|unnumbered>: <count> words, cap <cap>"); parseWordCaps (reads ## Word caps in agent-rules.md, mirroring the parseSensitivePaths bullet scan without calling it, per D6; known keys requirements|design|task, value ^[1-9]\\d*$, unknown keys ignored, other values -> invalid). Findings leave file empty for finishLint. Pure module, no I/O; gate-rules.ts untouched; caps-invalid is emitted by task 7's handler, not here. checkTaskWords takes the document lines in addition to task 2's TaskBlock[] because TaskBlock carries only start/end/promptLines line numbers, not text. Added lint-words.test.ts (12 cases): wc -w equality on a temp ASCII fixture, prompt-line exclusion, the ## block bound, unnumbered id, and parseWordCaps on valid/non-integer/no-heading/unknown-key.

**Timestamp:** 2026-09-14T20:46:10.375Z
**Log ID:** 5dc3911b-6b55-4673-b98d-95637ed0139d

---

## Statistics

- **Lines Added:** +256
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 256

## Files Modified
_No files modified_

## Files Created
- src/core/lint-words.ts
- src/core/__tests__/lint-words.test.ts

---

## Artifacts

### Functions

#### DEFAULT_CAPS
- **Purpose:** Default word caps (template values): requirements 3500, design 4000, task 150
- **Location:** src/core/lint-words.ts
- **Signature:** const DEFAULT_CAPS: LintCaps
- **Exported:** No

#### wordCount
- **Purpose:** Whitespace-token count; 0 for blank text, else text.trim().split(/\s+/).length; equals wc -w on ASCII (D5)
- **Location:** src/core/lint-words.ts
- **Signature:** wordCount(text: string): number
- **Exported:** No

#### checkDocWords
- **Purpose:** doc-words warning on line 1 when the whole document exceeds the cap; message '<count> words, cap <cap>'
- **Location:** src/core/lint-words.ts
- **Signature:** checkDocWords(content: string, cap: number): LintFinding[]
- **Exported:** No

#### checkTaskWords
- **Purpose:** task-words warning on each task block's checkbox line when its lines minus promptLines exceed the cap; message 'task <id|unnumbered>: <count> words, cap <cap>'. Takes the document lines plus task 2's TaskBlock[] (TaskBlock carries only line numbers).
- **Location:** src/core/lint-words.ts
- **Signature:** checkTaskWords(lines: string[], blocks: TaskBlock[], cap: number): LintFinding[]
- **Exported:** No

#### parseWordCaps
- **Purpose:** Parse ## Word caps override in agent-rules.md, mirroring parseSensitivePaths bullet scan (D6, not calling gate-rules); returns {caps, invalid}
- **Location:** src/core/lint-words.ts
- **Signature:** parseWordCaps(markdown: string): { caps: Partial<LintCaps>; invalid: { key: string; value: string }[] }
- **Exported:** No

