# Implementation Log: Task 7

**Summary:** Added the spec-lint tool (design Component 1): specLintTool definition (specName, phase enum, projectPath override, additionalProperties false, readOnlyHint true) and async specLintHandler in nine steps — argument checks, selectRoots, read the <phase>.md document (success:false naming the path on any read error), read agent-rules.md (ENOENT keeps defaults, any other error is a hard failure), parseWordCaps with one caps-invalid info per invalid entry, validateMarkdownForMdx to mdx errors, checkCitations over [workspacePath, workflowRoot, specDir], per-phase checks (requirements: checkEars+checkDocWords; design: checkDocWords; tasks: checkTasksFormat, checkRequirementIds with a null index on a requirements.md read error degraded to the 5.4 info, checkTaskWords using caps.task, checkCoverage with a null component list on a design.md read error degraded to the 7.3 info, checkBridges), then finishLint plus the gate projectContext shape with no nextSteps. Registered the tool in index.ts (import, registerTools array, handleToolCall case). Fixed the root-selection.ts comment to name five tools with spec-lint last and drop the requirement 3.8 reference. Followed the merged code signatures where they differ from the design (checkTaskWords/checkCoverage/checkBridges take lines first) per the orchestrator notes.

**Timestamp:** 2026-09-14T21:11:01.435Z
**Log ID:** dcd017d6-3db5-4370-a778-f1a6c2f0088b

---

## Statistics

- **Lines Added:** +285
- **Lines Removed:** -5
- **Files Changed:** 4
- **Net Change:** 280

## Files Modified
- src/tools/index.ts
- src/tools/root-selection.ts

## Files Created
- src/tools/spec-lint.ts
- src/tools/__tests__/spec-lint.test.ts

---

## Artifacts

### API Endpoints

#### TOOL spec-lint
- **Purpose:** One read-only MCP call that lints a spec document (requirements/design/tasks) and returns findings, summary, checks and caps
- **Location:** src/tools/spec-lint.ts
- **Request Format:** { specName: string (required), phase: 'requirements'|'design'|'tasks' (required), projectPath?: string }
- **Response Format:** { success, message (1.8 text), data: { findings, summary{error,warning,info,total}, checks: LintRule[], caps }, projectContext: { projectPath, workflowRoot, specName, dashboardUrl } }

### Functions

#### specLintHandler
- **Purpose:** Nine-step spec-lint handler: arg checks, selectRoots, read <phase>.md, agent-rules.md caps, mdx, citations, per-phase checks, finishLint, gate projectContext
- **Location:** src/tools/spec-lint.ts
- **Signature:** specLintHandler(args: any, context: ToolContext): Promise<ToolResponse>
- **Exported:** No

#### specLintTool
- **Purpose:** Tool definition, name spec-lint, phase enum, projectPath override, additionalProperties false, readOnlyHint true
- **Location:** src/tools/spec-lint.ts
- **Signature:** const specLintTool: Tool
- **Exported:** No

### Integrations

#### Integration
- **Description:** spec-lint registered in the tool registry and dispatched by name
- **Frontend Component:** registerTools()/handleToolCall switch (src/tools/index.ts)
- **Backend Endpoint:** specLintHandler
- **Data Flow:** handleToolCall('spec-lint', args, context) -> specLintHandler -> ToolResponse -> toMCPResponse

