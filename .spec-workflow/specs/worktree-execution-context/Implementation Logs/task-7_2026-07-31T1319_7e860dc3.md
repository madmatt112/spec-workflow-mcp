# Implementation Log: Task 7

**Summary:** Split both runner contracts and wired all four dashboard route sites. TaskReviewRunner.RunOptions now carries workflowRoot + workspacePath, replacing the single projectPath that was spent three incompatible ways (getSpecPath, ToolContext, spawn cwd). AdversarialRunner.RunOptions stays single-rooted (projectPath renamed to workspacePath) and its workflow root arrives as a second argument to run() -> executeJob -> the private runAgent spawn helper, ready for task 8's SPEC_WORKFLOW_SHARED_ROOT. Both adversarial routes now pass the translated project.workspacePath instead of originalProjectPath (req 5.8). tsc enumerated 45 call sites across 3 files.

**Timestamp:** 2026-07-31T13:19:15.942Z
**Log ID:** 7e860dc3-9c7c-414a-a06d-8daab9c22e18

---

## Statistics

- **Lines Added:** +110
- **Lines Removed:** -67
- **Files Changed:** 5
- **Net Change:** 43

## Files Modified
- src/dashboard/task-review-runner.ts
- src/dashboard/adversarial-runner.ts
- src/dashboard/multi-server.ts
- src/dashboard/__tests__/task-review-runner.test.ts
- src/dashboard/__tests__/adversarial-runner.test.ts

## Files Created
_No files created_

---

## Artifacts

### Classes

#### TaskReviewRunner
- **Purpose:** RunOptions.projectPath REPLACED by two fields (req 5.1). THE THREE-WAY SPLIT: `workflowRoot` -> PathUtils.getSpecPath (:104) and ToolContext.projectPath (:114); `workspacePath` -> ToolContext.workspacePath (:114) and the agent spawn cwd (:161 -> runAgent). Getting getSpecPath wrong (giving it the workspace) makes it resolve a .spec-workflow INSIDE the worktree and every review fails with 'tasks.md not found'. The private runAgent's `cwd` parameter was renamed `workspacePath` and the spawn now reads `cwd: workspacePath`; it already receives `opts`, so it needs no extra root parameter for task 8's env work.
- **Location:** src/dashboard/task-review-runner.ts:27-44 (RunOptions), :104, :114, :161, :371
- **Methods:** run(opts: RunOptions): Promise<string>, private executeJob(jobId, opts), private runAgent(jobId, workspacePath, prompt, opts)
- **Exported:** No

#### AdversarialRunner
- **Purpose:** DELIBERATE ASYMMETRY (req 5.5): RunOptions stays single-rooted — `projectPath` renamed to `workspacePath` — because the runner spends its root at exactly one place (spawn cwd) and never resolves a spec path or builds a ToolContext, so a second field would be dead. The workflow root R2 AC 13 needs at the spawn is threaded as a SECOND ARGUMENT: run(opts, workflowRoot) -> executeJob(jobId, opts, workflowRoot) -> runAgent(jobId, workspacePath, workflowRoot, prompt, opts). Task 8 sets SPEC_WORKFLOW_SHARED_ROOT from that parameter at the spawn env (:170); it is intentionally unused until then. GAP RESOLVED HERE: req 5.5 forbids the RunOptions field but the value must still enter the runner from the caller — the extra run() argument is the only compliant channel.
- **Location:** src/dashboard/adversarial-runner.ts:21-40 (RunOptions), :62, :100, :122, :155
- **Methods:** run(opts: RunOptions, workflowRoot: string): Promise<string>, private executeJob(jobId, opts, workflowRoot), private runAgent(jobId, workspacePath, workflowRoot, prompt, opts)
- **Exported:** No

### Integrations

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** undefined
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

