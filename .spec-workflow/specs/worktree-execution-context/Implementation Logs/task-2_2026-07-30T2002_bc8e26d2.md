# Implementation Log: Task 2

**Summary:** Wrote the non-worktree parity characterization test at src/__tests__/parity-baseline.test.ts (R3 AC 11, R7 AC 8): 20 assertions capturing today's values for a project with no path argument, driven by task 1's GitFixture. Each of requirement 3.11's observables is asserted individually — resolved workspace root (from root and from a subdirectory), resolved workflow root and its equality with the workspace root, containment decisions (accept in-root, accept nested, accept .spec-workflow, reject outside, drop missing, dedupe two spellings), the file set and root reaching computeTaskDiff, the file set and root(s) reaching runProjectTypecheck, tsconfigPath, and projectId with its stored path spelling. No production code was modified. Empirically verified: perturbing resolveGitWorkspaceRoot's fallback (src/core/git-utils.ts:25) to return the raw configured path turns the suite red on exactly the two workspace-root assertions and names the moved value; restoring it returns it green.

**Timestamp:** 2026-07-30T20:02:26.600Z
**Log ID:** bc8e26d2-1fad-4367-ae7c-aa3cb863c426

---

## Statistics

- **Lines Added:** +344
- **Lines Removed:** -0
- **Files Changed:** 1
- **Net Change:** 344

## Files Modified
_No files modified_

## Files Created
- src/__tests__/parity-baseline.test.ts

---

## Artifacts

### Functions

#### rootArgs
- **Purpose:** Extracts the string (root) arguments from a recorded call's argument list, by kind rather than by position. Required because runProjectTypecheck gains a required second root parameter at task 10 (R4 AC 5); a positional recorder would silently start reading the file list out of the wrong slot and the parity assertion would stop asserting anything.
- **Location:** src/__tests__/parity-baseline.test.ts
- **Signature:** rootArgs(args: unknown[]): string[]
- **Exported:** No

#### fileArg
- **Purpose:** Extracts the array (file set) argument from a recorded call's argument list, by kind rather than by position. Same arity-change reason as rootArgs.
- **Location:** src/__tests__/parity-baseline.test.ts
- **Signature:** fileArg(args: unknown[]): string[]
- **Exported:** No

#### toPaths
- **Purpose:** Normalizes filesToReview entries to plain path strings. filesToReview changes from string[] to a labelled { path, root, ambiguous }[] (R4 AC 18 / design section 4); that is a shape change, not a decision change, so normalizing keeps the containment assertions alive across it instead of failing on the shape.
- **Location:** src/__tests__/parity-baseline.test.ts
- **Signature:** toPaths(filesToReview: unknown): string[]
- **Exported:** No

#### withCwd
- **Purpose:** Runs a function with process.cwd() set to a directory, restoring the previous cwd in a finally. Needed because 'no path argument' means parseArguments([]) reads process.cwd(); vitest's default forks pool makes process.chdir available (verified empirically).
- **Location:** src/__tests__/parity-baseline.test.ts
- **Signature:** withCwd<T>(dir: string, fn: () => T | Promise<T>): Promise<T>
- **Exported:** No

#### makeContext
- **Purpose:** Builds the ToolContext passed to reviewTaskHandler. Marked in-file as the site task 3 must extend with the required workspacePath field (same path value, since the roots are equal here) — a compile-driven addition, not a change to any assertion.
- **Location:** src/__tests__/parity-baseline.test.ts
- **Signature:** makeContext(projectPath: string): ToolContext
- **Exported:** No

### Integrations

#### Integration
- **Description:** Observability of the diff and typecheck inputs. vi.hoisted recorder + pass-through vi.mock of '../core/typecheck.js' and '../core/task-diff.js' (the pattern already used by src/tools/__tests__/review-task.test.ts). Each mock pushes its raw argument array onto recorder.typecheckCalls / recorder.diffCalls and then delegates to the real implementation, so the prepare response still carries real values (notably tsconfigPath) while the inputs stay observable. The prepare response reports results, not inputs, so this is the only way to assert 'the file set reaching the diff / the typecheck'.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** reviewTaskHandler(prepare) -> handlePrepare -> validateAllFiles -> [recorded] runProjectTypecheck / computeTaskDiff -> real implementations -> prepare response

#### Integration
- **Description:** Fixture consumption. GitFixture.create('specwf-parity-') builds a realpath-normalized temp root; createRepo('parity-main') is the non-worktree single checkout; createNonGitDirectory('outside-the-repo') hosts the containment-rejection file; createSymlink(repo.path, 'parity-main-link') is the symlinked-configured-path case. Teardown is an explicit afterAll(fixture.cleanup) plus afterAll(cleanupAllGitFixtures) — vitest's worker pool never emits the process exit event (deferral d-2124a571). Verified no specwf-parity-* temp root survives a run.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** GitFixture -> repo + outside dir + symlink -> parseArguments / reviewTaskHandler / ProjectRegistry

#### Integration
- **Description:** Registry isolation. SPEC_WORKFLOW_HOME is pointed at <fixtureRoot>/global-dir before the first ProjectRegistry construction (getGlobalDir reads it per call), and SPEC_WORKFLOW_SHARED_ROOT / SPEC_WORKFLOW_WORKSPACE are deleted for the duration so ambient values cannot steer resolveGitRoot or later resolution. All three are saved and restored in afterAll. Verified the user's real ~/.spec-workflow-mcp/activeProjects.json is untouched.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** beforeAll env swap -> ProjectRegistry -> <fixtureRoot>/global-dir/activeProjects.json -> afterAll restore

