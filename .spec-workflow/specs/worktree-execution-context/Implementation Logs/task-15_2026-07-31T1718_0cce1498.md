# Implementation Log: Task 15

**Summary:** Moved realpath normalization INSIDE generateProjectId (requirement 1.10) so every call site — including the dashboard's manual-add path, which never goes through registerProject — normalizes by construction; normalized both stored paths in registerProjectLocked identically (1.11); added requirement 1.12's fallback log via a new optional onFallback callback on normalizeIdentityPath, deduplicated per distinct path; extended unregisterProjectById with the existing optional-pid convention and made unregisterProject delegate to it; cached the registration id on SpecWorkflowMCPServer and used it in stop() (1.13). DECISION: readRegistry normalizes on WRITE ONLY — it keeps the pure resolve() and performs no realpath on read. Rationale recorded in a doc block on readRegistry.

**Timestamp:** 2026-07-31T17:18:26.080Z
**Log ID:** 0cce1498-f3e6-4706-aaec-f0adf8a93313

---

## Statistics

- **Lines Added:** +438
- **Lines Removed:** -37
- **Files Changed:** 6
- **Net Change:** 401

## Files Modified
- src/core/project-registry.ts
- src/core/git-utils.ts
- src/server.ts
- src/core/__tests__/project-registry.test.ts
- src/core/__tests__/registry-lock.test.ts
- src/__tests__/parity-baseline.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### generateProjectId
- **Purpose:** UNCHANGED NAME, CHANGED CONTRACT — it was a pure SHA-1 of its argument and is now filesystem-dependent: it calls normalizeIdentityPath(projectPath, logIdentityFallback) first and hashes the normalized string. The normalization is INSIDE deliberately (requirement 1.10). Normalizing at the register site only would be WORSE than not normalizing, because the server unregisters by path: a symlinked workspace would register under the physical id and unregister under the link id and strand its entry forever. Inside, both sides of any id comparison normalize together — which is why parity-baseline's `expect(projectId).toBe(generateProjectId(linkPath))` stayed green. COST, stated: one realpathSync per CALL, not per registry entry. readRegistry does not call this function (it uses the ids already stored as map keys), so reading N entries still costs zero realpath calls; the per-request read paths (getProject, isProjectRegistered, unregisterProject) pay one syscall each. The returned value can MOVE if the filesystem does — realpath fails once a directory is removed and the deterministic fallback returns a different string, hence a different id. That is exactly why requirement 1.13 forbids recomputing an id to unregister. Exported.
- **Location:** src/core/project-registry.ts:75
- **Signature:** generateProjectId(projectPath: string): string
- **Exported:** No

#### logIdentityFallback
- **Purpose:** Requirement 1.12's log. Deduplicated through a module-level Set (loggedIdentityFallbacks) keyed by absolute path, so the line is emitted once per distinct path per process rather than once per call. Without the dedupe a dashboard with one dead worktree in its registry prints a line per request forever. The fallback itself lives in normalizeIdentityPath and is deterministic; this only reports it. Passed as normalizeIdentityPath's onFallback at all three identity sites in this module.
- **Location:** src/core/project-registry.ts:30
- **Signature:** logIdentityFallback(error: unknown, absolutePath: string): void (module-private)
- **Exported:** No

#### normalizeIdentityPath
- **Purpose:** EXTENDED by task 15 with an optional onFallback callback — the logging seam task 3's doc comment reserved for requirement 1.12. Backward compatible (all four existing call sites pass one argument), and the returned value is identical whether or not the callback is supplied, so determinism is untouched. The function still does NOT log on its own: it runs on read paths the dashboard exercises per request, so an unconditional console.error here would emit a line per request per dead entry. The caller with an identity at stake supplies the sink and decides the rate limiting.
- **Location:** src/core/git-utils.ts:93
- **Signature:** normalizeIdentityPath(p: string, onFallback?: (error: unknown, absolutePath: string) => void): string
- **Exported:** No

#### ProjectRegistry.unregisterProjectById
- **Purpose:** EXTENDED with the SAME optional-pid convention unregisterProject already carried — deliberately not a second near-duplicate method name, which would leave two removal paths to keep in step. pid given: removes only that instance, deleting the entry when it was the last. pid omitted: removes the entry. Now early-returns when the id is absent instead of writing the registry back unchanged. THIS IS THE ONE server.stop() MUST CALL (requirement 1.13).
- **Location:** src/core/project-registry.ts:325
- **Signature:** async unregisterProjectById(projectId: string, pid?: number): Promise<void>
- **Exported:** No

#### ProjectRegistry.unregisterProject
- **Purpose:** Now a one-line delegate to unregisterProjectById(generateProjectId(projectPath), pid); the duplicated instance-filtering body is gone. Its doc comment marks it as the path-based overload to AVOID: it recomputes the id, and realpath fails once the directory is gone, so after `git worktree remove` the recomputed id is the fallback's id and the entry is never deleted. Kept for the dashboard/back-compat callers that only have a path.
- **Location:** src/core/project-registry.ts:311
- **Signature:** async unregisterProject(projectPath: string, pid?: number): Promise<void>
- **Exported:** No

#### ProjectRegistry.registerProjectLocked
- **Purpose:** workspacePath and workflowRootPath are now normalizeIdentityPath(...) rather than resolve(...) (requirement 1.11). BOTH, not just the workspace: generateProjectDisplayName decides between 'repo' and 'repo · worktree' by string-comparing the two, so normalizing one and not the other names a symlinked main checkout as a worktree of itself — and workflowRootPath is the root the dashboard runs git in. The id is computed from the already-normalized workspacePath, so identity and stored path are one spelling by construction.
- **Location:** src/core/project-registry.ts:257
- **Signature:** private async registerProjectLocked(projectPath, pid, options): Promise<string>
- **Exported:** No

### Classes

#### SpecWorkflowMCPServer.registeredProjectId
- **Purpose:** New private field caching the projectId returned by registerProject (assigned at src/server.ts:193). stop() now calls unregisterProjectById(this.registeredProjectId, process.pid) instead of unregisterProject(this.workspacePath, process.pid) — requirement 1.13. Undefined until registration returns, and stop() skips unregistration when it is unset, because stop() can run after initialize threw earlier than registration and there is then nothing registered to remove. A lock-budget failure still returns an id, and unregistering by an id with no entry is a no-op.
- **Location:** src/server.ts:90
- **Exported:** No

### Integrations

#### Integration
- **Description:** THE readRegistry DECISION, recorded in a doc block on the method itself: normalize on WRITE ONLY. readRegistry keeps the pure resolve() and performs NO realpath on read. Three reasons, and the two options differ observably so the choice is explicit. (1) ENTRIES WRITTEN BEFORE THIS CHANGE carry a link spelling under a link-spelling id. realpath-on-read would rewrite the entry VALUE to the physical path while the map KEY stayed the link-spelling id, because ids are stored data and are not recomputed on read — that makes identity and stored path disagree, the exact condition requirement 1.11 forbids, and makes it worse rather than better. Genuinely repairing such an entry means re-keying the map, which is a migration; a read path must not silently rewrite identities. Left alone, the entry is replaced the next time that workspace registers and cleanupStaleProjects removes the old one once its instances die. (2) A REMOVED WORKTREE has no realpath, so realpath-on-read would make the served spelling depend on filesystem state at read time; normalize-on-write means a read returns the same string before and after the directory disappears, which is what lets the dashboard list and serve the survivors unchanged. (3) COST: reads are the hot path (every dashboard request, every registry-watcher event) and realpath-on-read is O(entries) synchronous syscalls per read on a path that today performs none — and blocks the event loop if one path sits on an unresponsive mount.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** register: path -> normalizeIdentityPath (realpath, logged fallback) -> stored projectPath/workflowRootPath AND generateProjectId -> registry key. read: JSON -> resolve() only, no realpath. unregister: cached projectId -> registry key

#### Integration
- **Description:** 13 new tests in src/core/__tests__/project-registry.test.ts under 'ProjectRegistry identity normalization', describe.skipIf(win32), grouped by requirement. Every path is built under a REALPATH-NORMALIZED temp root (fs.realpath of fs.mkdtemp) so the only symlinks in play are the ones a test creates — otherwise macOS's /var -> /private/var makes resolve and realpath disagree everywhere and the assertions stop meaning what they say. Helper symlinkedWorkspace(name) builds <root>/real/<name> plus <root>/link -> <root>/real and returns both spellings. 1.10: same id for link and physical; registering via the link makes isProjectRegistered true for BOTH spellings (the guard against normalize-at-the-register-site — it fails if normalization leaves generateProjectId); register-via-link then unregisterProject-via-link empties the registry. 1.11: stored projectPath AND workflowRootPath are the physical spelling and entry.projectId === generateProjectId(entry.projectPath); a symlinked main checkout is named 'repo', not 'repo · repo'; a worktree case pins 'main-repo · feature-a'. 1.12: the fallback is deterministic and agrees with resolve(); an entry whose directory was REMOVED still reads back beside a live one (2 entries, no throw) with its stored spelling unmoved. 1.13: the PREMISE test — recomputing the id after removing a symlinked worktree yields a DIFFERENT id; unregisterProjectById(cachedId, pid) deletes it anyway; and unregisterProject(path, pid) demonstrably LEAVES THE ENTRY BEHIND, kept as a live reproduction so a revert to path-based unregistration is caught here rather than in production; plus pid-vs-no-pid semantics (two instances written directly, because registerProject filters dead pids and a test process has only one live pid) and an unknown id being a no-op. Teardown is an explicit afterAll rm over every created root — vitest workers never emit process 'exit'; verified no specwf-registry-identity-* survives a run.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** fs.realpath(mkdtemp) root -> real dirs + deliberate symlink -> ProjectRegistry (SPEC_WORKFLOW_HOME per test) -> afterAll rm

#### Integration
- **Description:** PARITY BASELINE, the one permitted edit. src/__tests__/parity-baseline.test.ts, symlink case only. Exactly the two moves the file's own comment authorized: `expect(projectId).not.toBe(generateProjectId(repo.path))` became `toBe` (requirement 1.10), and `expect(entry?.projectPath).toBe(resolve(linkPath))` became `toBe(await fs.realpath(linkPath))` (requirement 1.11); the title moved with them ('...under the id of the physical path'). UNTOUCHED AND GREEN: `expect(projectId).toBe(generateProjectId(linkPath))` — it never went red, which is the evidence that normalization went inside generateProjectId rather than to the call site; LITERAL_ID / LITERAL_ID_PATH (a constant path that does not exist, so the deterministic fallback yields the same string and the same id); and every other test in the file. A DONE banner was added to the case's comment recording that task 15 made the change and that the permission is now SPENT — the test is off limits to every task including task 15, and a failure there is a regression.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** n/a — characterization test

#### Integration
- **Description:** src/core/__tests__/registry-lock.test.ts (task 4's suite) needed two consequential edits, both forced by generateProjectId becoming filesystem-dependent. (a) testRoot is now realpath'd at creation, so a platform whose tmpdir() is itself a symlink cannot make a registered path and its stored spelling differ for reasons unrelated to that suite. (b) The 6-process concurrent-registration test registered SIX NONEXISTENT workspace directories, so requirement 1.12's fallback log landed on every child's stderr and broke its `expect(result.stderr).toBe('')` assertion. The workspaces are now created — but under a SEPARATE root, not under globalDir, because creating them under globalDir would create globalDir and destroy the ENOENT-not-EEXIST case of requirement 6.2 that the test exists to cover. workflowRootPath moved to that same root for the same reason. Both edits are commented in place.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** child process -> registerProject(real workspace dir) -> clean stderr -> parent assertions

