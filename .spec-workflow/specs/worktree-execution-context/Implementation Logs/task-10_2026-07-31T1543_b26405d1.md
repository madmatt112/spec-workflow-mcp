# Implementation Log: Task 10

**Summary:** Converted TaskReviewRunner.buildPrompt from eleven positional parameters to a single BuildPromptOptions object FIRST (verified tsc + tests green at that point), then changed filesToReview to the labelled ResolvedFile[] shape and updated its renderer to `- <path> (<root>)`. In handlePrepare, deleted the `.map(p => path.resolve(projectPath, p))` pre-resolution (keeping the `new Set`), threaded a workspacePath parameter, and passed the two roots distinctly to resolveLoggedFiles. Routed fileResolution.workspaceFiles to computeTaskDiff (whose root argument is now workspacePath), runProjectTypecheck and computeHygieneSignals, while spec, steering, settings, workflow-root report and the typecheck root stay on the shared workflow root.

**Timestamp:** 2026-07-31T15:43:16.795Z
**Log ID:** b26405d1-6e79-4033-b150-5187508fff4b

---

## Statistics

- **Lines Added:** +322
- **Lines Removed:** -68
- **Files Changed:** 4
- **Net Change:** 254

## Files Modified
- src/tools/review-task.ts
- src/dashboard/task-review-runner.ts
- src/tools/__tests__/review-task.test.ts
- src/dashboard/__tests__/task-review-runner.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### buildPrompt
- **Purpose:** CONVERTED to an options object (requirement 4.22). Previously eleven positional arguments, several mutually assignable (string, string | null), so inserting one shifted methodology into the new slot, outputPath into methodology and the real output path into a nullable string — all type-checking, with the agent silently never learning where to write its result. The conversion landed and was verified green BEFORE any parameter shape changed. `filesToReview` is now typed `ResolvedFile[]`, imported as a type from src/core/file-resolution.js; that declaration is the ONLY thing making the renderer's `.path` access checked, because the value arrives through an any-typed destructure of prepareResponse.data at :161.
- **Location:** src/dashboard/task-review-runner.ts:335 (BuildPromptOptions at :61, call site at :189)
- **Signature:** private buildPrompt(opts: BuildPromptOptions): string
- **Exported:** No

#### handlePrepare
- **Purpose:** Gained a REQUIRED fifth parameter `workspacePath`, inserted between `projectPath` and `context`. The two roots are now spent differently: projectPath (workflow root) -> getSpecPath, getSteeringPath, loadSettings, getWorkflowRoot, runProjectTypecheck's root arg (task 11 splits that one) and unwrapTypecheck's tsconfigPath; workspacePath -> resolveLoggedFiles' workspace anchor and computeTaskDiff's root argument (requirement 4.1).
- **Location:** src/tools/review-task.ts:242
- **Signature:** handlePrepare(specPath, specName, taskId, projectPath, workspacePath, context): Promise<ToolResponse>
- **Exported:** No

### Integrations

#### Integration
- **Description:** PRE-RESOLUTION DELETED (requirement 4.7). `src/tools/review-task.ts:301` was `[...new Set(...)].map(p => path.resolve(projectPath, p))`; the `.map` is gone and the `new Set` stays. The Set dedupes RAW strings and is a different job from the resolver's realpath dedupe — `src/foo.ts` and `./src/foo.ts` both survive it and only collapse inside resolveLoggedFiles (requirement 4.11). resolveLoggedFiles now receives the two roots DISTINCTLY: `{ workspacePath, workflowRoot: projectPath }`. Ordering was load-bearing: deleting the .map alone is safe because the resolver re-anchors against the workflow root, but partitioning the roots first would anchor every relative entry to the wrong tree and drop it on containment.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** log filesModified+filesCreated -> new Set (raw-string dedupe) -> resolveLoggedFiles(raw, {workspacePath, workflowRoot}) -> FileResolution -> .files (labelled, to the reviewer) and .workspaceFiles (to diff/typecheck/hygiene)

#### Integration
- **Description:** TWO-ROOT ROUTING. `fileResolution.workspaceFiles` — not `.files` — is the file list handed to runProjectTypecheck (4.25), computeHygieneSignals (4.26) and computeTaskDiff (4.23), so a `.spec-workflow` document reaches the reviewer's labelled list but none of the three code operations. computeTaskDiff's ROOT argument is now `workspacePath` (4.1). runProjectTypecheck's root argument is deliberately still `projectPath` (the workflow root) — requirement 4.2 and the per-workspace cache key are task 11's, and unwrapTypecheck still reports tsconfigPath from projectPath for the same reason.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** workspaceFiles -> runProjectTypecheck(projectPath, workspaceFiles) | computeHygieneSignals(workspaceFiles) | computeTaskDiff(workspacePath, workspaceFiles)

#### Integration
- **Description:** RESPONSE SHAPE BREAKING CHANGE. `data.filesToReview` changed from `string[]` to `ResolvedFile[]` = `{ path: string; root: 'workspace' | 'workflow'; ambiguous: boolean }[]` (requirement 4.18). The runner's renderer emits `- <path> (<root>)`; interpolating the record renders `[object Object]` and nothing upstream reports it, so two committed tests assert the absence of that placeholder — one unit test on buildPrompt and one on the prompt actually handed to a spawned dashboard review agent. Both were confirmed to FAIL against the old `- ${f}` renderer before being kept. `src/__tests__/parity-baseline.test.ts` already anticipated this via its `toPaths()` normalizer (:161) and needed NO edit.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** handlePrepare data.filesToReview (ResolvedFile[]) -> any-typed destructure at task-review-runner.ts:161 -> BuildPromptOptions.filesToReview (typed) -> '## Files to Review' section

#### Integration
- **Description:** INTERIM OVERRIDE RULE at reviewTaskHandler (src/tools/review-task.ts:220). `const workspacePath = args.projectPath ? projectPath : context.workspacePath;` — an explicit projectPath ARGUMENT overrides BOTH roots, exactly as it does today. Pairing an override workflow root with the context's workspace would diff one project's files against another's tree. Task 14's selectRoots replaces this line: the override becomes the workspace and the workflow root is derived from it.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** args.projectPath || context.projectPath -> projectPath (workflow root); args.projectPath ? projectPath : context.workspacePath -> workspacePath

