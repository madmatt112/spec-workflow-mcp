# Implementation Log: Task 11

**Summary:** Split runProjectTypecheck's single root into a required (workspacePath, workflowRoot) pair and keyed the incremental cache file per workspace. Enumerated the six uses of the old projectPath parameter inside the function and classified each: tsconfigPath, resolveTscBinary, the -p argument and spawnTsc's cwd take the WORKSPACE; the cache-directory mkdir and ensureGitignoreEntry take the WORKFLOW ROOT. The tsbuildinfo filename gained a sha256-of-workspace discriminator (tsc-<key>.tsbuildinfo) while staying in the shared cache directory. unwrapTypecheck now reports tsconfigPath from the workspace so the rejection arm names the same file as the success and unavailable arms. All 25 test call sites updated in the same change (tsc reported exactly 25 arity errors, matching requirement 4.5's count).

**Timestamp:** 2026-07-31T16:03:36.646Z
**Log ID:** 0e606764-1613-4b73-91a5-94c5abd0218a

---

## Statistics

- **Lines Added:** +275
- **Lines Removed:** -33
- **Files Changed:** 4
- **Net Change:** 242

## Files Modified
- src/core/typecheck.ts
- src/tools/review-task.ts
- src/core/__tests__/typecheck.test.ts
- src/tools/__tests__/review-task.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### runProjectTypecheck
- **Purpose:** Gained a REQUIRED second root parameter. Signature is now (workspacePath, workflowRoot, allFiles, opts) — the roots are adjacent positional STRINGS, which is load-bearing: parity-baseline.test.ts's rootArgs() predicate collects absolute-path string arguments and asserts every one equals the expected root, so packing the roots into an object would make roots.length === 0 and silently pass. ENUMERATION (grep -n projectPath before the change): SIX uses inside the function, not the five the spec counted. WORKSPACE (4): tsconfigPath (:114), resolveTscBinary (:135), the -p argument (:147), spawnTsc's cwd (:158). WORKFLOW ROOT (2): the cache-directory mkdir (:140) and ensureGitignoreEntry (:142). The spec's five counted unwrapTypecheck's tsconfigPath in review-task.ts as the fifth; inside typecheck.ts itself the cache mkdir and the ignore write are two separate uses, not one. Helper parameters renamed to match (resolveTscBinary(workspacePath), ensureGitignoreEntry(workflowRoot)), so `projectPath` no longer appears anywhere in the module.
- **Location:** src/core/typecheck.ts:124
- **Signature:** runProjectTypecheck(workspacePath: string, workflowRoot: string, allFiles: string[], opts: { enabled: boolean }): Promise<TypecheckResult[]>
- **Exported:** No

#### workspaceCacheKey
- **Purpose:** Per-workspace discriminator for the incremental cache FILE (requirement 4.4). The cache DIRECTORY stays on the shared workflow root; the filename becomes tsc-<16 hex>.tsbuildinfo. Reuses the module's existing caseFold so two spellings of one workspace on a case-insensitive volume key the same. Keyed on path.resolve(workspacePath) rather than realpath: the key must be derivable without I/O and a spelling difference costs at most one extra cache file, whereas a shared file costs a full rebuild every run or a concurrent truncation that trips TSBUILDINFO_REBUILD_RE on an unrelated review.
- **Location:** src/core/typecheck.ts:117
- **Signature:** workspaceCacheKey(workspacePath: string): string
- **Exported:** No

#### unwrapTypecheck
- **Purpose:** Second parameter renamed projectPath -> workspacePath and the call site changed from projectPath to workspacePath (requirement 4.2). tsconfigPath is now identical across the success, unavailable and rejection arms; before, a rejection reported the workflow root's tsconfig while every other arm reported the workspace's.
- **Location:** src/tools/review-task.ts:78 (call site at :361)
- **Signature:** unwrapTypecheck(settled: PromiseSettledResult<TypecheckResult[]>, workspacePath: string): TypecheckResult[]
- **Exported:** No

### Integrations

#### Integration
- **Description:** handlePrepare now calls runProjectTypecheck(workspacePath, projectPath, workspaceFiles, { enabled }) — workspace first, shared workflow root second. This completes the routing task 10 deliberately left on one root. The two positional assertions in review-task.test.ts's two-root block moved with it (typecheckArgs[1] -> [2] for the file list, [2] -> [3] for opts); tsc cannot catch those because the mock spreads ...args: any[], so they are runtime-only and were verified by running the suite.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** workspaceFiles -> runProjectTypecheck(workspacePath, projectPath, files, opts) -> tsc -p <workspace> with cwd <workspace>, --tsBuildInfoFile <workflowRoot>/.spec-workflow/.cache/tsc-<key>.tsbuildinfo -> TypecheckResult[] -> unwrapTypecheck(settled[0], workspacePath)

#### Integration
- **Description:** TEST EVIDENCE — the compiled file list, not the reported path. The strong test spawns a REAL fake tsc (a /bin/sh script installed in the workspace's node_modules/.bin) that behaves like a compiler: it globs the .ts files of whatever directory follows -p and echoes them, and writes its pwd to a probe file. With one .ts file in each root and both roots given in allFiles, coverage.compiled === [workspace file] and coverage.excluded === [workflow-root file]. That distinguishes 'the right tree was compiled' from 'a string was reported' — asserting tsconfigPath alone passes under the exact defect requirement 4.2 describes. Both roots are given a tsconfig.json in the fixture so a mis-rooted implementation still proceeds and compiles the wrong tree rather than failing loudly with no-tsconfig. Mutation-verified: reverting -p to the workflow root fails the compiled-list assertion; reverting spawnTsc's cwd fails the pwd probe; reverting the cache dir/ignore write fails the workflow-root-only test; reverting resolveTscBinary fails the tsc-not-found test; reverting the filename to a constant fails the per-workspace key test.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** fake tsc: -p <root> -> glob <root>/*.ts -> stdout listFiles -> postProcess intersect allFiles -> coverage.compiled

#### Integration
- **Description:** REGRESSION NET. src/__tests__/parity-baseline.test.ts passes UNMODIFIED. Its rootArgs() predicate already tolerates a second root ('for every root it takes... expect(root).toBe(repo.path)'), and the cache key never appears as an argument, so the shape change is invisible to it while the equal-roots behaviour it pins is unchanged. Full suite 876 passing (868 before; +6 in typecheck.test.ts, +2 in review-task.test.ts), tsc clean.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** n/a

