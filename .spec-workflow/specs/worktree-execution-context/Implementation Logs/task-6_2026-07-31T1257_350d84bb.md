# Implementation Log: Task 6

**Summary:** Made ToolContext.workspacePath a required field and updated all 22 construction sites in one commit-sized change, using `npx tsc --noEmit` as the enumeration mechanism. Annotated the context literal in server.ts and changed setupHandlers(context: any) to context: ToolContext — the pair that makes server.ts visible to the compiler. Both adversarialReviewHandler call sites now receive the workflow root (project.projectPath) instead of project.originalProjectPath, per requirement 5.7; four dashboard test fixtures that had pinned the pre-fix behaviour by placing spec docs under the workspace were moved to the workflow root.

**Timestamp:** 2026-07-31T12:57:28.195Z
**Log ID:** 350d84bb-1888-46dc-977e-3925a4e240cb

---

## Statistics

- **Lines Added:** +76
- **Lines Removed:** -30
- **Files Changed:** 16
- **Net Change:** 46

## Files Modified
- src/types.ts
- src/server.ts
- src/dashboard/multi-server.ts
- src/dashboard/task-review-runner.ts
- src/__tests__/parity-baseline.test.ts
- src/prompts/__tests__/create-steering-doc.test.ts
- src/tools/__tests__/adversarial-response.test.ts
- src/tools/__tests__/adversarial-review.test.ts
- src/tools/__tests__/decomposition-guide.test.ts
- src/tools/__tests__/deferrals.test.ts
- src/tools/__tests__/get-task-review.test.ts
- src/tools/__tests__/projectPath.test.ts
- src/tools/__tests__/review-task.test.ts
- src/tools/__tests__/spec-index.test.ts
- src/dashboard/__tests__/adversarial-endpoints.test.ts
- src/dashboard/__tests__/multi-server.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### ToolContext (interface)
- **Purpose:** Two-root tool context. `workspacePath` is now REQUIRED (req 3.1): the worktree whose code is read/diffed/compiled, distinct from `projectPath` which is the shared workflow root holding `.spec-workflow`. Requiring it makes the compiler the enumeration mechanism for construction sites — an optional field would silently default to undefined everywhere.
- **Location:** src/types.ts:58-70
- **Signature:** interface ToolContext { projectPath: string; workspacePath: string; dashboardUrl?: string; lang?: string }
- **Exported:** Yes

#### SpecWorkflowMCPServer.setupHandlers
- **Purpose:** Parameter type changed from `any` to `ToolContext`. THE LAUNDERING TRAP: with `context: any` plus an unannotated object literal, tsc reports 20 errors and src/server.ts is NOT among them. Both the literal annotation (server.ts:204) and this parameter type are required for server.ts to surface as the 21st error. Verified empirically by removing the field with the annotation in place: `src/server.ts(204,13): error TS2741`.
- **Location:** src/server.ts:246
- **Signature:** private setupHandlers(context: ToolContext)
- **Exported:** No

### Integrations

#### Integration
- **Description:** REQUIREMENT 5.7 FIX — both adversarial dashboard routes (primary :828 and retry :969) previously passed `{ projectPath: project.originalProjectPath }`. `adversarialReviewHandler` immediately does `args.projectPath || context.projectPath` then `PathUtils.getWorkflowRoot(projectPath)` (adversarial-review.ts:51,60), so originalProjectPath — the UNTRANSLATED WORKSPACE path — sent `.spec-workflow` lookups into the worktree and, under Docker translation, at a path this process cannot read. Now `{ projectPath: project.projectPath, workspacePath: project.workspacePath }`. FIELD CHOICE: ProjectContext carries four path fields; `projectPath` is the TRANSLATED workflow root (project-manager.ts:13, `translatePath(entry.workflowRootPath)`) and is what in-process filesystem access needs — `workflowRootPath` is the untranslated host path kept for display/debugging only. This also makes the routes internally consistent: `loadSettings(project.projectPath)` on the same routes already used the translated workflow root.
- **Frontend Component:** dashboard adversarial-review + adversarial-retry routes
- **Backend Endpoint:** POST /api/projects/:projectId/approvals/:id/adversarial-review and /adversarial-retry
- **Data Flow:** route -> adversarialReviewHandler(args, { projectPath: project.projectPath, workspacePath: project.workspacePath }) -> getWorkflowRoot -> .spec-workflow/specs/<spec>/<phase>.md on the SHARED root

#### Integration
- **Description:** BUG-PINNING FIXTURES CORRECTED. Four dashboard tests placed the adversarial target document under `workspacePath` while their ApprovalStorage lived at `workflowRootPath`; adversarial-endpoints.test.ts:254 said so outright ('under workspacePath (originalProjectPath) where adversarialReviewHandler looks'). These fixtures use a two-root layout, so they were encoding the exact defect req 5.7 removes and went red on the fix. Target docs moved to workflowRootPath (adversarial-endpoints.test.ts: steering product.md, test-feat, test-feat2, retry-feat; multi-server.test.ts:331 setupApproval helper). They are now the regression test for 5.7 rather than a pin on its absence.
- **Frontend Component:** src/dashboard/__tests__/adversarial-endpoints.test.ts, src/dashboard/__tests__/multi-server.test.ts
- **Backend Endpoint:** adversarial-review / adversarial-retry routes
- **Data Flow:** fixture writes spec under workflowRootPath -> route passes workflow root -> handler locates target document (previously 500)

#### Integration
- **Description:** TaskReviewRunner ToolContext (task-review-runner.ts:100) sets BOTH fields to `opts.projectPath`. RunOptions still carries a single path and both callers (multi-server.ts:1798, :1860) pass `project.projectPath` = translated workflow root, so the roots are equal here — which req 3.11 pins to pre-change behaviour. Marked in-file: when req 5.1/5.3 splits RunOptions into `workflowRoot` + `workspacePath`, the second field becomes `opts.workspacePath`.
- **Frontend Component:** TaskReviewRunner.executeJob
- **Backend Endpoint:** reviewTaskHandler prepare
- **Data Flow:** RunOptions.projectPath -> ToolContext { projectPath, workspacePath } (equal today, split by req 5.1/5.3)

#### Integration
- **Description:** NO TYPE ASSERTIONS IN FIXTURES (req 3.4). A pre-existing `{} as ToolContext` at spec-index.test.ts:79 was found by grep, NOT by tsc — the assertion launders the site so the compiler cannot report it, making tsc's enumeration one short. Replaced with `{ projectPath: '', workspacePath: '' }`, which still exercises the falsy `!projectPath` guard (spec-index.ts:50) the test is about. Same empty-both-roots pattern used for the deliberate no-path fixtures in projectPath.test.ts (3 sites) and deferrals.test.ts:277. All other fixtures are single-checkout, so workspacePath === projectPath.
- **Frontend Component:** test fixtures across 10 files
- **Backend Endpoint:** n/a
- **Data Flow:** real string values, never `undefined` via assertion

