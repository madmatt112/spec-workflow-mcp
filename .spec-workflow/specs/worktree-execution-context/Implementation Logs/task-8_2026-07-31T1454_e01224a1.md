# Implementation Log: Task 8

**Summary:** Scrubbed the four GIT_* location variables from every code-path git invocation and set both SPEC_WORKFLOW_* roots explicitly at the two agent spawns. `grep -rn "process.env" src/core/task-diff.ts src/core/typecheck.ts src/dashboard/task-review-runner.ts src/dashboard/adversarial-runner.ts` found FOUR sites (not the spec's stated three — typecheck.ts:153 is the fourth); all four now route through a new exported `scrubbedGitEnv()` in git-utils.ts, and the re-grep returns zero hits. Task 3's `SCRUBBED_GIT_ENV_VARS` list is reused, not duplicated: `gitExecOptions` now delegates to `scrubbedGitEnv()`, so there is exactly one definition of the four names. Task 3's classification guard in git-utils.test.ts caught the new export and forced it into `nonGitExports`.

**Timestamp:** 2026-07-31T14:54:30.369Z
**Log ID:** e01224a1-49f0-4bda-ac0a-1b47ec479cff

---

## Statistics

- **Lines Added:** +304
- **Lines Removed:** -20
- **Files Changed:** 9
- **Net Change:** 284

## Files Modified
- src/core/git-utils.ts
- src/core/task-diff.ts
- src/core/typecheck.ts
- src/dashboard/task-review-runner.ts
- src/dashboard/adversarial-runner.ts
- src/core/__tests__/task-diff.test.ts
- src/core/__tests__/git-utils.test.ts
- src/dashboard/__tests__/task-review-runner.test.ts
- src/dashboard/__tests__/adversarial-runner.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### scrubbedGitEnv
- **Purpose:** THE shared scrub point for requirement 2.12 outside git-utils' own execSync calls. Returns a copy of the live process.env with GIT_DIR, GIT_COMMON_DIR, GIT_WORK_TREE and GIT_INDEX_FILE deleted; everything else — notably SPEC_WORKFLOW_HOST_PATH_PREFIX and SPEC_WORKFLOW_CONTAINER_PATH_PREFIX, which Docker path translation needs (req 2.13) — is preserved. Rebuilt per call, not snapshotted at module load, so a variable exported after import is still scrubbed. Task 3's private gitExecOptions() now delegates to it, so SCRUBBED_GIT_ENV_VARS has exactly one definition. DO NOT re-add a `...process.env` spread at any consumer: the four variables must not be re-inherited after the scrub.
- **Location:** src/core/git-utils.ts:47 (SCRUBBED_GIT_ENV_VARS now exported at :27)
- **Signature:** scrubbedGitEnv(): NodeJS.ProcessEnv
- **Exported:** No

#### runGit (modified)
- **Purpose:** env changed from `{ ...process.env, GIT_OPTIONAL_LOCKS: '0' }` to `{ ...scrubbedGitEnv(), GIT_OPTIONAL_LOCKS: '0' }`. THE SUBTLE SITE: it runs in the PARENT process, so neither runner's spawn-site scrub reaches it. Empirically verified by reverting the one line: with GIT_DIR exported to an unrelated real repository, `git diff --numstat -M HEAD -- <abs path>` returns EMPTY STDOUT AT EXIT 0, so runGit reports ok:true, computeTaskDiff takes the success path with no rejection, and the reviewing agent is told the changes were already committed. Three of the four new tests turned red on that revert; the CONTROL test stayed green, which is what proves GIT_DIR really reaches git in this environment.
- **Location:** src/core/task-diff.ts:21-45
- **Signature:** runGit(projectPath: string, args: string[]): Promise<GitRun> (module-private)
- **Exported:** No

#### runProjectTypecheck (modified)
- **Purpose:** THE FOURTH SITE the spec's hand-written count of three omits. `const env = { ...process.env, FORCE_COLOR: '0', NO_COLOR: '1' }` became `{ ...scrubbedGitEnv(), ... }`. tsc does not read the git location variables itself; this is the consistency case design.md:219 calls for, and it keeps the mechanical rule 'no `...process.env` survives in these four files' checkable by grep.
- **Location:** src/core/typecheck.ts:156 (spawnTsc consumer at :157)
- **Signature:** runProjectTypecheck(projectPath, allFiles, opts) — env built at src/core/typecheck.ts:156
- **Exported:** No

### Classes

#### TaskReviewRunner (modified)
- **Purpose:** runAgent's spawn env (req 2.12/2.13) is now `{ ...scrubbedGitEnv(), [SPEC_WORKFLOW_WORKSPACE_ENV]: workspacePath, [SPEC_WORKFLOW_SHARED_ROOT_ENV]: opts.workflowRoot }`. NOTE THE TWO SOURCES: the workspace comes from the runAgent PARAMETER (which is also the spawn cwd, so they cannot drift), the workflow root from `opts` — this runner is the two-field one task 7 split. The env-var name constants are imported from git-utils rather than written as string literals.
- **Location:** src/dashboard/task-review-runner.ts:387-400
- **Methods:** private runAgent(jobId, workspacePath, prompt, opts)
- **Exported:** No

#### AdversarialRunner (modified)
- **Purpose:** Same spawn env shape, but SPEC_WORKFLOW_SHARED_ROOT comes from the `workflowRoot` PARAMETER task 7 threaded through run() -> executeJob() -> runAgent() specifically for this — RunOptions stays single-rooted per req 5.5, so there is no opts field to read. The two 'task 8 will set this / not read yet' comments task 7 left at :66 and :150 are now updated to describe the live behaviour.
- **Location:** src/dashboard/adversarial-runner.ts:167-180
- **Methods:** private runAgent(jobId, workspacePath, workflowRoot, prompt, opts)
- **Exported:** No

### Integrations

#### Integration
- **Description:** REQUIRED TEST (req 2.12, design.md:331): src/core/__tests__/task-diff.test.ts gains 'computeTaskDiff — inherited GIT_* variables', four tests against a REAL unrelated repository (a second mkdtemp + git init + commit), not a mock — the failure mode is git SUCCEEDING against the wrong repository, which a mocked git cannot reproduce. ORDERING TRAP: both repositories must be built BEFORE the hostile variables are exported, because the file's own `gitCmd` helper inherits process.env, so an exported GIT_DIR corrupts the fixture setup rather than the code under test. Tests: (1) a CONTROL asserting an UNSCRUBBED `git diff --numstat -M HEAD` returns '' at exit 0 — without it the suite could pass merely because git ignored GIT_DIR here; (2) correct diff with GIT_DIR set; (3) correct diff with all four set; (4) all four absent from BOTH execFile envs while the two path-translation prefixes and PATH survive. Mutation-verified: reverting task-diff.ts:41 to `...process.env` turns 2, 3 and 4 red and leaves the CONTROL green.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** process.env (hostile GIT_DIR -> unrelated repo) -> scrubbedGitEnv() deletes 4 -> execFile opts.env -> real git in tempDir -> correct diff

#### Integration
- **Description:** Spawn-env guards in both runner test suites, added inside task 7's existing 'root separation' describes where the two roots are already GENUINELY DISTINCT (equal roots cannot detect a swap). Each test exports all four GIT_* variables AND stale SPEC_WORKFLOW_WORKSPACE=/stale/workspace + SPEC_WORKFLOW_SHARED_ROOT=/stale/root before running, then asserts on the captured spawn options env: the four are undefined, the two roots equal the JOB's roots (not the stale inherited ones), and SPEC_WORKFLOW_HOST_PATH_PREFIX / SPEC_WORKFLOW_CONTAINER_PATH_PREFIX / PATH survive. The stale values are what make 'set explicitly' distinguishable from 'inherited' — asserting only that the variable is present would pass on a wholesale inherit. Env is saved/restored in a finally block.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** hostile process.env -> scrubbedGitEnv() + explicit roots -> spawn options.env -> review agent child

#### Integration
- **Description:** Task 3's guard mechanism DOES extend here, and it fired. git-utils.test.ts:443 'classifies every exported function as git-invoking or not' failed the moment scrubbedGitEnv was exported, forcing an explicit classification into `nonGitExports` — where it is then asserted to run no command at all, so the list is not an escape hatch. This is the third time that guard has caught an unclassified export; it is the reason the shared helper went into git-utils.ts rather than being duplicated per module. No new guard was invented for task-diff/typecheck/the runners: their own suites assert on the observed child env, which is the stronger check.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** new export -> Object.entries(gitUtils) filter typeof function -> compared against gitInvokingFunctions + nonGitExports -> fails until classified

