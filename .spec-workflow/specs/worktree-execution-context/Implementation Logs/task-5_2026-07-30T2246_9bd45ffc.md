# Implementation Log: Task 5

**Summary:** Added resolveWorkspaceRoots to src/core/git-utils.ts as the single decision point for the workspace and workflow roots (env → --no-workspace-inference → inference → git toplevel fallback), wired it into parseArguments, registered --no-workspace-inference at all six parse sites with --flag=value rejection for all three boolean flags, and moved inferred-path validation into SpecWorkflowMCPServer.initialize via a new exported validateInferredWorkspace helper that falls back with a log instead of throwing.

**Timestamp:** 2026-07-30T22:46:27.686Z
**Log ID:** 9bd45ffc-78ad-4463-a5d6-bfb052313077

---

## Statistics

- **Lines Added:** +640
- **Lines Removed:** -40
- **Files Changed:** 6
- **Net Change:** 600

## Files Modified
- src/core/git-utils.ts
- src/index.ts
- src/server.ts
- src/__tests__/index-args.test.ts
- src/core/__tests__/git-utils.test.ts

## Files Created
- src/__tests__/workspace-roots.test.ts

---

## Artifacts

### Functions

#### resolveWorkspaceRoots
- **Purpose:** THE one decision point for workspace identity and the shared workflow root. Workspace precedence (req 2.3): SPEC_WORKFLOW_WORKSPACE (skipped when dashboardMode) -> noInference -> inference -> resolveGitWorkspaceRoot(configuredPath). Workflow-root precedence: resolve(SPEC_WORKFLOW_SHARED_ROOT) -> workspacePath when noSharedWorktreeSpecs -> resolveGitRoot(configuredPath). Never throws; every git call fails closed. Reports `source` so the async caller can validate an inferred path. Consume this instead of calling resolveGitWorkspaceRoot/resolveGitRoot pairwise.
- **Location:** src/core/git-utils.ts:344
- **Signature:** resolveWorkspaceRoots(options: WorkspaceRootsOptions): ResolvedRoots
- **Exported:** No

#### validateInferredWorkspace
- **Purpose:** Requirement 1.8's async half. Awaits validateProjectPath on an INFERRED workspace and returns fallbackPath with a console.error on rejection rather than throwing, because initialize runs before the MCP transport connects. Called from SpecWorkflowMCPServer.initialize only when the 4th param inferredWorkspaceFallback is supplied.
- **Location:** src/server.ts:22
- **Signature:** validateInferredWorkspace(workspacePath: string, fallbackPath: string): Promise<string>
- **Exported:** No

#### resolveEnvWorkspace
- **Purpose:** Validates SPEC_WORKFLOW_WORKSPACE with stat().isDirectory() (req 2.4), logs and returns null on failure so resolution falls through to inference (req 2.5), and warns without aborting when the value is in a different repository from the configured path (req 2.6).
- **Location:** src/core/git-utils.ts:245
- **Signature:** resolveEnvWorkspace(configuredPath: string): string | null (module-private, src/core/git-utils.ts:245)
- **Exported:** No

#### inferWorkspaceFromCwd
- **Purpose:** Requirement 1.1/1.3/1.7 gate. Returns gitTopLevel(cwd) only when BOTH gitTopLevel lookups succeed, the toplevels differ, and sameRepository(cwd, configuredPath). Equal toplevels return null so the single fallback owns requirement 1.2.
- **Location:** src/core/git-utils.ts:296
- **Signature:** inferWorkspaceFromCwd(configuredPath: string, cwd: string): string | null (module-private, src/core/git-utils.ts:296)
- **Exported:** No

#### showHelp
- **Purpose:** Now EXPORTED so the OPTIONS block can be asserted from tests; previously module-private. Lists --no-workspace-inference and a new ENVIRONMENT section for SPEC_WORKFLOW_WORKSPACE / SPEC_WORKFLOW_SHARED_ROOT.
- **Location:** src/index.ts:15
- **Signature:** showHelp(): void
- **Exported:** No

#### parseArguments
- **Purpose:** Return type gained noWorkspaceInference and workspaceSource. It no longer calls resolveGitWorkspaceRoot/resolveGitRoot itself — both roots come from resolveWorkspaceRoots. A module-level BOOLEAN_FLAGS list ['--no-open','--no-shared-worktree-specs','--no-workspace-inference'] makes the validation loop REJECT --flag=value forms with an error naming the bare form (req 1.17).
- **Location:** src/index.ts:124
- **Signature:** parseArguments(args: string[]): { workspacePath; workflowRootPath; expandedPath; isDashboardMode; noSharedWorktreeSpecs; noWorkspaceInference; workspaceSource; port?; lang?; noOpen? }
- **Exported:** No

### Integrations

#### Integration
- **Description:** main() computes inferredWorkspaceFallback = resolveGitWorkspaceRoot(cliArgs.expandedPath) only when workspaceSource === 'inference' and passes it as the 4th argument to SpecWorkflowMCPServer.initialize(workflowRootPath, workspacePath, lang, inferredWorkspaceFallback). initialize awaits validateInferredWorkspace before assigning this.workspacePath. Future tasks needing the configured-path fallback should reuse this parameter rather than adding another.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** src/index.ts:266-289 is now ONE mutually exclusive if/else chain with four arms — inference, env, worktree-detected, shared-specs-disabled, inference-disabled — replacing the two independent blocks previously at :218-225, so one event produces one log.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** src/core/__tests__/git-utils.test.ts keeps two lists (gitInvokingFunctions / nonGitExports) and asserts every exported FUNCTION appears in exactly one. Any new git-utils export must be added there or the whole file fails. resolveWorkspaceRoots was added to gitInvokingFunctions.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

#### Integration
- **Description:** The factory must declare every value export src/index.ts imports from git-utils.js — currently SPEC_WORKFLOW_WORKSPACE_ENV, resolveGitWorkspaceRoot, resolveWorkspaceRoots. An incomplete factory throws every test in the file (req 7.7). The old call-graph assertions on resolveGitRoot/resolveGitWorkspaceRoot were re-derived as assertions on the resolveWorkspaceRoots options object.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

