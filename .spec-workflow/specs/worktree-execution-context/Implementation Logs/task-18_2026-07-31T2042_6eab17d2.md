# Implementation Log: Task 18

**Summary:** Wrote the configuration documentation and migration notes for the two-root worktree execution model (requirement 4.6). docs/CONFIGURATION.md gains worktree setup, the --no-workspace-inference opt-out, SPEC_WORKFLOW_WORKSPACE, both precedence ladders, the absolute-SPEC_WORKFLOW_HOME requirement, the typecheck/tsc-not-found note and the GIT_* scrub note. CHANGELOG.md gains a 5.0.0 entry whose Migration section states all four consequences. README.md gains a Git Worktrees section and its stale tsc.tsbuildinfo recovery path is corrected. Every additional behaviour change was verified against the shipped code (and against `git show 2605819:src/index.ts` for the "previously" claims) before being written down. No exports map was added to package.json.

**Timestamp:** 2026-07-31T20:42:16.691Z
**Log ID:** 6eab17d2-215d-49a6-ba0e-68b70a0f9547

---

## Statistics

- **Lines Added:** +186
- **Lines Removed:** -5
- **Files Changed:** 3
- **Net Change:** 181

## Files Modified
- docs/CONFIGURATION.md
- CHANGELOG.md
- README.md

## Files Created
_No files created_

---

## Artifacts

### Integrations

#### Integration
- **Description:** docs/CONFIGURATION.md — the four documentation surfaces requirement 4.6 names. (1) OPTIONS TABLE gains `--no-workspace-inference`, plus an Important Note stating that all five boolean options (`--dashboard`, `--help`/`-h`, `--no-open`, `--no-shared-worktree-specs`, `--no-workspace-inference`) reject the `--flag=value` form while `--port` still accepts both. (2) NEW `### SPEC_WORKFLOW_WORKSPACE` section documenting the stat().isDirectory() validation, the fall-through-on-failure behaviour, the different-repository warning, the --dashboard-mode suppression, and that the dashboard sets it on spawned agents. (3) SPEC_WORKFLOW_HOME gains a blockquote stating that a relative value is unsupported in multi-worktree use — getGlobalDir() resolves it against process.cwd() and every worktree's server now runs from its own worktree, so each gets a private registry and the dashboard sees none. (4) SPEC_WORKFLOW_SHARED_ROOT gains a Notes list stating relative-value absolutization and that it OUTRANKS --no-shared-worktree-specs, with a link to the new precedence section.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** requirement 4.6 -> docs/CONFIGURATION.md sections: Available Options, Important Notes, SPEC_WORKFLOW_HOME, SPEC_WORKFLOW_WORKSPACE, SPEC_WORKFLOW_SHARED_ROOT

#### Integration
- **Description:** docs/CONFIGURATION.md `## Git Worktree Configuration` — REWRITTEN HEAD, EXISTING MODE SECTIONS KEPT. New material: a two-root table (workspace = code/diff/tsc/agent cwd; workflow root = .spec-workflow); a `### Setting Up a Worktree` walkthrough with the verbatim stderr the server emits ('Workspace inferred from the working directory. Pass --no-workspace-inference to disable.' + workspacePath= / workflowRootPath=), and the .mcp.json rationale for why the launch directory outranks the path argument; the three conditions inference requires (both --show-toplevel succeed, toplevels differ, same git common directory) stated as gates rather than as prose; `### Turning Inference Off` noting that the fallback is the path argument's GIT TOP-LEVEL, not the raw argument; `### Workspace and Workflow Root Precedence` with both first-match-wins ladders; `### Typechecking in a Worktree` covering tsc-not-found and the per-workspace tsc-<hash>.tsbuildinfo; `### Environment Inherited by git` covering the four scrubbed variables and the dotfiles-pattern consequence. The pre-existing Default/Isolated mode sections and their comparison table are UNCHANGED except for one added sentence recording the SPEC_WORKFLOW_SHARED_ROOT precedence. A stale example output line ('Git worktree detected. Using main repo: <path>') was corrected to the three lines the code actually prints.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** src/core/git-utils.ts resolveWorkspaceRoots precedence + src/index.ts logWorkspaceResolution arms -> documented ladders and example output

#### Integration
- **Description:** CHANGELOG.md `## [5.0.0] - 2026-07-31` — ALL FOUR MIGRATION CONSEQUENCES ARE IN THE `### Migration` SUBSECTION, each as its own bullet: (1) projectId moves wherever realpath differs from resolve, with the orphans stated as PERMANENT under Docker path translation because isProcessAlive returns true unconditionally when both prefix variables are set (verified at src/core/project-registry.ts:287-294), plus the manual-removal recovery; (2) FRESH WORKTREES REPORT tsc-not-found — the one that reads like a bug report and is easiest to drop — stated explicitly as a change in the PRIMARY use case, with `npm install` as the recovery (verified: resolveTscBinary at src/core/typecheck.ts:395 looks only at <workspacePath>/node_modules/.bin and does not walk parents); (3) GIT_* scrubbing changes the `git --git-dir=$HOME/.dotfiles --work-tree=$HOME` pattern, resolution seeing the directory's own repository instead; (4) the exported type surface is undocumented and unintended (no exports map, no types field, so deep imports of generated declarations resolve today) with NO exports map added and the bin-via-npx reasoning stated. Two further migration bullets: worktrees gain new identifiers with no history migration, and a relative SPEC_WORKFLOW_HOME is unsupported in multi-worktree use.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** design.md Migration section + implementation logs 1-17 -> CHANGELOG 5.0.0 ### Migration

#### Integration
- **Description:** CHANGELOG.md 5.0.0 — the four ADDITIONAL behaviour changes, each VERIFIED AGAINST SHIPPED CODE before being written, not taken on faith. (a) SPEC_WORKFLOW_SHARED_ROOT OUTRANKS --no-shared-worktree-specs: verified as a genuine precedence FLIP by diffing against `git show 2605819:src/index.ts:181`, which read `noSharedWorktreeSpecs ? workspacePath : resolveGitRoot(workspacePath)` — the flag arm never consulted the variable; the shipped resolveWorkspaceRoots checks the variable first. (b) `--flag=value` REJECTED FOR ALL FIVE BOOLEANS: verified against the CLI_FLAGS registry (src/index.ts:130-138) whose six entries — --dashboard, --help, -h, --no-open, --no-shared-worktree-specs, --no-workspace-inference — are all typed 'boolean', and against the old parseArguments, which passed `--dashboard=true` through validation (flagName was in validFlags), read it as false via args.includes, failed to filter it, and let it become filteredArgs[0] = the project path. The changelog states that exact old sequence. (c) BANNER: verified old `Starting Spec Workflow MCP Server for project: ${workflowRootPath}` (2605819:src/index.ts:359) against the shipped pathless banner plus the single settled emission from a `finally`. (d) INFERENCE ON BY DEFAULT: recorded as the headline in the section preamble and the first Added bullet. NONE of the four was found false.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** git show 2605819:src/index.ts (previous release) vs working tree src/index.ts + src/core/git-utils.ts -> verified claims -> CHANGELOG ### Changed

#### Integration
- **Description:** VERSION CHOICE: 5.0.0. package.json is at 4.3.0 and CHANGELOG's newest entry is `## [4.3.0] - 2026-07-24`, so 4.3.0 is released and this is the next entry. Major rather than minor because the fork's own precedent sets that bar: 4.0.0 was taken for a single field-type change in one status payload (SteeringStatus.documents), and this release changes project IDENTITY (registry entries orphan and dashboard URLs move), flips a documented config precedence, turns a previously-accepted CLI form into a hard error, and changes the `review-task action: prepare` response shape (data.filesToReview string[] -> {path,root,ambiguous}[]). package.json, package-lock.json and the three .claude-plugin manifests are NOT bumped here — task 18's file list is the three documentation files, and the repo's `npm version` hook (sync:plugin-version) owns those five files together; bumping package.json alone would fail the check:plugin-version gate.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** package.json version 4.3.0 + CHANGELOG [4.3.0] released -> next entry 5.0.0 -> version bump left to the release step

#### Integration
- **Description:** README.md — new `## 🌳 Git Worktrees` section placed before Sandboxed Environments: the two-root summary, the inference default with the .mcp.json rationale, a worked `git worktree add` example carrying the verbatim stderr, the `--no-workspace-inference` opt-out, the fresh-worktree `npm install` requirement for the typecheck, the absolute-SPEC_WORKFLOW_HOME requirement, and a link to docs/CONFIGURATION.md#git-worktree-configuration. Separately, a STALE PATH was corrected in the existing Limitations block: `rm <projectPath>/.spec-workflow/.cache/tsc.tsbuildinfo` no longer names any file — the cache directory is on the workflow root and the file is keyed per workspace — so it now reads `rm <workflowRoot>/.spec-workflow/.cache/tsc-*.tsbuildinfo` with a sentence explaining that separate worktrees do not contend for it.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** src/core/typecheck.ts:167-174 (cacheDir on workflowRoot, tsc-<workspaceCacheKey>.tsbuildinfo) -> corrected README recovery command

