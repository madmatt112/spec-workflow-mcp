# Implementation Log: Task 1

**Summary:** Built the unit-level git fixture (R7 AC 1) at src/core/__tests__/helpers/git-fixture.ts: a temp-directory host that builds real git repositories with linked worktrees in sibling and nested layouts, plus a submodule (with -c protocol.file.allow=always), a bare repo, a --separate-git-dir repo, a symlinked root, and non-git directories. Every layout self-verifies by reading `git rev-parse --git-common-dir` from inside it and throws on mismatch. Added a self-test at src/core/__tests__/git-fixture.test.ts — real-repository tests get their own file because git-utils.test.ts mocks child_process wholesale.

**Timestamp:** 2026-07-30T19:37:13.834Z
**Log ID:** 07463368-4e04-4074-8bb2-a75c05d53aa2

---

## Statistics

- **Lines Added:** +574
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 574

## Files Modified
_No files modified_

## Files Created
- src/core/__tests__/helpers/git-fixture.ts
- src/core/__tests__/git-fixture.test.ts

---

## Artifacts

### Functions

#### cleanupAllGitFixtures
- **Purpose:** Removes every temp root not yet cleaned up. Required as `afterAll(cleanupAllGitFixtures)` because vitest's worker pool terminates without emitting process 'exit', so the module's process-exit hook only covers plain node consumers. Empirically verified: without it an uncleaned fixture survives a vitest run; with it, it does not.
- **Location:** src/core/__tests__/helpers/git-fixture.ts
- **Signature:** cleanupAllGitFixtures(): Promise<void>
- **Exported:** No

#### buildFixtureGitEnv
- **Purpose:** Drops every GIT_* variable and points global+system git config at an empty file inside the fixture.
- **Location:** src/core/__tests__/helpers/git-fixture.ts
- **Signature:** buildFixtureGitEnv(gitConfigPath: string): NodeJS.ProcessEnv (module-private)
- **Exported:** No

### Classes

#### GitFixture
- **Purpose:** Temporary root hosting real git repositories for unit tests. Scrubs every GIT_* variable from its own git invocations and pins GIT_CONFIG_GLOBAL/GIT_CONFIG_SYSTEM to an empty file inside the fixture, so an ambient GIT_DIR or a developer's commit.gpgsign/core.hooksPath cannot break or contaminate construction. Every layout is verified via `git rev-parse --git-common-dir` read from inside it.
- **Location:** src/core/__tests__/helpers/git-fixture.ts
- **Methods:** static create(prefix = 'specwf-git-fixture-'): Promise<GitFixture> - creates the realpath-normalized temp root and registers it for teardown, git(args: string[], cwd: string): Promise<string> - runs git with the scrubbed env, returns trimmed stdout, throws with stderr on failure, readGitCommonDir(cwd: string): Promise<string | null> - raw `rev-parse --git-common-dir`, null when cwd is not a repository, verifyGitCommonDir(cwd, expected: { raw?: string; absolute?: string }, label): Promise<string> - asserts git shape, returns the absolute common dir, createRepo(name): Promise<GitRepoFixture> - repo with one commit; verified common dir is the literal '.git', createBareRepo(name): Promise<BareRepoFixture> - verified common dir '.' and no work tree, createSeparateGitDirRepo(name): Promise<SeparateGitDirFixture> - verified common dir is the external git dir, containing no .git segment, createNonGitDirectory(name): Promise<string> - plain dir, verified to resolve to no repository, createSymlink(targetPath, linkName): Promise<string> - symlinked root, verified via realpath, cleanup(): Promise<void> - idempotent removal of the temp root
- **Exported:** No

#### GitRepoFixture
- **Purpose:** A single real git repository inside a GitFixture.
- **Location:** src/core/__tests__/helpers/git-fixture.ts
- **Methods:** addWorktree(name, layout: 'sibling' | 'nested' = 'sibling'): Promise<GitWorktreeFixture> - sibling worktrees sit beside the main checkout, nested ones under <repo>/worktrees/<name> (the layout where the shared workflow root contains every sibling worktree). Verifies the common dir resolves to <repo>/.git and that .git is a file. Nested layout appends /worktrees/ to .git/info/exclude., addSubmodule(name, source?: GitRepoFixture): Promise<GitSubmoduleFixture> - passes `-c protocol.file.allow=always` (mandatory since git 2.38.1 / CVE-2022-39253); verifies common dir is <super>/.git/modules/<name> and that the submodule is registered, mkdirp(relativePath): Promise<string> - subdirectory at depth, writeFile(relativePath, contents): Promise<string>, commitAll(message): Promise<void> - no-ops when the tree is clean, git(args: string[]): Promise<string>
- **Exported:** No

### Integrations

#### Integration
- **Description:** Import { GitFixture, cleanupAllGitFixtures } from './helpers/git-fixture.js'. Assign the fixture to the tracked variable BEFORE building anything, call cleanup() in afterEach/afterAll, and add afterAll(cleanupAllGitFixtures) as a backstop. Verified git shapes available to consumers: repo root -> '.git'; subdirectory at depth -> '../../../.git'; sibling and nested worktree -> absolute <main>/.git; submodule -> <super>/.git/modules/<name>; bare -> '.'; --separate-git-dir -> the external dir (no .git segment); symlinked root -> '.git'; non-git -> null.
- **Frontend Component:** undefined
- **Backend Endpoint:** undefined
- **Data Flow:** undefined

