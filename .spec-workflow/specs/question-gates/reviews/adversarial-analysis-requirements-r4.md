R3-1: addressed — Req 2 AC1/AC6, D2, D11 now require the drafter's frontmatter to gain an MCP write tool (cited to `sdd-drafter.md:7-13`, matched against the reviser's real grant at `sdd-reviser.md:7-16`), closing the capability gap; the "no relay" conclusion in D2 now follows from that granted-capability premise rather than the prior false document-Write/Edit conflation.
R3-2: addressed — new Req 2 AC7 adds a durable pre-ask receipt (decisions, no `answer`) to `questions.md`, best-effort/non-stalling, and defines fresh-supervisor recovery via step 3 (`sdd-continue/SKILL.md:122`, accurately the requirements-routing rule) to re-ask or fall to `record` instead of silently skipping the gate.
R3-3: addressed — Req 2 AC4 now defines the second-call denial/error/timeout outcome explicitly: keep the first call's real answers, treat only the unreturned decisions as `no answer`, and proceed, not discarding call 1.

VERIFIED: 3/3

## Deferred findings
- The v4 Revision History claims R3-1 "deleted" the drafter's "directly to the gate-A server surface" claim and D2's "needs no relay" clause, but both phrases remain verbatim in the current document text (Req 2 AC1 line 32, D11 line 104, D2 line 95); the fix instead added a preceding capability-grant clause that grounds the retained language, so the disposition's "deleted" wording overstates what changed.
