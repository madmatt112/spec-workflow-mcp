R2-1: addressed — Req 4 crit 6 now guards the collapse on the cell's Anthropic count being above 0, and states an all-DeepSeek cell (count 0) prints `-` per criterion 4.
R2-2: addressed — Req 4 crit 1/2 split the counter into `cacheUnknownWrite` and `cacheUnknownGap`, and crit 6 collapses `cw5m`/`cw1h` and `gapRewrites` on their own counts, so a gap-only-unknown cell keeps its write sums as Req 3 crit 7 requires.
R2-3: addressed — Req 6 crit 7 now names `.spec-workflow/specs/agent-cache-ttl/verification-evidence.md` as the tracked file the restarted session writes and the retrospective reads; the mandatory block wording is unchanged.

VERIFIED: 3/3

## Deferred findings
