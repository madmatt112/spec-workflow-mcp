# Implementation Log: Task 6

**Summary:** Added the cache-lifetime override probe sdd-cache-ttl.sh (design C6). One no-argument shipped script prints exactly one `cacheTtl=VALUE` stdout line and exits 0 in every case, deciding VALUE in order (first match wins): unknown (claude --version fails under a 10s timeout or prints no three-part version), unsupported (below 2.1.248 compared numerically), FORCE_PROMPT_CACHING_5M=VALUE (non-empty, not 0), CLAUDE_CODE_SUBAGENT_PROMPT_CACHE_TTL=VALUE (non-empty), subagentPromptCacheTtl=VALUE from the first settings file that sets it (.claude/settings.local.json, .claude/settings.json in the working directory, then CLAUDE_CONFIG_DIR/settings.json or ~/.claude/settings.json via os.homedir), else per-agent. Body is a node -e in a single-quoted string with no apostrophes, following the sdd-providers.sh shape; the shell reads optional env vars as ${VAR:-} for set -u safety. Added the vitest cache-ttl-probe.test.ts (15 cases) driving the real script with a stub claude, temp code root and separate HOME.

**Timestamp:** 2026-09-24T19:35:33.170Z
**Log ID:** 39f8d358-1b9e-4ba7-abcd-dd784ef4b368

---

## Statistics

- **Lines Added:** +274
- **Lines Removed:** -0
- **Files Changed:** 5
- **Net Change:** 274

## Files Modified
_No files modified_

## Files Created
- harness/skills/sdd-continue/references/sdd-cache-ttl.sh
- plugins/spec-workflow-harness/skills/sdd-continue/references/sdd-cache-ttl.sh
- plugins/spec-workflow-mcp-with-dashboard/skills/sdd-continue/references/sdd-cache-ttl.sh
- plugins/spec-workflow-mcp/skills/sdd-continue/references/sdd-cache-ttl.sh
- src/__tests__/cache-ttl-probe.test.ts

---

## Artifacts

### Functions

#### sdd-cache-ttl.sh (probe)
- **Purpose:** Report whether the orchestrators' frontmatter cacheTtl can apply. Prints one `cacheTtl=VALUE` line and exits 0 always. Called by the sdd-continue supervisor at Step 0 (C7, not yet wired). VALUE order: unknown, unsupported, FORCE_PROMPT_CACHING_5M=VALUE, CLAUDE_CODE_SUBAGENT_PROMPT_CACHE_TTL=VALUE, subagentPromptCacheTtl=VALUE, per-agent.
- **Location:** harness/skills/sdd-continue/references/sdd-cache-ttl.sh
- **Signature:** bash sdd-cache-ttl.sh (no args; cwd = code root; reads FORCE_PROMPT_CACHING_5M, CLAUDE_CODE_SUBAGENT_PROMPT_CACHE_TTL, CLAUDE_CONFIG_DIR, HOME and .claude settings files)
- **Exported:** No

### Integrations

#### Integration
- **Description:** Supervisor Step 0 will run this probe and carry cacheTtl onto the run.start ledger row (Task 7, C7). This task ships the probe and its test only; the SKILL.md wiring is a separate task.
- **Frontend Component:** harness/skills/sdd-continue/SKILL.md Step 0 (pending, C7)
- **Backend Endpoint:** harness/skills/sdd-continue/references/sdd-cache-ttl.sh
- **Data Flow:** probe stdout cacheTtl=VALUE -> CACHE_TTL -> run.start row cacheTtl field

