# Implementation Log: Task 5

**Summary:** Implemented design C5: the usage fold carries the cache numbers through every cell and the tables print three columns. UsageCell gained cacheWrite5m, cacheWrite1h, gapRewrites, cacheUnknownWrite and cacheUnknownGap; emptyCell and addCell cover all eight fields. ReducedSpawn gained a cache object; reduceSpawn sets it from the same spawn.end row that sets tokens via cacheOf (write-unknown when a write field is not a digit, gap-unknown when only gapRewrites is not a digit, unknown-write when no digit spawn.end). Aggregation adds the five fields only for anthropic spawns to the agent cell, ph.total, ph.providers.anthropic and the report total. New cacheCols/cacheStr helper renders three dashes at an Anthropic count of 0, else unknown or grouped sums with (+N unknown) on the gap column. formatOne heads and prints cw5m | cw1h | gapRewrites after each token cell; formatCompare repeats the columns per side, pair takes the Anthropic count and returns five dashes for an absent cell. No change to kinds, orchestratorShare, usageDelta or src/tools/harness.ts.

**Timestamp:** 2026-09-24T19:30:19.197Z
**Log ID:** 4ad461ca-8692-43ad-9825-81e0923b529e

---

## Statistics

- **Lines Added:** +205
- **Lines Removed:** -24
- **Files Changed:** 3
- **Net Change:** 181

## Files Modified
- src/watch/usage.ts
- src/watch/__tests__/usage.test.ts
- src/tools/__tests__/harness.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### cacheOf
- **Purpose:** Design C5/Req 4.2: returns a SpawnCache {w5m,w1h,gap,unknownWrite,unknownGap} from the spawn.end row that set tokens. When cacheWrite5m or cacheWrite1h is not a digit string, unknownWrite=1 and sums 0; when only gapRewrites is not a digit, keeps the write sums, gap=0 and unknownGap=1; else the three sums.
- **Location:** src/watch/usage.ts
- **Signature:** cacheOf(r: LedgerEvent): SpawnCache
- **Exported:** No

#### unknownWriteCache
- **Purpose:** The write-unknown default SpawnCache (Req 4.3) for a spawn with no digit spawn.end row (a spawn.usage-only spawn).
- **Location:** src/watch/usage.ts
- **Signature:** unknownWriteCache(): SpawnCache
- **Exported:** No

#### cacheCols
- **Purpose:** Design C5: the three cache columns for a UsageCell gated on its Anthropic spawn count. At 0 returns ['-','-','-']; else each write column is 'unknown' when cacheUnknownWrite equals the count else grp(sum); the gap column is 'unknown' when cacheUnknownWrite+cacheUnknownGap equals the count else grp(gapRewrites) ending ' (+N unknown)' when N>0.
- **Location:** src/watch/usage.ts
- **Signature:** cacheCols(c: UsageCell, anthropicSpawns: number): [string, string, string]
- **Exported:** No

#### cacheStr
- **Purpose:** Joins cacheCols with ' | ' for a table line.
- **Location:** src/watch/usage.ts
- **Signature:** cacheStr(c: UsageCell, anthropicSpawns: number): string
- **Exported:** No

### Integrations

#### Integration
- **Description:** The hook's spawn.end cache-lifetime fields (task 4) fold through the watch usage layer into the harness usage tables; the tool result carries the new UsageCell fields with no change to src/tools/harness.ts usageAction.
- **Frontend Component:** src/watch/usage.ts formatOne/formatCompare (harness usage tables)
- **Backend Endpoint:** src/tools/harness.ts usageAction (returns data.report/data.compare unchanged)
- **Data Flow:** spawn.end cacheWrite5m/cacheWrite1h/gapRewrites -> reduceSpawn.cacheOf -> UsageCell.cache* aggregation (anthropic only) -> cacheCols -> table columns cw5m|cw1h|gapRewrites

