# Implementation Log: Task 4

**Summary:** Implemented design C4: the SubagentStop hook now writes three cache-lifetime fields on every spawn.end row. The dedupe map in readUsage keeps a per-key smallest finite Date.parse(timestamp) (t) beside the usage and model. A new cacheFields(calls) over byId.values() returns cacheWrite5m, cacheWrite1h and gapRewrites as strings: all three "unknown" when any counted call lacks a numeric cache_creation.ephemeral_5m/1h; else the two decimal sums, and gapRewrites is "unknown" when any call has no time, else the count of calls (stably sorted by t) more than 300000 ms after a predecessor whose cache_creation_input_tokens exceeds half the predecessor's input+write+read prefix. readUsage returns the three strings; the usage branch adds them to the row, the no-usage branch adds all three as "unknown". Documented the keys on the spawn.end row in formats.md, regenerated the plugins/ mirrors, and added seven hook tests (5m/1h split, multi-block-counted-once, gap>300s counted, gap>300s small write not counted, gap<300s large write not counted, one call without cache_creation, no transcript). No apostrophe in the node body.

**Timestamp:** 2026-09-24T19:18:18.574Z
**Log ID:** b1b3800b-31a4-4ba1-9031-335836e8ee5c

---

## Statistics

- **Lines Added:** +289
- **Lines Removed:** -24
- **Files Changed:** 9
- **Net Change:** 265

## Files Modified
- harness/hooks/sdd-activity.sh
- harness/skills/sdd-continue/references/formats.md
- src/__tests__/hook-spawn-events.test.ts
- plugins/spec-workflow-harness/hooks/sdd-activity.sh
- plugins/spec-workflow-harness/skills/sdd-continue/references/formats.md
- plugins/spec-workflow-mcp/hooks/sdd-activity.sh
- plugins/spec-workflow-mcp/skills/sdd-continue/references/formats.md
- plugins/spec-workflow-mcp-with-dashboard/hooks/sdd-activity.sh
- plugins/spec-workflow-mcp-with-dashboard/skills/sdd-continue/references/formats.md

## Files Created
_No files created_

---

## Artifacts

### Functions

#### cacheFields
- **Purpose:** Design C4: returns { cacheWrite5m, cacheWrite1h, gapRewrites } as strings for a spawn.end row. All three 'unknown' when any call's cache_creation is not an object or its ephemeral_5m/1h are not finite numbers; else the two decimal sums, plus gapRewrites 'unknown' when any call lacks a finite timestamp, else the count of stably-time-sorted calls more than 300000 ms after a predecessor whose cache_creation_input_tokens exceeds half the predecessor's input+cacheWrite+cacheRead prefix
- **Location:** harness/hooks/sdd-activity.sh (node body, after num())
- **Signature:** cacheFields(calls: Array<{u, model, t}>): { cacheWrite5m: string; cacheWrite1h: string; gapRewrites: string }
- **Exported:** No

#### readUsage
- **Purpose:** Existing transcript reducer; the dedupe map now also keeps t (smallest finite Date.parse(e.timestamp) over a key's lines, else null), and the return object gains cacheWrite5m, cacheWrite1h and gapRewrites from cacheFields(Array.from(byId.values()))
- **Location:** harness/hooks/sdd-activity.sh (node body)
- **Signature:** readUsage(p): { input, output, cacheWrite, cacheRead, tokens, model, cacheWrite5m, cacheWrite1h, gapRewrites } | null
- **Exported:** No

### Integrations

#### Integration
- **Description:** The hook's spawn.end row carries the three cache-lifetime fields into harness-events.jsonl; the watch usage layer (C5, later task) folds them into the tables. Documented on the formats.md spawn.end row.
- **Frontend Component:** harness/skills/sdd-continue/references/formats.md spawn.end row
- **Backend Endpoint:** harness/hooks/sdd-activity.sh SubagentStop branch (cacheFields + row writers)
- **Data Flow:** subagent transcript timestamp + message.usage.cache_creation -> readUsage dedupe map (u, model, t) -> cacheFields -> spawn.end row keys cacheWrite5m/cacheWrite1h/gapRewrites -> harness-events.jsonl

