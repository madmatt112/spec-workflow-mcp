# Implementation Log: Task 3

**Summary:** Component 2: computeTaskDiff now takes a required third `base` parameter replacing both HEAD literals at the diff/numstat arg arrays. A failed git run on the diff (the !ok arm) classifies as a rejection carrying gitFailureMessage(cause, base, workspacePath) instead of a benign empty diff (req 1.10); the message is stated text like containmentRejectionMessage. runGit gained a 10s timeout and a cause helper (gitRunCause) that reads node-20 execFile error.code: a string system code as-is, a numeric exit code as `exit <n>`, else error.message. Exported readHeadCommit (rev-parse --verify HEAD^{commit}, null unless 40 hex), isAncestorOfHead (merge-base --is-ancestor, true only on exit 0) and gitFailureMessage. Production caller review-task.ts:473 passes literal 'HEAD' as a bridge (task 8 replaces it). Env-fact correction: git diff HEAD exits 128 on an unborn-HEAD repo but exit 1 (not 128) outside a repository in this environment, contrary to the design prose; tests assert the real observed cause. The empty-repo (unborn HEAD) test now expects a rejection naming exit 128 per req 1.10, with diff/stats/truncation still byte-identical to before (req 1.9).

**Timestamp:** 2026-09-18T22:27:18.761Z
**Log ID:** 3e4b6fb3-5653-43b7-9c84-c1dc975cf3c2

---

## Statistics

- **Lines Added:** +237
- **Lines Removed:** -39
- **Files Changed:** 3
- **Net Change:** 198

## Files Modified
- src/core/task-diff.ts
- src/tools/review-task.ts
- src/core/__tests__/task-diff.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### computeTaskDiff
- **Purpose:** Base-aware task diff: takes a required third `base` string that replaces both HEAD literals in the diff/numstat arg arrays; a failed git run returns { diff:'', stats:undefined, rejection:{message: gitFailureMessage(...)} } naming the first failing run's cause (req 1.10).
- **Location:** src/core/task-diff.ts:computeTaskDiff
- **Signature:** computeTaskDiff(workspacePath: string, allFiles: string[], base: string): Promise<TaskDiffResult>
- **Exported:** No

#### readHeadCommit
- **Purpose:** Reads the workspace HEAD as a 40-hex sha via rev-parse --verify HEAD^{commit}; returns null for an unborn HEAD or a non-repository. Used by the status route (Component 6) and log-implementation (Component 3/R3).
- **Location:** src/core/task-diff.ts
- **Signature:** readHeadCommit(workspacePath: string): Promise<string | null>
- **Exported:** No

#### isAncestorOfHead
- **Purpose:** True only when commit is an ancestor of HEAD via merge-base --is-ancestor (exit 0). rev-parse --verify is insufficient because linked worktrees share one object database. Used by handlePrepare (Component 4) to validate a recorded base (req 1.5).
- **Location:** src/core/task-diff.ts
- **Signature:** isAncestorOfHead(workspacePath: string, commit: string): Promise<boolean>
- **Exported:** No

#### gitFailureMessage
- **Purpose:** Reviewer-facing stated text for a git diff failure: 'GIT DIFF FAILED. git diff from <base> in <workspacePath> did not complete: <cause>...'. Contradicts the benign-empty-diff and already-committed fabrications. Same pattern as containmentRejectionMessage.
- **Location:** src/core/task-diff.ts
- **Signature:** gitFailureMessage(cause: string, base: string, workspacePath: string): string
- **Exported:** No

#### gitRunCause
- **Purpose:** Maps a node-20 execFile callback error to a cause string: string error.code as-is, numeric error.code as `exit <n>`, else error.message. Fills GitRun.cause on the !ok path.
- **Location:** src/core/task-diff.ts
- **Signature:** gitRunCause(err: Error & { code?: unknown }): string
- **Exported:** No

### Integrations

#### Integration
- **Description:** review-task handlePrepare invokes computeTaskDiff with the required base; for this task it is the literal 'HEAD' (bridge). Task 8 swaps in the resolved diffBase.commit from the TaskStateStore record.
- **Frontend Component:** handlePrepare (src/tools/review-task.ts:473)
- **Backend Endpoint:** computeTaskDiff (src/core/task-diff.ts)
- **Data Flow:** workspaceFiles + base 'HEAD' -> computeTaskDiff -> TaskDiffResult{diff,stats,skippedPaths,truncated,rejection?} -> computeDiffMethodologyState

