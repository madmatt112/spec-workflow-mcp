# Implementation Log: Task 7

**Summary:** Component 6: the dashboard status route now records the task's diff base. After the tasks.md write and broadcast, and only on a transition to 'in-progress', it reads the workspace HEAD via readHeadCommit(project.workspacePath) (task 3 export) and, when non-null, calls new TaskStateStore(PathUtils.getSpecPath(project.projectPath, name)).recordBase(taskId, project.workspacePath, head) (task 2 export). The await sits in try/catch with console.warn so a git or lock failure never blocks or alters the response, which stays byte-identical on every arm. The same-status early return records nothing; 'completed' and 'pending' record nothing (the status !== 'in-progress' guard). A later in-progress transition overwrites the workspace's entry because recordBase keys by normalizeIdentityPath(workspacePath) (design D6). Added three status-route cases to multi-server.test.ts using the Track-C harness (distinct plain-directory workspace vs workflow-root): in-progress after git init+commit writes task-state.json under the workflow root's spec dir with the HEAD sha; completed writes nothing; an un-initialised workspace returns 200 and writes nothing.

**Timestamp:** 2026-09-18T23:03:11.712Z
**Log ID:** 53c3d715-8a3f-42d7-b428-df3e3acfc358

---

## Statistics

- **Lines Added:** +97
- **Lines Removed:** -0
- **Files Changed:** 2
- **Net Change:** 97

## Files Modified
- src/dashboard/multi-server.ts
- src/dashboard/__tests__/multi-server.test.ts

## Files Created
_No files created_

---

## Artifacts

### API Endpoints

#### PUT /api/projects/:projectId/specs/:name/tasks/:taskId/status
- **Purpose:** Existing task-status route; extended so a transition to in-progress records the workspace HEAD as the task's diff base (Component 6). Behaviour and response literal are unchanged on every arm; base recording is a non-blocking side effect after the tasks.md write and broadcast.
- **Location:** src/dashboard/multi-server.ts:1417-1490
- **Request Format:** JSON body { status: 'pending' | 'in-progress' | 'completed' }
- **Response Format:** 200 { success: true, message: string, task: {...task, status} } (unchanged). On status==='in-progress' with a resolvable HEAD, writes <workflowRoot>/.spec-workflow/specs/<name>/task-state.json via TaskStateStore.recordBase; git/lock failures are caught and console.warn'd, response unaffected.

### Integrations

#### Integration
- **Description:** The status route is the one site that knows the pre-work workspace, so it captures the task's starting commit there. It reads HEAD in the translated workspace and persists it to the per-spec TaskStateStore; handlePrepare (Component 4) later reads that base to scope the review diff.
- **Frontend Component:** PUT status route in src/dashboard/multi-server.ts
- **Backend Endpoint:** readHeadCommit (src/core/task-diff.ts) + TaskStateStore.recordBase (src/core/task-state-store.ts)
- **Data Flow:** status==='in-progress' -> readHeadCommit(project.workspacePath) -> head|null; if head: new TaskStateStore(PathUtils.getSpecPath(project.projectPath, name)).recordBase(taskId, project.workspacePath, head) -> task-state.json bases[normalizeIdentityPath(workspacePath)] = { commit: head, recordedAt }

