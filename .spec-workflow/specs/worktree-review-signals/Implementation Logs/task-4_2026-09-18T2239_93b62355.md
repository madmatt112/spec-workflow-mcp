# Implementation Log: Task 4

**Summary:** Made the typecheck degrade honestly (design Component 3). Added a required `observed: string` to the `unavailable` arm of `TypecheckResult` and a new reason `dependencies-unresolved`. Added `probeDeclaredDependencies`, which runs after `resolveTscBinary` and before `spawnTsc`: it reads `<workspace>/package.json` (null when absent/unparseable, so the check proceeds), collects the keys of `dependencies` and `devDependencies` (optionalDependencies excluded), and does one `fs.access` on `<workspace>/node_modules/<name>/package.json` per name, awaited together; any unresolved name returns `dependencies-unresolved` with no `tsc` spawn. Every `unavailable` construction site now states `observed` per the design table, including `unwrapTypecheck`'s rejection arm. The `no-tsconfig` arm probes `<workflowRoot>/tsconfig.json` so `observed` states whether the workflow root has one. Dependency state is decided by direct existence check, never inferred from diagnostic shape (req 2.7). R4_6B constant and the seventeen fixtures untouched.

**Timestamp:** 2026-09-18T22:39:44.437Z
**Log ID:** 93b62355-7b57-4ea6-8c3e-564523996c9f

---

## Statistics

- **Lines Added:** +190
- **Lines Removed:** -18
- **Files Changed:** 5
- **Net Change:** 172

## Files Modified
- src/core/typecheck.ts
- src/tools/review-task.ts
- src/core/__tests__/typecheck.test.ts
- src/tools/__tests__/review-task.test.ts
- src/tools/__tests__/review-gate.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### probeDeclaredDependencies
- **Purpose:** Existence-probe every package named in the workspace's dependencies and devDependencies (optionalDependencies excluded) via one fs.access on <workspace>/node_modules/<name>/package.json per name, awaited together. Returns null when package.json is absent or unparseable so the typecheck proceeds to spawn.
- **Location:** src/core/typecheck.ts (module-private, defined after resolveTscBinary)
- **Signature:** async function probeDeclaredDependencies(workspacePath: string): Promise<{ declared: number; unresolved: string[] } | null>
- **Exported:** No

#### describeUnresolvedDependencies
- **Purpose:** Builds the dependencies-unresolved `observed` text: unresolvable count, declared count, package.json path, up to five backticked names with a '+N more' tail, and 'tsc was not run'.
- **Location:** src/core/typecheck.ts (module-private)
- **Signature:** function describeUnresolvedDependencies(workspacePath: string, probe: { declared: number; unresolved: string[] }): string
- **Exported:** No

### Integrations

#### Integration
- **Description:** The unavailable arm of TypecheckResult gained required field `observed: string` and reason value `dependencies-unresolved`. computeTypecheckMethodologyState maps the new reason to `unavailable-other` unchanged; gate-rules.ts prints the reason string unchanged; R4_6B_TYPECHECK_UNAVAILABLE is not edited. No runtime code reads `.observed` yet — that is Component 4 (handlePrepare executionContext), a later task.
- **Frontend Component:** n/a
- **Backend Endpoint:** src/core/typecheck.ts runProjectTypecheck / src/tools/review-task.ts unwrapTypecheck
- **Data Flow:** runProjectTypecheck and unwrapTypecheck construct TypecheckResult[] with observed; consumed later by handlePrepare's executionContext.typecheck.observed (future task)

