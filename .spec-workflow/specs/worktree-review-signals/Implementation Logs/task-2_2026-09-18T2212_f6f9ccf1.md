# Implementation Log: Task 2

**Summary:** Component 1: new TaskStateStore module beside registry-lock.ts. One task-state.json per spec under the shared spec dir, holding per-task bases keyed by normalizeIdentityPath(workspacePath) and a shared attribution; two owners (status route recordBase, log-implementation recordAttribution), one sibling .lock via withRegistryLock. read() never throws and takes no lock: missing/unreadable/malformed/version!==1 returns null and warns once per file; a well-formed file with no entry for the task returns null silently. Writers load-or-init the file under the lock, mutate one field to preserve the other, write uniqueTempPath then fs.rename; an unacquired lock returns false with console.warn; a write failure rejects (EH #10 via withRegistryLock's finally). Added .spec-workflow/specs/*/task-state.json* to .gitignore beside :150, covering record, lock, temp and stale variants. Constructor takes an optional RegistryLockOptions (default {}) so the lock-held test injects timeoutMs 50; new TaskStateStore(specPath) still works for Component 4/6/7.

**Timestamp:** 2026-09-18T22:12:44.326Z
**Log ID:** f6f9ccf1-3ddc-445e-9f8f-c95b8877079d

---

## Statistics

- **Lines Added:** +322
- **Lines Removed:** -0
- **Files Changed:** 3
- **Net Change:** 322

## Files Modified
- .gitignore

## Files Created
- src/core/task-state-store.ts
- src/core/__tests__/task-state-store.test.ts

---

## Artifacts

### Functions

#### parseTaskStateFile
- **Purpose:** Parse raw text into a TaskStateFile or null when not JSON, not an object, or version !== 1. Shared by read() and the writers' load-or-init.
- **Location:** src/core/task-state-store.ts
- **Signature:** parseTaskStateFile(raw: string): TaskStateFile | null
- **Exported:** No

### Classes

#### TaskStateStore
- **Purpose:** Per-spec task state: one task-state.json record per task holding diff bases keyed by workspace and shared attribution; two owners, one lock. read() never throws and takes no lock; recordBase/recordAttribution serialise on a sibling .lock via withRegistryLock and preserve the other field.
- **Location:** src/core/task-state-store.ts
- **Methods:** read(taskId: string): Promise<TaskStateRecord | null>, recordBase(taskId: string, workspacePath: string, commit: string): Promise<boolean>, recordAttribution(taskId: string, attribution: TaskAttribution): Promise<boolean>
- **Exported:** No

### Integrations

#### Integration
- **Description:** TaskStateStore is the store Component 4 (handlePrepare) reads via new TaskStateStore(specPath).read(taskId), and Components 6/7 (status route, log-implementation) write via recordBase/recordAttribution. Exports TASK_STATE_FILE and the interfaces TaskStateFile, TaskStateRecord, TaskAttribution for those callers.
- **Frontend Component:** handlePrepare (src/tools/review-task.ts) and log-implementation/status route
- **Backend Endpoint:** src/core/task-state-store.ts (withRegistryLock on <specPath>/task-state.json.lock)
- **Data Flow:** recordBase/recordAttribution -> withRegistryLock(lockPath) -> loadForWrite (read+parse or fresh) -> mutate one field -> write uniqueTempPath + fs.rename; read(taskId) -> readFile -> parseTaskStateFile -> tasks[taskId] ?? null

