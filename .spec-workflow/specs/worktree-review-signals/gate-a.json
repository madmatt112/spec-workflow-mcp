{
  "items": [
    {
      "header": "D1 — Diff base recording site",
      "question": "Record the per-task diff base at the dashboard status route only",
      "options": [
        "the dashboard status route only",
        "an explicit `baseRef` argument on `prepare`",
        "a computed merge-base with the default branch"
      ]
    },
    {
      "header": "D3 — Where base and attribution live",
      "question": "One per-task record under the shared `.spec-workflow/specs/<spec>/` holds base and attribution with one owner per field",
      "options": [
        "one per-task record on the shared root, one owner per field",
        "attribution in the implementation-log markdown entry",
        "two separate stores"
      ]
    },
    {
      "header": "D6 — All-drop honesty without touching pinned text",
      "question": "Make the all-drop review honest via a new diff-state kind with new preamble and header constants",
      "options": [
        "a new diff-state kind with new preamble and header constants",
        "editing `R4_2A_DIFF_EMPTY` and the header with a fixture regeneration",
        "leaving the residual"
      ]
    },
    {
      "header": "D5 — New typecheck reasons bypass the pinned constant",
      "question": "New `unavailable` reasons surface through `executionContext.typecheck`; `R4_6B_TYPECHECK_UNAVAILABLE` is not edited",
      "options": [
        "surface through `executionContext.typecheck`, constant untouched",
        "editing the constant with the fixtures and `tighter-reviews` R4.6b in one change"
      ]
    },
    {
      "header": "D7 — Diff delivery on the dashboard path",
      "question": "The diff body travels by a file beside the output path; the diff state travels inline in the prompt",
      "options": [
        "diff body by file beside the output path, state inline",
        "inlining the body in the prompt",
        "omitting it"
      ]
    }
  ]
}