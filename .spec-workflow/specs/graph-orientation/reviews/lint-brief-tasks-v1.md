# Lint brief — graph-orientation tasks v1

Read and obey /home/mcf/repo/spec-workflow-mcp/.spec-workflow/agent-rules.md first.

## Job
Fix the lint findings below in v1 of `/home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/graph-orientation/tasks.md` in place, then report in 150 words or fewer: files touched; each finding as `<id>: accepted | partially accepted | rejected`; citations verified (count); the document's word count; flags. No file contents. Context file `/home/mcf/repo/spec-workflow-mcp/.spec-workflow/specs/graph-orientation/codebase-context.md` first; requirements.md and design.md in the same dir; code root `/home/mcf/repo/spec-workflow-mcp`. Cap: 150 words per task block excluding its prompt; do not grow past it. Edit v1 in place, add no version line, append under the v1 Revision History line one nested bullet `- **Lint pass.** <n> fixed; rejected: <none | L-n reason, …>`. Verify every citation you add or change against the real tree, reading both ends of every range. Every citation carries its filename, never a bare `:<line>`. After accepting a finding, fix every sibling with the same construct and list them. Edit only the document (and a context-file line an accepted finding refutes). Do not ask questions.

## Findings
L-1 through L-24 (warning, citation-identifier, line 35): the citation at tasks.md line 35 lists these identifiers as present in the cited ranges `src/watch/__tests__/usage.test.ts:333` and `src/tools/__tests__/harness.test.ts:533`, but each is absent from those ranges: UsageCell, emptyCell, addCell, reduceSpawn, isGraphCall, event, tool, Bash, summary, graphify, explain, query, path, buildUsageReport, start, graph, anthropic, UsagePhase, cmpPhase, cols, pair, usageDelta, kinds, orchestratorShare. Fix the line-35 citation: cite the exact file and line range where each referenced identifier actually lives (read both ends of every range you write), or drop identifier names the citation does not need. A citation must resolve to a range that contains the identifier it names.
L-25 (error, citation-path, line 53): cited path `formats.md` has no directory prefix; cite it by its path from the code root or spec store (e.g. `dir/formats.md`).
L-26 (error, citation-path, line 80): cited path `TOOLS-REFERENCE.md` has no directory prefix; cite it by its full path from the code root or spec store.
L-27 (error, citation-path, line 88): cited path `briefs.md` has no directory prefix; cite it by its full path from the code root or spec store.
