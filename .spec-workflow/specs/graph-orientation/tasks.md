# Tasks Document — graph-orientation
Document version: v1

Design components map to tasks as follows: C1 is task 1, C3 is task 2, C5 is tasks 3 and 4, C2 is task 5, C4 is tasks 6 and 7, C6 is task 8 and C7 is task 9; task 10 runs the in-loop scenarios (1), (2), (5) and (6). No steering document exists, so the standards are those of `.spec-workflow/agent-rules.md`: tests in `__tests__` next to the code, `harness/` as the source with the `plugins/` copies committed in the same commit, node 20 guaranteed fields only.

Dependency order: task 1 adds the graph script and task 2 the brief section, both with no dependency. Task 3 adds the `graph` cell field and the table column, and updates every exact-value assertion the new field touches in the same commit; task 4 then feeds the activity log into the fold task 3 exports. Task 5 makes the supervisor call the task 1 script and copy it to the run's scratch dir; task 6 mirrors the task 2 section text into the document skill and guards it with a test; task 7 calls the scratch copy task 5 makes. Tasks 8 and 9 depend on no code; task 10 runs last. Every task leaves `npx tsc --noEmit` clean and every existing suite green, and its prompt names each existing exact-value assertion it changes.

- [x] 1. Graph script with `fact` and `refresh` modes
  - File: harness/skills/sdd-continue/references/sdd-graph.sh
  - File: src/__tests__/sdd-graph.test.ts
  - File: plugins/ (generated copies)
  - Write the script and its integration test as design C1 specifies.
  - Purpose: one tested file resolves the graph fact and runs the refresh with a timeout and without force (design C1, D1, D2, D3).
  - _Leverage: harness/skills/sdd-continue/references/sdd-cache-ttl.sh:1-22, src/__tests__/providers-map.test.ts:17-34, src/__tests__/cache-ttl-probe.test.ts:1-40_
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.4, 2.5, 2.7_
  - _Prompt: Task: Write sdd-graph.sh with a header comment in the sdd-cache-ttl.sh shape, `set -u`, and node bodies in single-quoted strings with no apostrophes. `fact CODE_ROOT MAIN_CHECKOUT` prints exactly the three design C1 lines (`GRAPH:`, `GRAPH_BEHIND:`, `GRAPH_BUILT_AT:`) and exits 0: `GRAPH: none` with both freshness lines `n/a` when `MAIN_CHECKOUT/graphify-out/graph.json` is missing or `command -v graphify` fails; both freshness lines `unknown` on a missing or empty top-level `built_at_commit`, a JSON parse error, or a non-zero `git -C CODE_ROOT rev-list --count SHA..HEAD`. `refresh CODE_ROOT [TIMEOUT_S]` runs `graphify update CODE_ROOT` through node `spawnSync` with `stdio: 'ignore'`, `timeout` of TIMEOUT_S times 1000 (default 100) and a copy of the environment without `GRAPHIFY_FORCE`. On status 0 it prints `refresh: ok`, `GRAPH_BEHIND: 0` and `GRAPH_BUILT_AT:` plus the output of `git -C CODE_ROOT rev-parse HEAD` (`unknown` when that fails); otherwise one line, first match wins: `refresh: failed timeout Ns` (`error.code` is `ETIMEDOUT`), `refresh: failed CODE` (another spawn error), `refresh: failed exit N` (non-zero status), `refresh: failed signal SIG`. It always exits 0 and never passes `--force`. The shell part reads every optional environment variable as `${VAR:-}`, never bare `$VAR`. Run `node scripts/sync-plugin-assets.cjs` and commit `plugins/`. Write the test on the cache-ttl-probe.test.ts pattern: a temp git repo with author and committer set through the environment; a stub `graphify` in a temp dir that exits 0, exits 1, or runs `exec sleep 5`, and writes `${GRAPHIFY_FORCE:-}` to a file in its temp dir; `PATH` is the stub dir (when the case has one), the dir of `process.execPath`, and `/usr/bin:/bin`. Cover the design Testing Strategy cases: no graph file gives `none` and `n/a`; no binary gives `none`; two commits after `built_at_commit` give `GRAPH_BEHIND: 2`; a missing key gives `unknown`; refresh ok prints the HEAD sha; a stub exit 1 prints `refresh: failed exit 1`; `TIMEOUT_S` 1 with the sleeping stub prints `refresh: failed timeout 1s`; `GRAPHIFY_FORCE=1` in the caller's environment reaches the stub empty; every case exits 0 | Restrictions: Assert only on exit status and stdout (node 20 fields). The script writes no file itself; the test writes only under its temp dirs. No existing test file changes | Success: `bash -n harness/skills/sdd-continue/references/sdd-graph.sh`; `npx vitest run src/__tests__/sdd-graph.test.ts` green; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [x] 2. `## Code graph` section in `harness brief`
  - File: src/tools/harness.ts
  - File: src/tools/__tests__/harness.test.ts
  - Add the section function, the missing-freshness check and the append of design C3.
  - Purpose: a brief called with a graph path ends with the section; every other call writes the pre-spec bytes (design C3).
  - _Leverage: src/tools/harness.ts:76, src/tools/harness.ts:485-535, src/tools/harness.ts:547-655, src/tools/harness.ts:568-574, src/tools/harness.ts:617-631, src/tools/harness.ts:633-635, src/tools/__tests__/harness.test.ts:165-185_
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 3.7, 3.8_
  - _Prompt: Task: Export `codeGraphSection(graph, builtAt, behind)` that returns the design C3 text exactly, one line each, ending in a newline, with the hint line only when `behind !== '0'`. In `briefAction`: after the output-path check (:568-574), when `values.graph` is a non-empty string other than `none`, collect `graphBuiltAt` and `graphBehind` that are `undefined` or `null` and fail with `brief: graph value(s) 'graphBuiltAt', 'graphBehind' missing (values.graph is set); no file written`, naming only the missing ones in the quote style of :617-631. After the fill (:633-635), on the same condition, append the section called with `String()` of the three values; the separator is `\n` when the filled text ends in a newline, else `\n\n`. Any other `values.graph` (absent, `none`, not a string) leaves the text untouched. The `values` description (:76) names `graph`, `graphBuiltAt` and `graphBehind`. Add tests: for each of the five templates (`grep -n '^  [a-z]*: \[' src/tools/harness.ts` lists them) with `graph`, `graphBuiltAt: 'abc1234'` and `graphBehind: '3'`, the file ends with one blank line and then the section, hint line included; `graphBehind: '0'` gives no hint line; no graph values and `graph: 'none'` give identical files without `## Code graph`; `graph` without `graphBehind` fails, the message names `graphBehind`, and no file is written | Restrictions: No template gains a `{{graph...}}` placeholder. The tool reads no graph file, runs no git and spawns no process, so the doc comment at :17-27 stays true. No existing assertion in harness.test.ts changes value: no existing brief call passes a graph value, so its output is byte-identical | Success: `npx tsc --noEmit`; `npx vitest run src/tools/__tests__/harness.test.ts` green_

- [x] 3. Graph count in the usage fold and tables
  - File: src/watch/usage.ts
  - File: src/watch/__tests__/usage.test.ts
  - File: src/tools/__tests__/harness.test.ts
  - Add the cell field, the window helper, the match rule, the count function and the table column of design C5.
  - Purpose: every usage cell carries a `graph` count and every table row prints it as the last column of its group (design C5, D5, D8, D9).
  - _Leverage: src/watch/usage.ts:13-34, src/watch/usage.ts:75-92, src/watch/usage.ts:106-111, src/watch/usage.ts:113-218, src/watch/usage.ts:277-294, src/watch/usage.ts:361-410, src/watch/ledger.ts:26-36, src/watch/__tests__/usage.test.ts:8-11_
  - _Requirements: 6.2, 6.3, 6.4, 6.5, 6.7, 6.8_
  - _Prompt: Task: `UsageCell` gains `graph: number`; `emptyCell` sets 0 and `addCell` sums it. Lift the window loop (:283-292) unchanged into `windowPhase(at, phaseStarts, phaseEnds)` and call it from `reduceSpawn` with those three arguments, so spawn attribution does not change. Export `isGraphCall(row)`: `event` is `tool`, `tool` is `Bash`, and `summary` matches the design C5 token rule: `graphify` at the start of the summary or after whitespace, `;`, `&`, a pipe character or `(`, then whitespace and one of the words `explain`, `query` or `path` (copy the regex from design C5). Export `applyGraphCounts(report, events, activity)`: sort the events by `ts` as `buildUsageReport` does, take the `phase.start` and `phase.end` rows, and for each graph-call row add 1 to `graph` on `phases[windowPhase(ms(row.ts), starts, ends) ?? 'unknown'].agents[row.agent ?? 'unknown']`, on that phase's `total` and `providers.anthropic`, and on the report's `total` and `providers.anthropic`. A missing phase gets an empty `UsagePhase` and the phases are re-sorted by `cmpPhase`; a missing agent gets an empty cell with 0 spawns; the function mutates and returns `report`. Tables (design C5): the one-report header gains `graph` as its last column name; agent rows, phase totals (before `  orch`) and the spec total (before the kinds text) end their cell group with one more separator and the grouped count; in the compare table `cols` gains `graph` as its last name, `pair` prints the graph cell, and an absent side prints six dashes. `ce` (:9-11) defaults `graph: 0`. Update at least these exact assertions, and widen the list to every assertion the change touches: src/watch/__tests__/usage.test.ts:333, 335, 336, 337, 352, 353 and 467 (compare strings) and src/tools/__tests__/harness.test.ts:533, 596 and 597 (cell literals gain `graph: 0`); `grep -n "cacheUnknownGap" src/tools/__tests__/harness.test.ts src/watch/__tests__/usage.test.ts` finds the literals. The one-report substrings (for example :300, :417, :428, :440) stay unedited and still match. Add cases: `isGraphCall` matches `graphify explain "x" --graph p` and `cd a && graphify query t`, and rejects `graphify update .`, `grep -n "graphify explain" f` and a non-Bash row; window attribution; a row outside every window goes to `unknown`; a phase and an agent present only through graph rows (0 spawns, cache columns `-`) | Restrictions: `buildUsageReport` changes only through `emptyCell` and `addCell`; `usageDelta`, `kinds`, `orchestratorShare` and src/tools/harness.ts do not change; the module reads no file | Success: `npx tsc --noEmit`; `npx vitest run src/watch/__tests__/usage.test.ts src/tools/__tests__/harness.test.ts` green_

- [x] 4. `harness usage` reads the activity log
  - File: src/tools/harness.ts
  - File: src/tools/__tests__/harness.test.ts
  - Add the activity reader and join it to each report in the usage action.
  - Purpose: each spec of a `usage` call gets its graph counts from its own `harness-activity.jsonl` (design C5).
  - _Leverage: src/tools/harness.ts:1029-1064, src/tools/harness.ts:1072-1093, src/watch/ledger.ts:176-189, harness/hooks/sdd-activity.sh:28-29_
  - _Requirements: 6.1, 6.4, 6.6_
  - _Prompt: Task: Add `readSpecActivity(workflowRoot, specName)` beside `readSpecLedger` (:1029-1064): resolve `harness-activity.jsonl` in the spec dir through `PathUtils.safeJoin`, return an empty list on `ENOENT`, return the `Failed to read <path>: <message>` error on any other read error, and parse with `parseJsonl`. In `usageAction` (:1072-1093) pass each report through the graph-count function task 3 exports (read its name in src/watch/usage.ts), with that spec's events and activity rows, for the primary spec and for `compareSpecName`; an activity read error fails the call as a ledger read error does. Add tests with an activity fixture written next to the ledger: counts per agent and phase in `data.report`, the `graph` header in the message, a compare spec whose count comes from its own file, and a missing activity file giving 0 | Restrictions: Do not change the hook; read nothing outside the spec dir; the tool still spawns no process. No existing harness.test.ts assertion changes value in this task: task 3 already added `graph: 0` to the cell literals, and no existing test writes an activity file | Success: `npx tsc --noEmit`; `npx vitest run src/tools/__tests__/harness.test.ts src/watch/__tests__/usage.test.ts` green_

- [x] 5. Supervisor resolves the graph fact and carries it
  - File: harness/skills/sdd-continue/SKILL.md
  - File: harness/skills/sdd-continue/references/formats.md
  - File: plugins/ (generated copies)
  - Purpose: every orchestrator launch prompt carries the three graph lines, `run.start` carries the graph keys only with a graph, and a stale main-checkout graph is refreshed once before `run.start` (design C2, D4, D7, D10).
  - _Leverage: harness/skills/sdd-continue/SKILL.md:66-94, harness/skills/sdd-continue/SKILL.md:81-86, harness/skills/sdd-continue/SKILL.md:96-116, harness/skills/sdd-continue/SKILL.md:224-247, harness/skills/sdd-continue/SKILL.md:262-269, harness/skills/sdd-continue/SKILL.md:320-327, harness/skills/sdd-continue/references/formats.md:194_
  - _Requirements: 1.1, 1.5, 1.6, 1.7, 1.8, 2.1, 2.4, 2.5, 2.6_
  - _Prompt: Task: Edit per design C2. Step 1 roots gains a **Code graph** bullet after the worktree check (:71-73): run the graph script task 1 added (read its name in harness/skills/sdd-continue/references/) as `bash <base dir>/references/` plus that name, with `fact <CODE_ROOT>` and the main checkout (or `CODE_ROOT` when not in a worktree); keep its three lines as `GRAPH`, `GRAPH_BEHIND` and `GRAPH_BUILT_AT`; never resolve against the spec store repo. The run-ledger paragraph (:96-116), before the `run.start` call: when `GRAPH` is a path, `cp` the script into `/tmp/scratchpad/sdd/<spec>/` under its own name; then, when `GRAPH_BEHIND` is not `0` and `WORKTREE` is `no`, run that copy with `refresh <CODE_ROOT>`. On `refresh: ok` take its two value lines; otherwise keep the old values and hold the failure line. The `run.start` call adds `graph=<GRAPH> graphBehind=<GRAPH_BEHIND>` only when `GRAPH` is a path; a held failure line becomes `note "text=graph refresh: <line>"` right after `run.start`. The launch block (:226-244) gains `GRAPH:`, `GRAPH_BEHIND:` and `GRAPH_BUILT_AT:` after `LAUNCHER:`, with the value forms of the design Data Models. The worktree rule (:320-327) re-runs `fact` with the new `CODE_ROOT` and the main checkout after its re-run worktree check, with no refresh. Scratch-wipe recovery (:262-269) also re-copies the script when `GRAPH` is a path. The `run.start` row at harness/skills/sdd-continue/references/formats.md:194 lists `graph` and `graphBehind`, present only when a graph exists. Run the asset sync and commit `plugins/` | Restrictions: The supervisor never reads `graph.json` itself, never commits `graphify-out/`, and never stops a run on a graph outcome; with `GRAPH` `none` the ledger keeps the pre-spec shape | Success: `grep -c 'GRAPH_BUILT_AT' harness/skills/sdd-continue/SKILL.md` prints at least 2; `grep -n 'graphBehind' harness/skills/sdd-continue/references/formats.md` finds the `run.start` row; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [ ] 6. Document skill passes the graph and mirrors the block
  - File: harness/skills/sdd-document-phase/SKILL.md
  - File: harness/skills/sdd-document-phase/references/briefs.md
  - File: src/tools/__tests__/harness.test.ts
  - File: plugins/ (generated copies)
  - Purpose: every document-phase brief, round section and narrow-check prompt carries the code graph when `GRAPH` is a path, and a test keeps the mirrored block equal to the server text (design C4, D6).
  - _Leverage: harness/skills/sdd-document-phase/SKILL.md:8-12, harness/skills/sdd-document-phase/SKILL.md:43-44, harness/skills/sdd-document-phase/SKILL.md:161-163, harness/skills/sdd-document-phase/SKILL.md:283-286, harness/skills/sdd-document-phase/references/briefs.md:1-4, harness/skills/sdd-document-phase/references/briefs.md:50-59, harness/skills/sdd-document-phase/references/briefs.md:141-208, harness/skills/sdd-document-phase/references/briefs.md:390-409_
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5, 5.1, 5.2, 5.3, 5.4_
  - _Prompt: Task: Per design C4. SKILL.md: the launch-key list (:8-12) gains `GRAPH`, `GRAPH_BEHIND` and `GRAPH_BUILT_AT`; the design C4 standing rule goes, verbatim, after the read-and-obey rule (:43-44); Step 2 item 3 (:161-163) and Step 4b item 1 (:283-286) name the code graph block. harness/skills/sdd-document-phase/references/briefs.md: add a `## Code graph block` section at the end of the file with the one fill sentence of design C4 and a fenced block equal to the output of the section function task 2 exports, called with `'<GRAPH>'`, `'<GRAPH_BUILT_AT>'` and `'<GRAPH_BEHIND>'` (generate the text by calling that function; do not retype it). The round-section fence (:141-208) and the narrow-check fence (:390-409) each end, inside the fence, with the line `<GRAPH is a path: the code graph block, filled.>`. The `## Codebase context` text (:50-59) gains the design C4 conditional for requirements and for design or tasks, and the line "A graph node alone is not a citation; every line cites a range you read at both ends." inside the same condition; the shape bullets stay. Add a drift-guard test to harness.test.ts: read harness/skills/sdd-document-phase/references/briefs.md from the repository, take the lines between the fence lines under `## Code graph block`, and assert that they, joined with newlines plus a final newline, equal the section function's output for those three placeholders. Run the asset sync and commit `plugins/` | Restrictions: With `GRAPH` `none` every brief, round section and prompt reads as today; no orchestrator reads `graph.json` or runs a graphify read call. No existing harness.test.ts assertion changes value | Success: `npx vitest run src/tools/__tests__/harness.test.ts` green; `grep -c 'code graph block' harness/skills/sdd-document-phase/SKILL.md` prints at least 2; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [ ] 7. Implementation and close-out skills pass the graph and refresh it
  - File: harness/skills/sdd-implementation-phase/SKILL.md
  - File: harness/skills/sdd-closeout-phase/SKILL.md
  - File: plugins/ (generated copies)
  - Purpose: every brief these orchestrators assemble carries the graph values, and an implementer report in a non-worktree code root refreshes the graph (design C4, D7).
  - _Leverage: harness/skills/sdd-implementation-phase/SKILL.md:14-19, harness/skills/sdd-implementation-phase/SKILL.md:37-38, harness/skills/sdd-implementation-phase/SKILL.md:100-117, harness/skills/sdd-implementation-phase/SKILL.md:144-159, harness/skills/sdd-closeout-phase/SKILL.md:14-20, harness/skills/sdd-closeout-phase/SKILL.md:33-34, harness/skills/sdd-closeout-phase/SKILL.md:108-119, harness/skills/sdd-closeout-phase/SKILL.md:120-159_
  - _Requirements: 2.2, 2.3, 2.4, 2.5, 2.6, 4.1, 4.2, 4.5_
  - _Prompt: Task: In both skills the launch-key sentence (implementation :14-19, close-out :14-20) gains `GRAPH`, `GRAPH_BEHIND` and `GRAPH_BUILT_AT`, and the design C4 standing rule goes after the read-and-obey rule (implementation :37-38, close-out :33-34). Implementation per-task loop: after step 3 reads an implementer report (:100-117) and after each fix spawn's report in step 5 (:144-159), when `GRAPH` is a path and `WORKTREE` is `no`, run `bash /tmp/scratchpad/sdd/<SPEC>/` plus the name of the script task 5 copies there, with `refresh <CODE_ROOT>`, before the gate. `refresh: ok` replaces `GRAPH_BEHIND` and `GRAPH_BUILT_AT`; any other output keeps them and writes `note "text=graph refresh: <first line>"`. Close-out: the same call after each `sdd-implementer` report for a `harness` or `code` batch, with the landing root, only when that root is `CODE_ROOT` and `WORKTREE` is `no`; add one clause that today those batches land in a retro worktree (:113-118), so the call does not fire. Run the asset sync and commit `plugins/` | Restrictions: No refresh in a worktree; never `--force`; never commit `graphify-out/`; a refresh failure never stops the phase; no other step changes | Success: `grep -n 'graph refresh' harness/skills/sdd-implementation-phase/SKILL.md harness/skills/sdd-closeout-phase/SKILL.md` finds the rule in both files; `npm run check:plugin-assets` and `claude plugin validate . --strict` pass_

- [ ] 8. Docs name the graph values, lines and column
  - File: docs/TOOLS-REFERENCE.md
  - File: docs/SDD-HARNESS.md
  - Purpose: an operator reads what the three launch lines, the refresh and the `graph` column mean (design C6).
  - _Leverage: docs/TOOLS-REFERENCE.md:547-579, docs/SDD-HARNESS.md:253-264_
  - _Requirements: 7.1, 7.2_
  - _Prompt: Task: docs/TOOLS-REFERENCE.md: the `brief` bullet (:566-568) names the optional values `graph`, `graphBuiltAt` and `graphBehind` and the `## Code graph` section appended when `graph` is a path (a path without both freshness values fails the call); the `usage` bullet (:574-579) names the `graph` column, the count of `graphify explain`, `query` and `path` calls per agent from `harness-activity.jsonl`, for Agent-tool workers only. docs/SDD-HARNESS.md gains a `## Code graph` section after `## Workspace contract` (:253-264): the three launch lines and their values, the refresh rule (at run start when behind, after each implementer report, main checkout only, 100 s timeout, a failure is a ledger note), and that `graphify-out/` stays untracked. Write in ASD-STE100 Simplified Technical English | Restrictions: No other section changes; the "five actions" count at docs/TOOLS-REFERENCE.md:551-553 stays true | Success: `grep -n 'graphBuiltAt' docs/TOOLS-REFERENCE.md` and `grep -n '^## Code graph' docs/SDD-HARNESS.md` each find one line_

- [ ] 9. Pending live-verification record
  - File: .spec-workflow/specs/graph-orientation/verification-evidence.md
  - Verification-only task: its File line names no path under `CODE_ROOT`.
  - Purpose: the live halves of scenarios (2), (3) and (4) stay pending until an operator runs them in a rebuilt, restarted session, and the supervisor blocks the retrospective until they pass (design C7).
  - _Leverage: .spec-workflow/specs/agent-cache-ttl/verification-evidence.md:1-10, harness/skills/sdd-continue/SKILL.md:162-169, harness/skills/sdd-implementation-phase/SKILL.md:301-305, harness/skills/sdd-implementation-phase/references/briefs.md:12-15_
  - _Requirements: 7.3, 7.4_
  - _Prompt: Task: Write `/tmp/scratchpad/sdd/graph-orientation/verification-evidence.md` in the shape of the agent-cache-ttl record: the H1 `# Verification evidence — graph-orientation`, one paragraph (the lines run against the merged checkout after `npm run build` and a session restart; the retrospective requires every line `passed`), then three lines: `- (2) pending — a supervisor launch on a checkout with no graphify on PATH carries GRAPH: none and a run.start row without graph keys`; `- (3) pending — a requirements phase on a fixture spec with a graph: every codebase-context.md range exists in the code root, and harness-activity.jsonl shows the drafter's first graphify explain or query before its first raw read of the code root`; `- (4) pending — a fixture checkout whose agent-rules.md omits the worktree-per-change line: after an implementer commit that changes the code graph, graph.json built_at_commit equals HEAD`. `cp` it into the spec dir on its own line, and commit it in the spec store repo with a script under `/tmp/scratchpad/sdd/graph-orientation/` that changes into that repo (harness/skills/sdd-implementation-phase/references/briefs.md:12-15), message `docs(sdd): graph-orientation verification evidence pending`. Log the commit with `log-implementation` | Restrictions: Do not run the live scenarios; never edit `~/.claude`; the Edit and Write tools refuse spec-store paths in a worktree session, so write in the scratch dir and copy | Success: `grep -c '^- (.) pending' <spec dir>/verification-evidence.md` prints 3 and the file is committed_

- [ ] 10. End-to-end verification of scenarios (1), (2), (5) and (6)
  - File: none (verification only)
  - Run the full check suite and scenario (5); leave the task 9 lines `pending` for the operator.
  - Purpose: the PR opens on the in-loop scenarios, and the evidence file carries the live halves (requirements 7.3; design D11).
  - _Leverage: src/tools/harness.ts:110-130, src/tools/harness.ts:1072-1093, src/tools/__tests__/harness.test.ts:18-23_
  - _Requirements: 7.3_
  - _Prompt: Task: In `CODE_ROOT` run, each as its own command, `npm run build`, `npm test`, `npm run check:plugin-assets` and `claude plugin validate . --strict`. Scenario (1) is the task 2 brief tests and the in-loop half of scenario (2) is the task 1 no-binary case; confirm both ran green in `npm test`. Scenario (5): write a node script under `/tmp/scratchpad/sdd/graph-orientation/` that imports `dist/tools/harness.js` from `CODE_ROOT` and calls `harnessHandler({ action: 'usage', specName: 'graph-orientation', compareSpecName: 'agent-cache-ttl' }, { projectPath: R, workspacePath: R })`, with R the spec store repo root (the parent of `SPEC_STORE_ROOT`), and prints the message, `data.report.total.graph` and `data.compare.total.graph`. Pass when the header line holds `graph` twice and `data.compare.total.graph` is 0, which `grep -c graphify <SPEC_STORE_ROOT>/specs/agent-cache-ttl/harness-activity.jsonl` printing 0 confirms. Put the two counts and the check results in the report | Restrictions: Do not run the live scenarios or edit the evidence file; the script only reads, and never calls or rewrites `EVENT_SCRIPT` | Success: all four commands exit 0 and scenario (5) passes as stated_

## Decisions taken in this document

- D1 — The usage column is split over two tasks, the fold and the tool wiring: options were two tasks, one task; chosen because one block would pass the 150-word cap, and the fold task must update the cell literals the tool tests assert in the same commit.
- D2 — The drift-guard test lands with the document-skill task: options were the document-skill task, the brief-section task with a bridge; chosen because the mirrored block does not exist until that task writes it, so no bridge is needed.
- D3 — The mirrored code graph block goes at the end of the document skill's briefs reference: options were the end of the file, beside the round section; chosen because the round section and the narrow-check prompt both point at it and neither owns it.
- D4 — Order is script, server, supervisor, orchestrator skills, docs, evidence, verification: options were that order, skills first; chosen because each skill task uses an artefact an earlier task creates, so no task needs a bridge.
- D5 — Scenario (5) passes the spec store repo root as the project path: options were the spec store repo root, the worktree code root; chosen because the ledgers and activity logs live in the main checkout's spec store, and the worktree copy of the store can be stale.
- D6 — The close-out refresh condition follows the design as written: options were the design's condition, the requirement's main-checkout literal; chosen because the tasks phase does not re-decide the design and the rule does not fire today (design review R1-3, MINOR).
- D7 — The evidence file carries three pending lines, numbered (2), (3) and (4): options were three lines, one line per decomposition scenario; chosen because scenarios (1), (5), (6) and the in-loop half of (2) close in task 10.
- D8 — Scenario (5) checks the counts on the returned data and the header, not each table cell: options were data totals plus the header, parsing every table cell; chosen because the data totals are exact and the header proves the column prints.

## Scope notes

- Carried from design: none.
- Design review R1-1 (the compare-row assertion outside the compare block) is in the task 3 assertion list; R1-2 (the window helper's arguments) is stated in the task 3 prompt; R1-3 is kept as the design wrote it (D6).
- Deferred to the tracked evidence file: the live halves of scenarios (2), (3) and (4) (task 9).
- Deferred: spec 11's test-author template does not exist; the section is template-independent and covers it when added.
- Not changed: the activity hook, agent frontmatter, the graph ignore file, the global PreToolUse nudge, the retrospective orchestrator, the usage delta.

## Revision History

- **v1** (2026-09-25) — Initial draft.
  - **Lint pass.** L-25, L-26, L-27 accepted (7 bare-filename citations fixed with full paths: formats.md, briefs.md x2, TOOLS-REFERENCE.md, SDD-HARNESS.md, TOOLS-REFERENCE.md:551-553, briefs.md:12-15 — the last four are siblings of the same construct in tasks 6, 8 and 9); rejected: L-1 through L-24 (the line-35 citation names the lines where the exact-value assertions live, not lines that must hold every identifier used earlier in the same prompt; both cited lines were verified to hold the assertions they were cited for).
