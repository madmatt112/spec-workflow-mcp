# Implementation Log: Task 3

**Summary:** Added a `graph` count to the usage fold (design C5, D5, D8, D9). UsageCell gains a `graph: number` field (emptyCell sets 0, addCell sums it). Lifted the live-phase window loop from reduceSpawn into a module-level windowPhase(at, phaseStarts, phaseEnds) helper, called unchanged from reduceSpawn so spawn attribution is identical. Added exported isGraphCall(row) (Bash tool, summary matches /(^|[\s;&|(])graphify\s+(explain|query|path)\b/) and applyGraphCounts(report, events, activity) which folds graph-call activity rows onto the phase window's agent cell, phase total, phase anthropic provider, report total and report anthropic provider, creating a missing phase (re-sorted by cmpPhase) or agent (0 spawns) as needed and mutating/returning the report. Tables print `graph` as the last column of each group in both the one-report and compare views; the compare `pair` prints the graph cell and an absent side prints six dashes. buildUsageReport, usageDelta, kinds, orchestratorShare and src/tools/harness.ts are unchanged; the module reads no file.

**Timestamp:** 2026-09-25T21:40:35.093Z
**Log ID:** 3c3cfd62-e1d5-48ea-9ab6-c89f832089af

---

## Statistics

- **Lines Added:** +147
- **Lines Removed:** -35
- **Files Changed:** 3
- **Net Change:** 112

## Files Modified
- src/watch/usage.ts
- src/watch/__tests__/usage.test.ts
- src/tools/__tests__/harness.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### isGraphCall
- **Purpose:** True when an activity row is a graphify read call: event tool, tool Bash, summary matches /(^|[\s;&|(])graphify\s+(explain|query|path)\b/. Rejects graphify update and a grep of the phrase.
- **Location:** src/watch/usage.ts
- **Signature:** export function isGraphCall(row: ActivityEvent): boolean
- **Exported:** No

#### applyGraphCounts
- **Purpose:** Folds graph-call activity rows onto report: adds 1 to graph on the windowed phase's agent cell, phase total, phase anthropic provider, report total and report anthropic provider. Creates missing phase (empty UsagePhase, re-sorted by cmpPhase) or agent (empty cell, 0 spawns). Mutates and returns report. Meant to be called by usageAction in a later tool-layer task.
- **Location:** src/watch/usage.ts
- **Signature:** export function applyGraphCounts(report: UsageReport, events: LedgerEvent[], activity: ActivityEvent[]): UsageReport
- **Exported:** No

#### windowPhase
- **Purpose:** Live-phase window at time at: latest phase.start at or before at not yet closed. Lifted unchanged from reduceSpawn's inline loop; called by both reduceSpawn and applyGraphCounts.
- **Location:** src/watch/usage.ts
- **Signature:** function windowPhase(at: number, phaseStarts: LedgerEvent[], phaseEnds: LedgerEvent[]): string | undefined
- **Exported:** No

### Integrations

#### Integration
- **Description:** applyGraphCounts and isGraphCall are the pure fold half of the usage graph column; the tool layer (readSpecActivity + usageAction in src/tools/harness.ts) that reads harness-activity.jsonl and calls applyGraphCounts is a separate task and was not touched here.
- **Frontend Component:** src/watch/usage.ts applyGraphCounts
- **Backend Endpoint:** harness usage action (src/tools/harness.ts usageAction)
- **Data Flow:** harness-activity.jsonl -> ActivityEvent[] -> applyGraphCounts(buildUsageReport(events), events, activity) -> report.graph counts -> formatUsageTable graph column

