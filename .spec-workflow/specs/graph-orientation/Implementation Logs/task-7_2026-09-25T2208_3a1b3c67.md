# Implementation Log: Task 7

**Summary:** Implementation and close-out orchestrator skills now carry the code graph on every harness brief and refresh it in a non-worktree code root (design C4, D7). Both launch-key sentences (implementation :14-19, close-out :14-20) gain GRAPH, GRAPH_BEHIND, GRAPH_BUILT_AT. Each skill gains the verbatim C4 standing rule after the read-and-obey rule: when GRAPH is a path every harness brief carries values.graph/graphBuiltAt/graphBehind, when none pass none, never read graph.json. Implementation per-task loop: step 3 gains a Graph refresh clause (after the implementer report, when GRAPH is a path and WORKTREE is no, run the scratch sdd-graph.sh refresh CODE_ROOT before the gate; refresh: ok replaces the two freshness values, else keep them and note graph refresh: <first line>); step 5 references that same refresh after each fix spawn report. Close-out Step 2 item 3 gains the same call after a harness/code batch report only when the landing root is CODE_ROOT and WORKTREE is no, plus a clause that today those batches land in a retro worktree (Step 2 item 1), so the call does not fire. A refresh failure never stops the phase; the script (task 1) enforces no --force and writes only to the code root's gitignored graphify-out/. With GRAPH none every path reads as before. Plugin mirrors synced.

**Timestamp:** 2026-09-25T22:08:42.575Z
**Log ID:** 3a1b3c67-4e87-4e71-9725-47e8b6804528

---

## Statistics

- **Lines Added:** +112
- **Lines Removed:** -20
- **Files Changed:** 8
- **Net Change:** 92

## Files Modified
- harness/skills/sdd-implementation-phase/SKILL.md
- harness/skills/sdd-closeout-phase/SKILL.md
- plugins/spec-workflow-harness/skills/sdd-implementation-phase/SKILL.md
- plugins/spec-workflow-harness/skills/sdd-closeout-phase/SKILL.md
- plugins/spec-workflow-mcp/skills/sdd-implementation-phase/SKILL.md
- plugins/spec-workflow-mcp/skills/sdd-closeout-phase/SKILL.md
- plugins/spec-workflow-mcp-with-dashboard/skills/sdd-implementation-phase/SKILL.md
- plugins/spec-workflow-mcp-with-dashboard/skills/sdd-closeout-phase/SKILL.md

## Files Created
_No files created_

---

## Artifacts

### Integrations

#### Integration
- **Description:** The implementation and close-out orchestrators forward the supervisor's GRAPH/GRAPH_BUILT_AT/GRAPH_BEHIND to every harness brief (server appends codeGraphSection) and, in a non-worktree code root, refresh the graph via the per-run scratch copy of sdd-graph.sh, updating their own freshness values on refresh: ok.
- **Frontend Component:** harness/skills/sdd-implementation-phase/SKILL.md and harness/skills/sdd-closeout-phase/SKILL.md (orchestrators)
- **Backend Endpoint:** /tmp/scratchpad/sdd/<SPEC>/sdd-graph.sh refresh <root> (task 1) and codeGraphSection/briefAction in src/tools/harness.ts (task 2)
- **Data Flow:** supervisor launch lines GRAPH/GRAPH_BEHIND/GRAPH_BUILT_AT -> orchestrator standing rule -> harness brief values.graph/graphBuiltAt/graphBehind -> server appends ## Code graph; per-task/per-batch after an implementer report the orchestrator runs sdd-graph.sh refresh CODE_ROOT (impl: before the gate; close-out: only when the landing root is CODE_ROOT and WORKTREE is no, which never fires today) -> refresh: ok replaces GRAPH_BEHIND/GRAPH_BUILT_AT, else note graph refresh: <first line>

