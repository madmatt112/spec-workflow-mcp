# Implementation Log: Task 4

**Summary:** Wired the harness usage action to each spec's own harness-activity.jsonl so graph counts come from that spec's file (design C5). Added readSpecActivity(workflowRoot, specName) beside readSpecLedger: resolves harness-activity.jsonl through PathUtils.getSpecPath + safeJoin, returns an empty list on ENOENT, returns a Failed to read/resolve error naming the path otherwise, and parses with parseJsonl<ActivityEvent>. usageAction now reads the activity for the primary spec and, when comparing, for compareSpecName, then folds each report through applyGraphCounts (exported by src/watch/usage.ts, task 3) with that spec's events and activity rows; an activity read error fails the call exactly as a ledger read error does. The hook, the pure fold, and all other actions are untouched; the tool still spawns no process and reads nothing outside the spec dir.

**Timestamp:** 2026-09-25T21:46:07.304Z
**Log ID:** 34647b47-546e-4237-beb9-35669dea1aad

---

## Statistics

- **Lines Added:** +108
- **Lines Removed:** -3
- **Files Changed:** 2
- **Net Change:** 105

## Files Modified
- src/tools/harness.ts
- src/tools/__tests__/harness.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### readSpecActivity
- **Purpose:** Read one spec's harness-activity.jsonl under the spec store: resolve via PathUtils.getSpecPath + safeJoin, empty list on ENOENT, error naming the path on any other read error or a safeJoin throw, parse with parseJsonl<ActivityEvent>. The tool-layer read half of the usage graph column.
- **Location:** src/tools/harness.ts (beside readSpecLedger)
- **Signature:** async function readSpecActivity(workflowRoot: string, specName: string): Promise<{ activity: ActivityEvent[] } | { error: string }>
- **Exported:** No

### Integrations

#### Integration
- **Description:** usageAction reads each spec's harness-activity.jsonl and folds graph-call rows onto its report via applyGraphCounts, completing the usage graph column started in task 3.
- **Frontend Component:** src/tools/harness.ts usageAction
- **Backend Endpoint:** harness usage action
- **Data Flow:** harness-activity.jsonl -> readSpecActivity -> ActivityEvent[] -> applyGraphCounts(buildUsageReport(events, spec), events, activity) -> report.graph counts -> formatUsageTable graph column / data.report.graph

