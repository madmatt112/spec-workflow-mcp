# Implementation Log: Task 2

**Summary:** Carried the hook's usage keys onto the watch model's SpawnNode and moved PHASE_ORDER to the ledger module. Added five optional fields (model, input, output, cacheWrite, cacheRead) to SpawnNode. The spawn.end pairing now reads the four numbers with the same coercion as tokens (truthy + non-NaN Number()) and model as-is. The fold pass's token fill now guards on match.tokens === undefined, so a digit-string spawn.end wins over a later folded spawn.usage while a spawn.usage still fills a token-less spawn.end. The agent.stop join fill now also requires s.endedAt !== undefined, so only a closed node takes hook usage. Exported PHASE_ORDER from ledger.ts (was a local const in render.ts) and imported it in render.ts; its uses at render.ts lines 84-85 and 160 are unchanged. AGENT_PROFILES literal and tokensTotal are untouched (loader is Task 3). Added two ledger.test.ts cases on the ledger() fixture.

**Timestamp:** 2026-09-21T16:55:08.244Z
**Log ID:** a0885c1f-ef6e-48cc-9b46-dec0a144751a

---

## Statistics

- **Lines Added:** +40
- **Lines Removed:** -5
- **Files Changed:** 3
- **Net Change:** 35

## Files Modified
- src/watch/ledger.ts
- src/watch/render.ts
- src/watch/__tests__/ledger.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### PHASE_ORDER
- **Purpose:** The SDD phase order for the watch view's phase rail; moved from render.ts so the ledger module owns it (design D4, Req 5.2). Value ['requirements','design','tasks','implementation','retrospective','closeout']
- **Location:** src/watch/ledger.ts:56-57
- **Exported:** No

### Classes

#### SpawnNode
- **Purpose:** Watch model spawn node; now carries the hook-written usage breakdown so render (Task 4) and any usage report (Task 5) can read declared-vs-actual model and per-kind token counts
- **Location:** src/watch/ledger.ts:63-83
- **Exported:** No

### Integrations

#### Integration
- **Description:** buildModel reads the harness hook's spawn.end usage keys onto SpawnNode; spawn.end tokens win over a later spawn.usage; only a closed node takes an agent.stop token fill
- **Frontend Component:** src/watch/render.ts (renders SpawnNode)
- **Backend Endpoint:** buildModel in src/watch/ledger.ts (spawn.end pairing :246-252, fold token fill :277, agent.stop join :314)
- **Data Flow:** harness-events.jsonl spawn.end {model,input,output,cacheWrite,cacheRead,tokens as String(n)} -> buildModel coerces numbers (truthy + !Number.isNaN) and model as-is -> SpawnNode -> render/tokensTotal

