# Implementation Log: Task 7

**Summary:** Made the SubagentStop hook the single writer of per-spawn usage. Added `num` and `readUsage` to the inline node in sdd-activity.sh: readUsage does a whole-file read of transcript_path in its own try, JSON.parses each line (torn lines skipped), sums message.usage.{input_tokens,output_tokens,cache_creation_input_tokens,cache_read_input_tokens} over assistant entries, and joins distinct message.model values first-seen with `+`, returning null when no entry qualifies. On SubagentStop it now computes u = transcript_path ? readUsage(...) : null, sets e.tokens = u.tokens on the activity line when non-null, and appends an unguarded spawn.end row for EVERY sdd-* agent (orchestrators included). Non-null u writes the six usage keys as String(n) with model omitted when empty; null u writes tokens:"unknown" and no usage keys. Removed the old d.usage.tokens read and the -orchestrator guard. Regenerated plugins/*/hooks/ copies.

**Timestamp:** 2026-09-21T17:40:11.391Z
**Log ID:** 88d6ad4a-bca2-4329-8c56-3ea5def4b143

---

## Statistics

- **Lines Added:** +130
- **Lines Removed:** -8
- **Files Changed:** 5
- **Net Change:** 122

## Files Modified
- harness/hooks/sdd-activity.sh
- plugins/spec-workflow-harness/hooks/sdd-activity.sh
- plugins/spec-workflow-mcp/hooks/sdd-activity.sh
- plugins/spec-workflow-mcp-with-dashboard/hooks/sdd-activity.sh
- src/__tests__/hook-spawn-events.test.ts

## Files Created
_No files created_

---

## Artifacts

### Functions

#### readUsage
- **Purpose:** Sum per-spawn token usage from a subagent transcript file: whole-file read in its own try, one JSON.parse per line (torn lines skipped), sums input/output/cacheWrite/cacheRead over assistant entries with a message.usage object, distinct message.model values joined first-seen with '+', returns null when no entry qualified
- **Location:** harness/hooks/sdd-activity.sh (inline node, after the stdin JSON.parse)
- **Signature:** readUsage(p) => { input, output, cacheWrite, cacheRead, tokens, model } | null
- **Exported:** No

#### num
- **Purpose:** Coerce a value to a finite number or 0 for safe usage summing
- **Location:** harness/hooks/sdd-activity.sh (inline node)
- **Signature:** num(x) => number
- **Exported:** No

### Integrations

#### Integration
- **Description:** The SubagentStop hook is now the single writer of the spawn.end ledger row for every sdd-* agent. The row shape is { ts, type:'spawn.end', run, spec, agent, input, output, cacheWrite, cacheRead, tokens, model } with numeric values as decimal digit strings and model omitted when empty; or { ts, type:'spawn.end', run, spec, agent, tokens:'unknown' } when the transcript is absent or unusable. src/watch/ledger.ts buildModel pairs these onto SpawnNode.
- **Frontend Component:** harness/hooks/sdd-activity.sh SubagentStop branch
- **Backend Endpoint:** <spec dir>/harness-events.jsonl (LedgerEvent rows)
- **Data Flow:** SubagentStop payload transcript_path -> readUsage sums message.usage over assistant entries -> spawn.end row appended after the agent.stop activity line -> src/watch/ledger.ts and src/watch/usage.ts fold it

