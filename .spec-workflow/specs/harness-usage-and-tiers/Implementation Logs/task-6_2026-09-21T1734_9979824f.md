# Implementation Log: Task 6

**Summary:** Added the `usage` action to the harness tool. It folds one spec's harness-events.jsonl into the tokens-and-spawns-by-phase report from task 4's src/watch/usage.ts (buildUsageReport, usageDelta, formatUsageTable), and compares two specs when compareSpecName is given. The action reads only under the spec store via PathUtils.safeJoin, spawns no process; a missing spec dir or a safeJoin throw returns success:false naming the path, a missing ledger yields runs 0 with success:true. Also updated the tool enum, description, JSDoc, dispatch, added the compareSpecName input property, extended the harness tests, and rewrote the harness section (now five actions, with gate and usage) plus the tool index row in docs/TOOLS-REFERENCE.md.

**Timestamp:** 2026-09-21T17:34:16.768Z
**Log ID:** 9979824f-f747-4ce4-b649-9b89e0bb82e0

---

## Statistics

- **Lines Added:** +200
- **Lines Removed:** -15
- **Files Changed:** 3
- **Net Change:** 185

## Files Modified
- src/tools/harness.ts
- src/tools/__tests__/harness.test.ts
- docs/TOOLS-REFERENCE.md

## Files Created
_No files created_

---

## Artifacts

### Functions

#### usageAction
- **Purpose:** Harness `usage` action handler: folds one or two spec ledgers into the tokens-and-spawns-by-phase report; returns { report } for one spec, { report, compare, delta } for two
- **Location:** src/tools/harness.ts (usage section, end of file)
- **Signature:** async function usageAction(args: any, context: ToolContext): Promise<ToolResponse>
- **Exported:** No

#### readSpecLedger
- **Purpose:** Read one spec's harness-events.jsonl under the spec store; spec dir must exist (error naming path), missing ledger is empty, safeJoin throw returns error
- **Location:** src/tools/harness.ts (usage section)
- **Signature:** async function readSpecLedger(workflowRoot: string, specName: string): Promise<{ events: LedgerEvent[] } | { error: string }>
- **Exported:** No

### Integrations

#### Integration
- **Description:** The harness `usage` tool action calls the pure usage fold module from task 4
- **Frontend Component:** harness tool usageAction (src/tools/harness.ts)
- **Backend Endpoint:** buildUsageReport / usageDelta / formatUsageTable (src/watch/usage.ts)
- **Data Flow:** usageAction reads harness-events.jsonl via readSpecLedger -> parseJsonl -> buildUsageReport(events, spec); with compareSpecName it builds a second report, computes usageDelta, and formatUsageTable(report, compare); returns message=table and data={report, compare, delta}

